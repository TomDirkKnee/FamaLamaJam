---
phase: 03-server-authoritative-timing-sync
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_remote_interval_alignment_tests.cpp
autonomous: true
requirements:
  - SYNC-01
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - Remote participant audio switches only on shared server interval boundaries, never at packet arrival time.
    - A remote interval that is not fully buffered by its intended boundary is skipped rather than inserted late or mid-interval.
    - Reconnect and timing changes preserve boundary alignment across sources instead of letting each remote stream drift independently.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Remote interval queue and active-playback state capable of tracking target boundary ownership.
      contains: remoteQueuedIntervalsBySource_
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Boundary-quantized remote interval promotion and skip-on-late behavior driven by the authoritative timing engine.
      contains: processBlock
    - path: tests/integration/plugin_remote_interval_alignment_tests.cpp
      provides: Multi-source boundary alignment, late-interval skip, and reconnect re-alignment coverage for remote playback.
      min_lines: 60
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/net/FramedSocketTransport.h
      via: Decoded remote frames are assigned to authoritative boundary slots instead of activating immediately on arrival.
      pattern: popInboundFrame
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Remote promotion happens during timing boundary advancement, not when the active queue merely empties.
      pattern: remoteQueuedIntervalsBySource_
    - from: tests/integration/plugin_remote_interval_alignment_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Integration tests verify shared-boundary playback across multiple sources and silence on late intervals.
      pattern: FamaLamaJamAudioProcessor
---

<objective>
Implement boundary-quantized remote interval playback for Phase 3.

Purpose: Enforce the user’s locked Ninjam-like alignment rule so remote participants enter together on shared server boundaries and late intervals are skipped instead of being inserted mid-interval.
Output: Processor remote-queue semantics tied to the authoritative timing engine and integration coverage for multi-source boundary alignment, late skips, and reconnect re-alignment.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-server-authoritative-timing-sync/03-CONTEXT.md
@.planning/phases/03-server-authoritative-timing-sync/03-RESEARCH.md
@.planning/phases/03-server-authoritative-timing-sync/03-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/net/FramedSocketTransport.h
@tests/integration/support/MiniNinjamServer.h
@.planning/phases/03-server-authoritative-timing-sync/03-01-PLAN.md

<interfaces>
From src/net/FramedSocketTransport.h:
```cpp
struct InboundFrame
{
    juce::MemoryBlock payload;
    std::string sourceId;
};

bool popInboundFrame(InboundFrame& frame);
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
std::unordered_map<std::string, std::deque<juce::AudioBuffer<float>>> remoteQueuedIntervalsBySource_;
std::unordered_map<std::string, std::pair<juce::AudioBuffer<float>, int>> remoteActiveIntervalBySource_;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock boundary-quantized remote playback behavior with integration tests</name>
  <files>tests/integration/plugin_remote_interval_alignment_tests.cpp</files>
  <action>Create `plugin_remote_interval_alignment_tests.cpp` using the shared `MiniNinjamServer` harness from 03-01. Cover three cases explicitly: multiple remote sources that are ready before the same boundary switch together, a late interval is skipped instead of starting mid-interval, and reconnect plus resumed timing realigns remote playback to the new authoritative boundary sequence.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_remote_interval_alignment</automated></verify>
  <done>The dedicated remote alignment test file exists and encodes the locked boundary-switching rules before processor mixing logic is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace arrival-time remote promotion with shared-boundary activation</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_remote_interval_alignment_tests.cpp</files>
  <behavior>
    - Test 1: Two remote users with full intervals ready for the same boundary become active together on that boundary.
    - Test 2: A remote interval that misses its boundary never becomes active late and remains silent until a later valid interval is available.
    - Test 3: A BPM or BPI change only affects remote interval switching on the next boundary, matching the authoritative timing engine.
  </behavior>
  <action>Refactor remote queue bookkeeping so decoded intervals are queued for a target authoritative boundary instead of being promoted whenever `activeBySource` empties. Activate eligible intervals only during boundary advancement from the timing engine created in 03-01, and keep per-source silence when a source misses the boundary rather than letting it catch up mid-interval.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_remote_interval_alignment</automated></verify>
  <done>Remote playback no longer follows decode arrival timing; it follows shared authoritative boundaries with deterministic skip-on-late behavior across sources.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure reconnect and boundary changes preserve remote alignment</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_remote_interval_alignment_tests.cpp</files>
  <action>Clear or re-stage remote active and queued intervals appropriately when reconnect or timing loss invalidates the current boundary sequence, then resume only from later authoritative boundaries once timing returns. Keep the implementation strict and Ninjam-like: no mid-interval insertion, no late catch-up playback, and no hidden per-source local clocks.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_remote_interval_alignment</automated></verify>
  <done>Reconnect and timing resets do not leave stale remote audio active, and automated alignment tests prove remote playback re-enters only on valid later boundaries.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c '"C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs-ninja --config Debug --target famalamajam_tests'`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_remote_interval_alignment`

Phase-level verification criteria (applies after all Phase 3 plans finish):
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R "(plugin_server_timing_sync|plugin_transport_ui_sync|plugin_remote_interval_alignment)"`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure`
- [ ] Host validation confirms remote users switch together on interval boundaries and late intervals stay silent instead of entering mid-interval.
</verification>

<success_criteria>
- All tasks completed
- Remote playback obeys the locked server-boundary switching rules from 03-CONTEXT.md
- SYNC-01 authoritative interval behavior extends to remote playback, not just local transport display
- SYNC-03 alignment remains stable across reconnect and long-run playback scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-authoritative-timing-sync/03-03-SUMMARY.md`
</output>
