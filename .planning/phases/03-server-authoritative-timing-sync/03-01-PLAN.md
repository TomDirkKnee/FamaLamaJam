---
phase: 03-server-authoritative-timing-sync
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/net/FramedSocketTransport.h
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_server_timing_sync_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - SYNC-01
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - Plugin follows one connection-scoped authoritative interval clock driven by server BPM and BPI with no free-running local fallback while timing is unavailable.
    - Mid-session server timing changes do not cut the current interval; they take effect on the next shared boundary.
    - Reconnect or timing loss clears beat and interval state immediately so a later reconnect resumes from fresh authoritative timing rather than accumulated local drift.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Transport and timing state contract exposed to the editor and tests, including authoritative timing availability and processor-owned timing state.
      contains: struct TransportUiState
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Audio-thread timing advancement, boundary-deferred BPM/BPI application, and reconnect/timing reset behavior.
      contains: processBlock
    - path: tests/integration/plugin_server_timing_sync_tests.cpp
      provides: Integration coverage for authoritative timing progression, deferred config changes, and reconnect reset behavior.
      min_lines: 60
    - path: tests/integration/support/MiniNinjamServer.h
      provides: Shared mini-server harness for Phase 3 timing and boundary tests so later plans do not duplicate protocol scaffolding.
      contains: class MiniNinjamServer
  key_links:
    - from: src/net/FramedSocketTransport.h
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Processor reads authoritative BPM and BPI updates from the transport timing config instead of inventing local timing.
      pattern: getServerTimingConfig
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_server_timing_sync_tests.cpp
      via: Integration tests drive processor timing transitions and validate boundary application plus reset semantics.
      pattern: getTransportUiState
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/ConnectionLifecycleController.cpp
      via: Lifecycle transitions to reconnecting, error, or idle clear authoritative timing and prevent fake countdown progression.
      pattern: applyLifecycleTransition
---

<objective>
Implement the Phase 3 timing core and validation harness.

Purpose: Establish one server-authoritative timing engine in the processor so interval boundaries, BPM/BPI changes, and reconnect recovery all operate from the same sample-domain source of truth.
Output: Shared Phase 3 test harness, authoritative timing integration coverage, and processor timing state that resets honestly on timing loss and applies timing updates only on interval boundaries.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-server-authoritative-timing-sync/03-CONTEXT.md
@.planning/phases/03-server-authoritative-timing-sync/03-RESEARCH.md
@.planning/phases/03-server-authoritative-timing-sync/03-VALIDATION.md
@.planning/phases/02-connection-lifecycle-recovery/02-03-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/net/FramedSocketTransport.h
@tests/integration/plugin_experimental_transport_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct TransportUiState
{
    bool connected { false };
    bool hasServerTiming { false };
    int beatsPerMinute { 0 };
    int beatsPerInterval { 0 };
    int currentBeat { 0 };
    float intervalProgress { 0.0f };
};

[[nodiscard]] TransportUiState getTransportUiState() const noexcept;
void handleConnectionEvent(const app::session::ConnectionEvent& event);
```

From src/net/FramedSocketTransport.h:
```cpp
struct ServerTimingConfig
{
    int beatsPerMinute { 0 };
    int beatsPerInterval { 0 };
};

bool getServerTimingConfig(ServerTimingConfig& config) const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the reusable Phase 3 mini-server harness and register timing tests</name>
  <files>tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_server_timing_sync_tests.cpp, tests/CMakeLists.txt</files>
  <action>Extract the existing protocol harness from `plugin_experimental_transport_tests.cpp` into `tests/integration/support/MiniNinjamServer.h`, then create `plugin_server_timing_sync_tests.cpp` with initial scenarios for authoritative timing progression, deferred BPM/BPI application, and reconnect reset. Update `tests/CMakeLists.txt` to register the new test file now so later Phase 3 plans can add their own files without reworking the harness layout.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_server_timing_sync</automated></verify>
  <done>The shared mini-server header exists, the timing integration test file is registered in CMake, and the initial Phase 3 timing tests compile and run from the dedicated test filter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authoritative timing state with boundary-deferred server config application</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_server_timing_sync_tests.cpp</files>
  <behavior>
    - Test 1: Initial server timing config starts beat and interval progress only after authoritative timing is present.
    - Test 2: A BPM or BPI change received mid-interval does not rescale the current interval and becomes active only on the next boundary.
    - Test 3: The processor reports stable beat and interval progress from sample-domain advancement without UI timer ownership.
  </behavior>
  <action>Refactor the processor timing path into one connection-scoped authoritative timing state advanced only from `processBlock`. Stage new beat and interval sample counts when transport timing config changes, but commit them only when the current interval boundary is reached. Preserve the existing metronome control, but source it from the authoritative beat transitions instead of any free-running local estimation.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_server_timing_sync</automated></verify>
  <done>Processor timing no longer rescales the active interval in place, authoritative beat and interval progress come from one timing state, and timing tests prove boundary-deferred config changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reset timing honestly on disconnect, reconnect, and timing loss</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_server_timing_sync_tests.cpp</files>
  <behavior>
    - Test 1: Entering reconnecting or error clears `hasServerTiming`, beat, interval progress, and active metronome clicks immediately.
    - Test 2: After reconnect, timing does not resume until fresh authoritative server timing arrives.
    - Test 3: A rehearsal-length progression test shows interval indices stay aligned without cumulative drift.
  </behavior>
  <action>Wire lifecycle-driven timing resets into the processor so reconnect, disconnect, and timing-loss paths all stop the metronome and clear displayed transport state instead of continuing a local countdown. Add the long-run integration coverage required by `SYNC-03`, keeping the timing engine sample-domain and avoiding any wall-clock fallback behavior.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_server_timing_sync</automated></verify>
  <done>Timing resets are immediate and honest across lifecycle failures, reconnect waits for real server timing before resuming, and long-run timing coverage proves no cumulative drift in the authoritative path.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c '"C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs-ninja --config Debug --target famalamajam_tests'`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_server_timing_sync`

Phase-level verification criteria (applies after all Phase 3 plans finish):
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R "(plugin_server_timing_sync|plugin_transport_ui_sync|plugin_remote_interval_alignment)"`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure`
- [ ] Ableton validation confirms timing waits honestly during reconnect and resumes only after authoritative timing returns.
</verification>

<success_criteria>
- All tasks completed
- SYNC-01 authoritative timing semantics are implemented in the processor without local drift-prone fallback
- SYNC-03 long-run timing stability and reconnect realignment are covered by dedicated automation
- Shared test harness exists for downstream Phase 3 UI and remote-boundary plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-authoritative-timing-sync/03-01-SUMMARY.md`
</output>
