---
phase: 03-server-authoritative-timing-sync
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_transport_ui_sync_tests.cpp
autonomous: true
requirements:
  - SYNC-02
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - User can see whether sync is waiting, healthy, reconnecting, or timing-lost instead of inferring timing from a generic connected state.
    - Current beat and interval progress remain legible in the main UI, with beat-divided progress and an understandable but subtle boundary rollover.
    - Metronome control stays available as a simple on and off sync aid, but it only behaves as active when authoritative timing is present.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Expanded transport UI state for explicit sync-health projection from processor to editor.
      contains: struct TransportUiState
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Beat-divided interval progress rendering, explicit sync status wording, and metronome toggle behavior aligned with timing state.
      contains: refreshTransportStatus
    - path: tests/integration/plugin_transport_ui_sync_tests.cpp
      provides: Integration coverage for waiting, reconnecting, timing-lost, healthy beat/progress, and metronome-state snapshots.
      min_lines: 60
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Processor publishes authoritative sync-health and beat/progress data that the editor renders directly instead of deriving locally.
      pattern: getTransportUiState
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/app/session/ConnectionLifecycle.h
      via: Transport wording stays coherent with lifecycle reconnect and error states while preserving concise status text patterns from Phase 2.
      pattern: ConnectionState
    - from: tests/integration/plugin_transport_ui_sync_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: UI-facing integration tests validate the explicit waiting and reconnect display plus beat-divided progress snapshots.
      pattern: refreshTransportStatus
---

<objective>
Implement the visible sync state and metronome UI behavior for Phase 3.

Purpose: Make the authoritative timing engine understandable in the plugin UI so users can see beat and interval state, recognize missing timing immediately, and rely on the metronome only when server timing is real.
Output: Expanded processor transport UI state, editor rendering for explicit sync-health and beat-divided interval progress, and dedicated UI integration coverage.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-server-authoritative-timing-sync/03-CONTEXT.md
@.planning/phases/03-server-authoritative-timing-sync/03-RESEARCH.md
@.planning/phases/03-server-authoritative-timing-sync/03-VALIDATION.md
@.planning/phases/02-connection-lifecycle-recovery/02-03-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@.planning/phases/03-server-authoritative-timing-sync/03-01-PLAN.md

<interfaces>
From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
struct TransportUiState
{
    bool connected { false };
    bool hasServerTiming { false };
    int beatsPerMinute { 0 };
    int beatsPerInterval { 0 };
    int currentBeat { 0 };
    float intervalProgress { 0.0f };
};

using TransportUiGetter = std::function<TransportUiState()>;
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
[[nodiscard]] TransportUiState getTransportUiState() const noexcept;
[[nodiscard]] bool isMetronomeEnabled() const noexcept;
void setMetronomeEnabled(bool enabled) noexcept;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit sync-health fields to the processor-to-editor transport state</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, tests/integration/plugin_transport_ui_sync_tests.cpp</files>
  <action>Extend the transport UI contract with explicit sync-health information that distinguishes waiting for timing, healthy authoritative timing, reconnecting, and timing lost. Keep this projection sourced from the processor and lifecycle snapshot rather than inventing new editor-side heuristics, and create `plugin_transport_ui_sync_tests.cpp` to lock the expected snapshots before editor rendering changes.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_transport_ui_sync</automated></verify>
  <done>The editor-facing transport state contains enough information to render explicit timing-health wording and tests assert the state transitions for waiting, reconnecting, and healthy timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render beat-divided interval progress and concise sync messaging in the editor</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp</files>
  <behavior>
    - Test 1: When timing is absent but lifecycle is active, the transport label shows an explicit waiting-for-timing or reconnecting message rather than beat progress.
    - Test 2: When timing is healthy, the transport label and progress presentation expose current beat plus beat-divided interval progress.
    - Test 3: Timing loss clears the apparent transport movement and preserves concise, actionable wording.
  </behavior>
  <action>Update editor transport rendering so healthy sync prioritizes current beat plus beat-divided interval progress while keeping the rollover subtle, as locked in `03-CONTEXT.md`. Preserve the existing single-line concise status pattern from Phase 2 and do not add deferred diagnostics panels or richer mix controls.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_transport_ui_sync</automated></verify>
  <done>The editor no longer presents sync as a generic long progress sweep, waiting and reconnecting are explicit, and automated UI-facing integration tests cover the rendered transport states.</done>
</task>

<task type="auto">
  <name>Task 3: Keep metronome control aligned to authoritative timing availability</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp</files>
  <action>Preserve the simple metronome on and off control, but ensure the audible click path and UI behavior only operate when authoritative timing is present. On reconnect or timing loss, stop any active click immediately and keep the control behavior honest about missing timing. Do not introduce routing, level, or pan controls for the metronome in this phase.</action>
  <verify><automated>ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_transport_ui_sync</automated></verify>
  <done>Metronome behavior remains user-controllable, stops on timing loss, and the Phase 3 UI tests prove it stays aligned to authoritative timing availability.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c '"C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs-ninja --config Debug --target famalamajam_tests'`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R plugin_transport_ui_sync`

Phase-level verification criteria (applies after all Phase 3 plans finish):
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure --timeout 30 -R "(plugin_server_timing_sync|plugin_transport_ui_sync|plugin_remote_interval_alignment)"`
- [ ] `ctest --test-dir build-vs-ninja --output-on-failure`
- [ ] Ableton UI check confirms beat segmentation is readable, interval rollover is subtle, and reconnect wording remains explicit and concise.
</verification>

<success_criteria>
- All tasks completed
- SYNC-02 transport state is visible and explicit in the editor without fake timing fallback
- Metronome remains a simple authoritative sync aid and does not expand into deferred control scope
- UI coverage locks the intended transport states before Phase 3 closes
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-authoritative-timing-sync/03-02-SUMMARY.md`
</output>
