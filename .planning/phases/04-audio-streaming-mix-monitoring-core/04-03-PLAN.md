---
phase: 04-audio-streaming-mix-monitoring-core
plan: "03"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/audio/CodecStreamBridge.h
  - src/audio/CodecStreamBridge.cpp
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_streaming_runtime_compat_tests.cpp
  - tests/integration/plugin_codec_runtime_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
user_setup: []
must_haves:
  truths:
    - Supported host sample-rate and buffer-size changes do not reintroduce pitch or tempo artifacts in either outbound or inbound streaming.
    - Streaming state is reconfigured coherently on host runtime changes without stale interval buffers, stale meter state, or stale decode assumptions.
    - The existing send/receive path stays Ninjam-compatible while becoming robust under supported host reconfiguration scenarios.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Host reconfiguration handling for streaming buffers, decoder state, and runtime compatibility guarantees.
      contains: prepareToPlay
    - path: src/audio/CodecStreamBridge.cpp
      provides: Bridge behavior that remains correct under runtime sample-rate and block-size changes.
      contains: submitInput
    - path: tests/integration/plugin_streaming_runtime_compat_tests.cpp
      provides: Dedicated coverage for active streaming across supported sample rates and buffer sizes.
      min_lines: 80
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/audio/CodecStreamBridge.cpp
      via: Processor host reconfiguration logic keeps outbound and inbound codec state aligned with the current host format.
      pattern: currentSampleRate_
    - from: tests/integration/plugin_streaming_runtime_compat_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Integration tests drive runtime reconfiguration while transport send and receive remain active.
      pattern: prepareToPlay
---

<objective>
Validate and harden Phase 4 sample-rate and buffer-size compatibility.

Purpose: Make the current streaming core reliable under supported host runtime changes so the earlier sample-rate bug does not return in another form.
Output: Explicit runtime compatibility handling in the processor/codec bridge plus dedicated integration coverage for supported sample rates and block sizes during active streaming.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-CONTEXT.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-RESEARCH.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-VALIDATION.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-01-PLAN.md
@src/audio/CodecStreamBridge.h
@src/audio/CodecStreamBridge.cpp
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_codec_runtime_tests.cpp
@tests/integration/plugin_experimental_transport_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfigure streaming buffers and codec state safely on host format changes</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, src/audio/CodecStreamBridge.h, src/audio/CodecStreamBridge.cpp</files>
  <action>Audit and harden the processor and codec bridge paths so supported `prepareToPlay` sample-rate or block-size changes reset or resize all relevant streaming buffers, interval upload state, decode assumptions, and meter state coherently. Preserve the current Ninjam-compatible interval upload model while preventing stale sample-rate state from surviving a host reconfiguration.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_codec_runtime],[plugin_experimental_transport]"'</automated></verify>
  <done>Host format changes no longer leave stale streaming state behind, and the codec bridge remains aligned to the current sample-rate expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add active-streaming compatibility coverage for supported sample rates and block sizes</name>
  <files>tests/integration/plugin_streaming_runtime_compat_tests.cpp, tests/integration/plugin_codec_runtime_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Streaming remains functional after switching between supported sample rates such as 44.1 kHz and 48 kHz.
    - Test 2: Representative supported block-size changes do not break send/receive continuity or reintroduce pitch artifacts.
    - Test 3: The local upload path and remote playback path both remain active after host reconfiguration.
  </behavior>
  <action>Create dedicated runtime compatibility tests that exercise active send and receive behavior across supported host sample-rate and block-size changes. Reuse the existing transport and codec runtime harness where possible so the compatibility coverage stays close to the real streaming path rather than a synthetic unit-only abstraction.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_streaming_runtime_compat],[plugin_codec_runtime]"'</automated></verify>
  <done>Dedicated integration coverage proves supported sample-rate and block-size changes do not break active streaming or alter pitch/tempo behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Close the loop with full Phase 4 streaming validation</name>
  <files>tests/integration/plugin_streaming_runtime_compat_tests.cpp, tests/integration/plugin_experimental_transport_tests.cpp</files>
  <action>Use the completed runtime compatibility suite plus the existing transport and remote-alignment tests as the full streaming validation gate for Phase 4. Ensure the test set covers the current send path, receive path, and runtime compatibility together, and tighten any existing transport/runtime tests that are too weak to catch a regression in continuity or sample-rate handling.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'</automated></verify>
  <done>Phase 4 has a credible automated validation gate for send, receive, and runtime compatibility, and the remaining manual Ableton checks are limited to musician-facing confidence rather than basic streaming correctness.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_streaming_runtime_compat],[plugin_codec_runtime],[plugin_experimental_transport]"'`

Phase-level verification criteria (applies after all Phase 4 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_remote_interval_alignment],[plugin_codec_runtime],[plugin_mixer_controls],[plugin_mixer_ui],[plugin_streaming_runtime_compat]"'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Ableton validation confirms supported host sample rates and buffer sizes do not produce pitch drift, continuity loss, or broken send/receive behavior.
</verification>

<success_criteria>
- All tasks completed
- AUD-03 compatibility is explicitly implemented and covered by dedicated runtime tests
- Existing sample-rate fixes remain protected under active streaming conditions
- Phase 4 closes with a credible automated gate for send, receive, and compatibility behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-streaming-mix-monitoring-core/04-03-SUMMARY.md`
</output>
