---
phase: 04-audio-streaming-mix-monitoring-core
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
autonomous: true
requirements:
  - MIX-01
  - MIX-02
user_setup: []
must_haves:
  truths:
    - The editor presents a dedicated local monitor strip and grouped remote strips so users can distinguish live monitoring from delayed returns.
    - Remote strips are grouped by user but controlled at user-plus-channel granularity with stable order and persistent settings.
    - Strip controls and meters reflect processor-owned state; the editor is a consumer, not the source of truth.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.h
      provides: Editor-visible mixer strip contracts and test accessors for grouped local/remote strip rendering.
      contains: class FamaLamaJamAudioProcessorEditor
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Dedicated local strip, grouped remote strip layout, labels, controls, and simple realtime meter rendering.
      contains: resized
    - path: tests/integration/plugin_mixer_ui_tests.cpp
      provides: Integration coverage for local-vs-remote distinction, grouped remote strips, stable ordering, and inactive-retention behavior.
      min_lines: 80
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Editor renders mixer strips from processor-published strip snapshots and persisted control state.
      pattern: getter
    - from: tests/integration/plugin_mixer_ui_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: UI integration tests lock stable ordering, user grouping, and strip visibility semantics.
      pattern: testing
---

<objective>
Build the Phase 4 mixer UI for local and remote monitoring.

Purpose: Turn the processor-owned strip state from Plan 04-01 into a practical JUCE mixer surface that clearly separates local monitoring from delayed remote return and exposes the required per-channel controls.
Output: Dedicated local strip UI, grouped remote strip UI, editor bindings for meter/gain/pan/mute, and integration coverage for the locked strip behaviors.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-CONTEXT.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-RESEARCH.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-VALIDATION.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-01-PLAN.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated local strip and grouped remote strips to the editor</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <action>Refactor the editor layout to include a dedicated local monitor strip and grouped remote strips while preserving the existing simple JUCE UI style. Group remote channels under each user heading, keep stable user/channel order, and ensure strip labels prioritize username plus channel name with channel index available as secondary detail when metadata is incomplete.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_ui]"'</automated></verify>
  <done>The editor shows a dedicated local strip and grouped remote strips with stable ordering and labels consistent with the locked Phase 4 context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind strip controls and simple realtime meters to processor-owned mixer state</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <behavior>
    - Test 1: Local and remote strips display simple realtime meter activity from processor-published snapshots.
    - Test 2: Gain, pan, and mute controls on a strip update the targeted processor-owned mix state and remain stable across refreshes.
    - Test 3: The UI makes the local monitor path visually distinct from interval-delayed remote return even when both are active.
  </behavior>
  <action>Wire the editor to the processor-owned strip state and control handlers built in Plan 04-01 so strip meters and controls reflect live mixer state without moving control ownership into the editor. Keep meter behavior simple and realtime, and do not introduce solo, peak-hold diagnostics, or advanced routing controls in this phase.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_ui],[plugin_mixer_controls]"'</automated></verify>
  <done>Strip controls and meters are live, targeted, and processor-owned, and the editor clearly distinguishes local monitoring from delayed remote audio.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve inactive-strip retention and persisted UI restoration semantics</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_mixer_controls_tests.cpp</files>
  <action>Implement the brief inactive-strip retention behavior and ensure restored per-channel mix settings are reflected when strips reappear after reconnect or project reload. Keep the timeout implementation simple and deterministic, and verify that strips do not disappear immediately on brief inactivity while still eventually clearing from the UI when a source remains gone.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_ui],[plugin_mixer_controls]"'</automated></verify>
  <done>Remote strips remain visible briefly after inactivity, eventually hide when gone, and restore their persisted mix settings when the same sources reappear.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_ui],[plugin_mixer_controls]"'`

Phase-level verification criteria (applies after all Phase 4 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_remote_interval_alignment],[plugin_codec_runtime],[plugin_mixer_controls],[plugin_mixer_ui],[plugin_streaming_runtime_compat]"'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Ableton validation confirms the local strip is clearly identifiable as live monitoring and remote strips behave like delayed returns with independent controls.
</verification>

<success_criteria>
- All tasks completed
- MIX-01 essential controls are available on local and remote strips
- MIX-02 local-vs-remote distinction is clear in the UI
- UI behavior for grouping, ordering, and inactive retention is locked by tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-streaming-mix-monitoring-core/04-02-SUMMARY.md`
</output>
