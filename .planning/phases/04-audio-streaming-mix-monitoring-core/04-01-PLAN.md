---
phase: 04-audio-streaming-mix-monitoring-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/net/FramedSocketTransport.h
  - src/net/FramedSocketTransport.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_mixer_controls_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - MIX-01
user_setup: []
must_haves:
  truths:
    - The existing interval-based send and receive path remains the streaming foundation and is not replaced by a new protocol model.
    - Local and remote sources are tracked through stable identities suitable for per-channel gain, pan, mute, meter, and reconnect/project restore behavior.
    - Processor-owned mix state, not editor-owned state, is the source of truth for strip controls.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Source and mix-state contracts for local and remote strips, including stable identities and editor-visible snapshots.
      contains: struct
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: RT-safe source bookkeeping, mix-state application, meter snapshot updates, and restore hooks for remote channels.
      contains: processBlock
    - path: src/net/FramedSocketTransport.cpp
      provides: Remote channel metadata and source identity plumbing needed to build grouped strips and restore mix state by user plus channel.
      contains: handleUserInfo
    - path: tests/integration/plugin_mixer_controls_tests.cpp
      provides: Integration coverage for per-channel gain, pan, mute, and persisted restore behavior.
      min_lines: 80
  key_links:
    - from: src/net/FramedSocketTransport.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Transport source identity and user/channel metadata drive processor-owned remote strip state and restore keys.
      pattern: sourceId
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_mixer_controls_tests.cpp
      via: Integration tests validate local and remote per-channel control effects and persistence from the processor contract.
      pattern: getStateInformation
---

<objective>
Build the Phase 4 streaming and mix-state foundation.

Purpose: Keep the existing Ninjam send/receive path intact while introducing stable source identity, RT-safe per-channel mix state, and simple meter snapshots that later UI work can consume.
Output: Processor-owned local/remote strip state, transport metadata sufficient for user-plus-channel grouping, and integration coverage for per-channel gain, pan, mute, and persistence.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-CONTEXT.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-RESEARCH.md
@.planning/phases/04-audio-streaming-mix-monitoring-core/04-VALIDATION.md
@src/net/FramedSocketTransport.h
@src/net/FramedSocketTransport.cpp
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_experimental_transport_tests.cpp
@tests/integration/plugin_remote_interval_alignment_tests.cpp

<interfaces>
From src/net/FramedSocketTransport.h:
```cpp
struct InboundFrame
{
    juce::MemoryBlock payload;
    std::string sourceId;
};
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
[[nodiscard]] std::size_t getActiveRemoteSourceCountForTesting() const noexcept;
[[nodiscard]] std::vector<std::string> getActiveRemoteSourceIdsForTesting() const;
void getStateInformation(juce::MemoryBlock& destData) override;
void setStateInformation(const void* data, int sizeInBytes) override;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend transport and processor source state for user-plus-channel mixer identity</name>
  <files>src/net/FramedSocketTransport.h, src/net/FramedSocketTransport.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp</files>
  <action>Extend the transport-to-processor path so remote channels carry enough metadata to be represented as stable mixer strips grouped by user and keyed by user plus channel. Keep the existing interval-based upload/download flow intact, but add processor-owned source descriptors for the local monitor strip and remote delayed strips, with stable ordering and brief inactive retention metadata rather than editor-owned discovery logic.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_remote_interval_alignment]"'</automated></verify>
  <done>Remote channels are represented through stable user-plus-channel identities and metadata suitable for grouped strip UI, while the existing transport send/receive behavior stays intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add processor-owned per-channel gain, pan, mute, and simple meter snapshots</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_mixer_controls_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Remote channel gain, pan, and mute affect only the targeted user-plus-channel strip, not all remote audio.
    - Test 2: The dedicated local monitor strip exposes its own meter and gain/pan/mute state distinct from delayed remote returns.
    - Test 3: Strip meters publish simple realtime levels without replacing the authoritative interval playback model.
  </behavior>
  <action>Create a processor-owned mix-state layer for local and remote strips, apply that state in the audio render path, and publish simple realtime meter snapshots for editor consumption. Register dedicated integration coverage for per-channel control behavior before relying on the new state in the UI.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls]"'</automated></verify>
  <done>The processor exposes per-channel gain/pan/mute and meter snapshots for both local and remote strips, and integration tests prove the controls act on the correct channels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist per-channel mix state in plugin restore data</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_mixer_controls_tests.cpp</files>
  <behavior>
    - Test 1: Project state save and restore preserves per-remote-channel gain, pan, and mute by stable source identity.
    - Test 2: A reconnect or source reappearance reuses previously restored mix state for the same user-plus-channel identity.
    - Test 3: Remote mix persistence does not pollute the static session settings serializer used for endpoint/default configuration.
  </behavior>
  <action>Extend the plugin's wrapped state tree to store dynamic per-channel mix settings for local and remote strips, restoring them when the same source identities appear again. Keep this persistence in plugin state rather than `SessionSettingsSerializer`, and validate that reconnect/project reload semantics are correct in the mixer-controls integration suite.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls]"'</automated></verify>
  <done>Per-channel mix settings survive project restore and reconnect for the same source identities, and the persistence boundary remains separated from static session settings.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_remote_interval_alignment],[plugin_mixer_controls]"'`

Phase-level verification criteria (applies after all Phase 4 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_remote_interval_alignment],[plugin_codec_runtime],[plugin_mixer_controls],[plugin_mixer_ui],[plugin_streaming_runtime_compat]"'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Ableton validation confirms remote strips keep their balances after reconnect and project reload for the same user-plus-channel identities.
</verification>

<success_criteria>
- All tasks completed
- AUD-01 and AUD-02 keep using the existing interval-based streaming core
- MIX-01 has a processor-owned per-channel control and persistence foundation
- Local and remote strip state is stable enough for Phase 4 UI work to consume directly
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-streaming-mix-monitoring-core/04-01-SUMMARY.md`
</output>
