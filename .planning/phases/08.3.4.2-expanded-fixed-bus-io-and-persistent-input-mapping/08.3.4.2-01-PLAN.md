---
phase: 08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - tests/integration/plugin_host_multi_io_routing_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md
autonomous: true
requirements:
  - ROUTE-04
  - ROUTE-05
  - ROUTE-06
user_setup: []
must_haves:
  truths:
    - The widened fixed-bus phase starts from an explicit `8`-input and `8`-output contract with stable local-slot and remote-route labels.
    - The chosen lifecycle expectations are explicit before implementation: reveal the next hidden slot, preserve hidden-slot state, and reset remote outputs to main on reopen.
    - The phase has dedicated 08.3.4.2 host-validation evidence for the practical input/output ceiling and lifecycle behavior in Ableton.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Fixed-slot `8x8` routing contracts, widened local-slot descriptors, and widened host-facing output labels.
      contains: kFixedLocalRoutingSlots
    - path: tests/integration/plugin_host_multi_io_routing_tests.cpp
      provides: RED routing coverage for widened fixed-bus counts, widened route normalization, and loop-safe slot processing seams.
      min_lines: 300
    - path: tests/integration/plugin_mixer_ui_tests.cpp
      provides: RED coverage for reveal-next-hidden-slot behavior and visible-but-disabled slot presentation.
      min_lines: 350
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: RED coverage for exact-slot restore, hidden-slot persistence, and remote-route reset on reopen.
      min_lines: 500
    - path: docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md
      provides: Manual Ableton matrix for practical bus ceiling, local lifecycle, and restore semantics.
      contains: P8.3.4.2-CEILING-01
  key_links:
    - from: tests/integration/plugin_host_multi_io_routing_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.h
      via: The tests must lock widened slot and route tables before runtime implementation starts.
      pattern: kFixedLocalRoutingSlots|kFixedRemoteOutputRoutes|Remote Out 7|Local Send
    - from: tests/integration/plugin_state_roundtrip_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Restore expectations must fail until exact-slot persistence and remote-route reset are implemented.
      pattern: visible|remoteOutput|local-send-|setStateInformation|getStateInformation
    - from: docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md
      to: .planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-VALIDATION.md
      via: The manual matrix and validation strategy must describe the same ceiling and reopen expectations.
      pattern: ROUTE-04|ROUTE-05|ROUTE-06|Ableton|reopen
---

<objective>
Lock the executable Wave 0 contract for the widened fixed-bus phase: fixed-slot `8x8` arrays, RED coverage for lifecycle and persistence rules, and a dedicated manual Ableton matrix before runtime scaling begins.

Purpose: This phase already has a working small fixed-bus runtime. The first plan must stop execution drift by pinning the widened ceiling, exact restore semantics, and session-only remote-route expectations in code contracts and failing tests before runtime code expands.
Output: Header-level or layout-level `8x8` contracts, failing routing/UI/state coverage, and a new 08.3.4.2 manual validation matrix.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-CONTEXT.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-RESEARCH.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@tests/integration/plugin_host_multi_io_routing_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct FixedLocalRoutingSlot
{
    const char* sourceId { "" };
    int inputBusIndex { 0 };
    const char* fallbackLabel { "" };
    bool visibleByDefault { false };
};

struct FixedRemoteOutputRoute
{
    int outputBusIndex { 0 };
    const char* hostLabel { "" };
};
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
static constexpr const char* kAddLocalChannelLabel = "Add channel";
void rebuildMixerStripWidgets(const std::vector<MixerStripState>& visibleStrips);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock widened fixed-slot bus and route contracts with RED routing coverage</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_host_multi_io_routing_tests.cpp</files>
  <behavior>
    - Test 1: The processor exposes fixed local-slot descriptors for `Main` plus seven extra local sends toward the `8`-input target.
    - Test 2: The processor exposes fixed remote output route descriptors for `FLJ Main Output` plus seven routed outputs toward the `8`-output target.
    - Test 3: Layout and route-normalization tests fail until the runtime supports widened fixed counts rather than the current `2` inputs and `3` outputs.
  </behavior>
  <action>Replace the hard-coded `2`-slot and `3`-route contract assumptions with widened fixed-slot arrays and target-aware layout helpers, but keep the work at contract or RED level only. Update routing tests so they compile against the widened arrays, widened host-facing labels, and target-aware helpers, and fail on the current runtime. Do not implement loop-based processing yet, do not change serialization yet, and do not add dynamic bus APIs.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"</automated>
  </verify>
  <done>The routing suite locks the widened fixed-slot target and fails for the right runtime reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED lifecycle and restore coverage plus the new Ableton matrix</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md</files>
  <behavior>
    - Test 1: `Add channel` reveals the next hidden fixed slot in order rather than creating arbitrary slots.
    - Test 2: Hidden local slots restore hidden with their previous name and mix state intact, and re-adding restores the full prior state.
    - Test 3: Reopen expectations fail until remote output routes reset to main instead of persisting across saved projects.
  </behavior>
  <action>Extend the mixer UI and state-roundtrip suites so they fail on the widened lifecycle rules chosen in context: reveal-next-hidden-slot, hide-and-remember, exact-slot restore, and session-only remote output routing. In the same task, create `docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md` with manual rows for practical Ableton input/output ceilings, disabled-slot behavior, live-remove confirmation, same-session remote reconnect, and reopen reset of remote outputs to main. Keep this work at RED or documentation level only; do not implement runtime behavior yet.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip]"</automated>
  </verify>
  <done>The UI and restore suites fail for the widened fixed-slot lifecycle rules, and the new 08.3.4.2 Ableton matrix exists.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip]"`
- [ ] `rg -n "P8\\.3\\.4\\.2-CEILING-01|ROUTE-04|ROUTE-05|ROUTE-06|Ableton|reopen" docs/validation/phase8.3.4.2-expanded-fixed-bus-routing-matrix.md`
</verification>

<success_criteria>
- The phase is locked to a widened fixed-slot model before runtime work starts
- RED coverage now encodes the new lifecycle and persistence rules instead of inheriting small-proof assumptions
- Execution can scale the runtime against explicit contracts rather than widening the current hard-coded seams ad hoc
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-01-SUMMARY.md`
</output>
