---
phase: 08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping
plan: "02"
type: execute
wave: 2
depends_on:
  - 08.3.4.2-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_host_multi_io_routing_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
autonomous: true
requirements:
  - ROUTE-04
  - ROUTE-06
user_setup: []
must_haves:
  truths:
    - The widened fixed-bus runtime works at the declared target shape instead of stopping at the current small proof counts.
    - New remote sources default to `FLJ Main Output`, same-session reconnect keeps the chosen route, and project reopen resets remote outputs to main.
    - The runtime stays aligned with the phase goal by treating the practical ceiling as host-proven evidence rather than assuming every declared bus is usable.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Widened fixed-bus runtime, loop-based per-slot processing, and session-scoped remote output routing.
      min_lines: 3600
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Stable widened slot or route descriptors and helper seams for iteration.
      contains: kFixedRemoteOutputRoutes
    - path: tests/integration/plugin_host_multi_io_routing_tests.cpp
      provides: Green runtime coverage for widened bus declarations and route defaults.
      min_lines: 320
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: Coverage that remote output routing is no longer project-persistent across reopen.
      min_lines: 520
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_host_multi_io_routing_tests.cpp
      via: Loop-based widened bus processing must satisfy the new fixed-slot routing expectations without hard-coded aux branches.
      pattern: getBusBuffer|kFixedLocalRoutingSlots|kFixedRemoteOutputRoutes
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_state_roundtrip_tests.cpp
      via: Serialization must preserve local input identity while clearing remote project-persistent routes.
      pattern: getStateInformation|setStateInformation|remoteOutput|visible
---

<objective>
Implement the widened fixed-bus runtime: expand the processor from the small proof counts toward the `8x8` target, move per-bus work onto loop-safe slot iteration, and enforce session-only remote output routing semantics.

Purpose: This is the runtime-scaling plan. It should widen the processor and routing model without opening UI redesign scope, and it should explicitly correct the current remote-route persistence behavior so it matches the locked decisions for 08.3.4.2.
Output: Green integration coverage for widened fixed buses, default-to-main remote routing, same-session reconnect retention, and no project-persistent remote output restore.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-CONTEXT.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-RESEARCH.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-VALIDATION.md
@.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-01-PLAN.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_host_multi_io_routing_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.cpp:
```cpp
bool isBusesLayoutSupported(const BusesLayout& layouts) const override;
void processBlock(juce::AudioBuffer<float>& buffer, juce::MidiBuffer&) override;
void getStateInformation(juce::MemoryBlock& destData) override;
void setStateInformation(const void* data, int sizeInBytes) override;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand processor bus declarations and route processing to the widened fixed-slot model</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_host_multi_io_routing_tests.cpp</files>
  <behavior>
    - Test 1: The processor declares the widened fixed-bus ceiling and accepts only the widened fixed layout shape.
    - Test 2: `processBlock()` uses slot or route iteration rather than one-off `auxInput` or `auxOutput2` branches for widened local sends and routed outputs.
    - Test 3: New remote sources default to `FLJ Main Output` while extra outputs stay silent unless explicitly assigned.
  </behavior>
  <action>Expand `BusesProperties()`, layout validation, and routing helpers from the current small proof counts toward the `8x8` fixed target. Refactor per-bus processing to iterate fixed local slots and fixed remote outputs instead of hard-coded single aux branches. Preserve the existing host-owned routing model and do not add dynamic buses or plugin-owned host track browsing.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"</automated>
  </verify>
  <done>The widened runtime is green for fixed-bus declaration, widened route defaults, and loop-safe per-bus processing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce session-only remote route restore semantics</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_host_multi_io_routing_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp</files>
  <behavior>
    - Test 1: Same-session remote reconnect keeps the prior output assignment when the source identity returns.
    - Test 2: Saved project reopen resets remote outputs to `FLJ Main Output` instead of restoring prior aux assignments.
    - Test 3: Local input slot identity and visibility remain eligible for later exact-slot persistence work.
  </behavior>
  <action>Adjust the remote assignment lifetime model so remote routes remain session-scoped. Preserve in-memory reconnect behavior for the active session, but clear or ignore serialized remote output routes on project restore. Keep the local slot serialization path intact so Plan 03 can finish exact-slot persistence without reworking the runtime again.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_state_roundtrip]"</automated>
  </verify>
  <done>Remote route behavior now matches the phase decision: reconnect-safe within a session, reset to main on reopen.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_state_roundtrip]"`
- [ ] `ctest --test-dir build-vs-2026 --output-on-failure -C Debug`
</verification>

<success_criteria>
- The widened fixed-bus runtime is implemented without fragile hard-coded aux branches
- Remote output routing now obeys session-only semantics
- The codebase is ready for final lifecycle/persistence UI work and the manual Ableton verdict
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.4.2-expanded-fixed-bus-io-and-persistent-input-mapping/08.3.4.2-02-SUMMARY.md`
</output>
