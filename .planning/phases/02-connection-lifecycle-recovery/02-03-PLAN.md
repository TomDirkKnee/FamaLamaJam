---
phase: 02-connection-lifecycle-recovery
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/app/session/ConnectionLifecycle.h
  - src/app/session/ConnectionLifecycle.cpp
  - src/app/session/SessionSettingsController.h
  - src/app/session/SessionSettingsController.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_connection_error_recovery_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/CMakeLists.txt
  - docs/validation/phase2-ableton-lifecycle-matrix.md
autonomous: true
requirements:
  - SESS-03
  - SESS-02
user_setup: []
must_haves:
  truths:
    - Applying corrected settings while in Error returns lifecycle state to Idle or Ready and does not auto-connect.
    - After project reopen, plugin starts disconnected and can surface brief last-error context without resuming active session.
    - UI status remains concise and actionable, including reconnect countdowns and sticky hard-failure context until healthy state or manual retry.
  artifacts:
    - path: src/app/session/ConnectionLifecycle.cpp
      provides: Explicit Error recovery transition for corrected Apply that clears retry loop state and lands in Idle or Ready without implicit connect.
      contains: ApplySettings
    - path: tests/integration/plugin_connection_error_recovery_tests.cpp
      provides: Integration coverage for corrected-settings Error recovery and manual connect restart behavior.
      min_lines: 35
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: Restore behavior checks for disconnected startup and brief last-error status context after reopen.
      min_lines: 35
    - path: docs/validation/phase2-ableton-lifecycle-matrix.md
      provides: Manual Ableton validation matrix for error correction, reopen behavior, retry countdowns, and manual restart from Error.
      min_lines: 20
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/ConnectionLifecycle.cpp
      via: Processor applies corrected settings by transitioning Error to Idle or Ready without auto-connect side effects.
      pattern: apply
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_state_roundtrip_tests.cpp
      via: State restore path enforces disconnected startup with preserved short last-error context.
      pattern: setStateInformation
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.h
      via: Editor status line projects reconnect and error messaging from processor lifecycle snapshot fields.
      pattern: status
---

<objective>
Close Phase 2 behavior gaps around error correction flow, reopen semantics, and final lifecycle signaling.

Purpose: Complete the remaining SESS-03 and SESS-02 outcomes by explicitly defining corrected-settings behavior in Error and restore-time disconnected behavior with brief error context.
Output: Error apply transition handling, restore semantics coverage, UI status finalization, and manual Ableton matrix for lifecycle recovery acceptance.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-connection-lifecycle-recovery/02-CONTEXT.md
@.planning/phases/02-connection-lifecycle-recovery/02-RESEARCH.md
@.planning/phases/02-connection-lifecycle-recovery/02-VALIDATION.md
@.planning/phases/02-connection-lifecycle-recovery/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement corrected-settings transition from Error to Idle or Ready without auto-connect</name>
  <files>src/app/session/ConnectionLifecycle.h, src/app/session/ConnectionLifecycle.cpp, src/app/session/SessionSettingsController.h, src/app/session/SessionSettingsController.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_connection_error_recovery_tests.cpp</files>
  <action>When valid corrected settings are applied while lifecycle state is Error, clear retry state and transition to Idle or Ready immediately while remaining disconnected. Ensure this flow never issues implicit connect commands and requires explicit user Connect for retry restart.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_error_recovery</automated></verify>
  <done>Integration tests prove corrected Apply exits Error to Idle or Ready with no auto-connect side effects and with manual Connect still required.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit restore behavior coverage for disconnected startup and brief last-error context</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_state_roundtrip_tests.cpp</files>
  <action>Extend restore semantics so project reopen always starts disconnected while preserving a short, non-sticky last-error context string for user orientation. Validate this behavior for normal and error-path payloads in processor roundtrip tests.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_state_roundtrip</automated></verify>
  <done>Roundtrip tests confirm reopened instances are disconnected and expose brief last-error context without auto-connect.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize lifecycle status signaling and manual host validation matrix</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/CMakeLists.txt, docs/validation/phase2-ableton-lifecycle-matrix.md</files>
  <action>Finalize concise status rendering for reconnect countdown, attempts, hard-failure hints, and recovered healthy-state clearing behavior. Add manual Ableton checks for corrected-settings-in-Error, reopen-disconnected behavior, and reconnect policy outcomes.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_error_recovery</automated></verify>
  <done>Status messaging and manual matrix explicitly validate the two previously missing Phase 2 behaviors and overall lifecycle recovery UX expectations.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build --config Debug --target famalamajam_plugin`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_error_recovery`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_state_roundtrip`

Phase-level verification criteria (must be true to close Phase 2):
- [ ] `ctest --test-dir build --output-on-failure -R connection_lifecycle`
- [ ] `ctest --test-dir build --output-on-failure -R retry_policy`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_recovery`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_error_recovery`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_state_roundtrip`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Manual Ableton matrix passes for corrected settings in Error, reopen disconnected + brief error context, transient reconnect, and retry exhaustion (SESS-02, SESS-03).
</verification>

<success_criteria>
- All tasks completed
- Corrected-settings Apply behavior from Error is explicit, tested, and non-auto-connect
- Reopen behavior is explicitly disconnected with short last-error context
- Phase 2 lifecycle signaling and manual checks are complete and traceable to SESS-02 and SESS-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-lifecycle-recovery/02-03-SUMMARY.md`
</output>