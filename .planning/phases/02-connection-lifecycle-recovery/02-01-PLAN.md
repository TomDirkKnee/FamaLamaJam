---
phase: 02-connection-lifecycle-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/ConnectionLifecycle.h
  - src/app/session/ConnectionLifecycle.cpp
  - src/app/session/ConnectionLifecycleController.h
  - src/app/session/ConnectionLifecycleController.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/unit/connection_lifecycle_tests.cpp
  - tests/integration/plugin_connection_command_flow_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - SESS-02
user_setup: []
must_haves:
  truths:
    - Users can connect and disconnect repeatedly in a single Ableton session without plugin reload and without stale connected state.
    - Lifecycle transitions are explicit and deterministic across idle, connecting, active, reconnecting, and error states.
    - UI controls are state-gated so invalid duplicate commands are ignored during active transitions.
  artifacts:
    - path: src/app/session/ConnectionLifecycle.h
      provides: Lifecycle state and event model with explicit transition contract and snapshot data for UI and processor projection.
      contains: enum class ConnectionState
    - path: src/app/session/ConnectionLifecycleController.cpp
      provides: Single-writer command and event handling for connect and disconnect semantics plus transition validation.
      contains: handleCommand
    - path: tests/unit/connection_lifecycle_tests.cpp
      provides: Deterministic transition-table tests including duplicate command suppression and disconnect-cancel behavior.
      min_lines: 40
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/ConnectionLifecycleController.cpp
      via: Processor delegates connect and disconnect intent to lifecycle controller instead of mutating connection booleans directly.
      pattern: connect
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.h
      via: Editor maps lifecycle snapshot to status line and enabled control set.
      pattern: getLifecycleSnapshot
    - from: tests/integration/plugin_connection_command_flow_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Integration tests validate repeated connect and disconnect cycles through processor APIs.
      pattern: FamaLamaJamAudioProcessor
---

<objective>
Implement the Phase 2 lifecycle foundation for connection control.

Purpose: Replace boolean-only connection tracking with an explicit lifecycle state machine and deterministic connect and disconnect command handling that can be safely invoked repeatedly in one host session.
Output: Lifecycle domain model and controller, processor and editor integration for state projection and control gating, and deterministic unit and integration coverage for command behavior.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-connection-lifecycle-recovery/02-CONTEXT.md
@.planning/phases/02-connection-lifecycle-recovery/02-RESEARCH.md
@.planning/phases/02-connection-lifecycle-recovery/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit lifecycle state machine model with transition reducer semantics</name>
  <files>src/app/session/ConnectionLifecycle.h, src/app/session/ConnectionLifecycle.cpp, tests/unit/connection_lifecycle_tests.cpp, tests/CMakeLists.txt</files>
  <action>Introduce a pure lifecycle model in app/session with states Idle, Connecting, Active, Reconnecting, and Error; define command and event inputs with transition outputs, including no-op guards for invalid duplicate commands. Keep it independent from networking internals so transitions remain deterministic and testable.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R connection_lifecycle</automated></verify>
  <done>Unit tests demonstrate valid transition paths, invalid-command suppression, and guaranteed cancellation path to Idle on Disconnect from connecting or reconnecting.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lifecycle controller and processor command integration</name>
  <files>src/app/session/ConnectionLifecycleController.h, src/app/session/ConnectionLifecycleController.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp</files>
  <action>Create a single lifecycle controller that accepts connect and disconnect commands plus lifecycle events, then integrate it into FamaLamaJamAudioProcessor as the authoritative source for connection snapshot state. Replace direct ownership of connection truth from isConnected booleans with snapshot-driven query methods for UI and tests.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_command_flow</automated></verify>
  <done>Processor exposes explicit lifecycle snapshot and connect and disconnect APIs with deterministic state outcomes and no stale connected flag after cancellation or disconnect.</done>
</task>

<task type="auto">
  <name>Task 3: Project lifecycle state and command gating into editor UI</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_connection_command_flow_tests.cpp</files>
  <action>Add explicit Connect and Disconnect actions to the editor, map lifecycle snapshot to concise status text on the existing status line, and gate controls based on valid actions per state. Preserve explicit Apply semantics from Phase 1 and ensure Apply remains decoupled from immediate reconnect behavior.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_command_flow</automated></verify>
  <done>Integration tests prove repeated connect and disconnect behavior, command suppression during transitions, and consistent status projection across state changes.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build --config Debug --target famalamajam_plugin`
- [ ] `ctest --test-dir build --output-on-failure -R connection_lifecycle`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_command_flow`

Phase-level verification criteria (applies after all Phase 2 plans finish):
- [ ] `ctest --test-dir build --output-on-failure -R connection_lifecycle`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_recovery`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_error_recovery`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Manual Ableton check confirms repeated connect and disconnect in one session without restart (SESS-02).
</verification>

<success_criteria>
- All tasks completed
- State machine and command handling are explicit, deterministic, and test-covered
- SESS-02 acceptance behavior is implemented without crossing into timing or streaming scope
- Baseline lifecycle snapshot surface is ready for retry and backoff plus error signaling work in downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-lifecycle-recovery/02-01-SUMMARY.md`
</output>