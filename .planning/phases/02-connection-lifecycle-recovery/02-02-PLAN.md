---
phase: 02-connection-lifecycle-recovery
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/session/RetryPolicy.h
  - src/app/session/RetryPolicy.cpp
  - src/app/session/ConnectionFailureClassifier.h
  - src/app/session/ConnectionFailureClassifier.cpp
  - src/app/session/ConnectionLifecycle.h
  - src/app/session/ConnectionLifecycle.cpp
  - src/app/session/ConnectionLifecycleController.h
  - src/app/session/ConnectionLifecycleController.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/retry_policy_tests.cpp
  - tests/unit/connection_failure_classification_tests.cpp
  - tests/integration/plugin_connection_recovery_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - SESS-03
user_setup: []
must_haves:
  truths:
    - Transient transport failures automatically trigger reconnect attempts using capped exponential backoff and visible attempt context.
    - Non-retryable configuration, auth, or protocol failures transition directly to Error with clear actionable status and no retry loop.
    - Retry exhaustion lands in Error and requires explicit manual Connect to reset counters and restart recovery behavior.
  artifacts:
    - path: src/app/session/RetryPolicy.h
      provides: Capped exponential retry policy with deterministic delay and attempt progression contract.
      contains: struct RetryPolicy
    - path: src/app/session/ConnectionFailureClassifier.cpp
      provides: Failure-kind mapping that separates retryable transport faults from non-retryable user-correctable faults.
      contains: classifyFailure
    - path: tests/integration/plugin_connection_recovery_tests.cpp
      provides: Recovery tests for transient reconnect success, exhaustion, and non-retryable error branches.
      min_lines: 45
  key_links:
    - from: src/app/session/ConnectionLifecycleController.cpp
      to: src/app/session/RetryPolicy.cpp
      via: Lifecycle recovery path computes and schedules backoff delays per attempt progression.
      pattern: nextDelay
    - from: src/app/session/ConnectionLifecycleController.cpp
      to: src/app/session/ConnectionFailureClassifier.cpp
      via: Failure events are classified before deciding reconnect versus terminal error behavior.
      pattern: classifyFailure
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/ConnectionLifecycleController.cpp
      via: Processor hosts retry scheduling state and transitions for reconnect attempts.
      pattern: Reconnecting
---

<objective>
Implement automatic reconnect policy and deterministic error transitions for Phase 2.

Purpose: Deliver SESS-03 core behavior by adding retry backoff logic and failure classification on top of the lifecycle foundation from 02-01.
Output: Retry and classification components, controller scheduling behavior, and automated recovery-path coverage for transient, exhausted, and hard-failure branches.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-connection-lifecycle-recovery/02-CONTEXT.md
@.planning/phases/02-connection-lifecycle-recovery/02-RESEARCH.md
@.planning/phases/02-connection-lifecycle-recovery/02-VALIDATION.md
@.planning/phases/02-connection-lifecycle-recovery/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry policy and failure classification primitives</name>
  <files>src/app/session/RetryPolicy.h, src/app/session/RetryPolicy.cpp, src/app/session/ConnectionFailureClassifier.h, src/app/session/ConnectionFailureClassifier.cpp, tests/unit/retry_policy_tests.cpp, tests/unit/connection_failure_classification_tests.cpp, tests/CMakeLists.txt</files>
  <action>Implement capped exponential backoff policy (initial delay, multiplier, max delay, max attempts) and failure classification mapping for retryable transport faults versus non-retryable config, auth, or protocol faults. Keep these primitives deterministic and free of plugin UI concerns.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R retry_policy</automated></verify>
  <done>Unit tests verify delay progression, cap enforcement, attempt exhaustion behavior, and classification boundaries for retryable and non-retryable failures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reconnect scheduling and terminal error transitions in lifecycle controller</name>
  <files>src/app/session/ConnectionLifecycle.h, src/app/session/ConnectionLifecycle.cpp, src/app/session/ConnectionLifecycleController.h, src/app/session/ConnectionLifecycleController.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp</files>
  <action>Extend lifecycle snapshot and controller flow to track retry attempt counters and next-delay metadata. Schedule reconnect only for retryable failures, move to Error on non-retryable or exhausted paths, and require manual Connect from Error to reset recovery state.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_recovery</automated></verify>
  <done>Recovery behavior enforces retry policy boundaries, deterministic terminal Error rules, and manual reset semantics after exhaustion.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage for reconnect policy branches</name>
  <files>tests/integration/plugin_connection_recovery_tests.cpp</files>
  <action>Cover transient reconnect success, retry exhaustion to Error, and non-retryable hard failure with no retry scheduling so policy behavior is executable and regression-safe.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_connection_recovery</automated></verify>
  <done>Integration suite validates all reconnect policy branch outcomes expected by SESS-03.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build --config Debug --target famalamajam_plugin`
- [ ] `ctest --test-dir build --output-on-failure -R retry_policy`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_recovery`

Phase-level verification criteria (applies after all Phase 2 plans finish):
- [ ] `ctest --test-dir build --output-on-failure -R connection_lifecycle`
- [ ] `ctest --test-dir build --output-on-failure -R retry_policy`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_recovery`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_connection_error_recovery`
- [ ] `ctest --test-dir build --output-on-failure`
</verification>

<success_criteria>
- All tasks completed
- Automatic reconnect behavior follows capped exponential policy with deterministic branch behavior
- Non-retryable and exhausted failure paths terminate in explicit Error without retry loops
- Plan remains scoped to lifecycle and recovery logic only
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-lifecycle-recovery/02-02-SUMMARY.md`
</output>