---
phase: 05-ableton-reliability-v1-rehearsal-ux-validation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/ConnectionLifecycleController.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
  - tests/integration/plugin_transport_ui_sync_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup: []
must_haves:
  truths:
    - The plugin remains a single-page JUCE UI; Phase 5 improves clarity and flow instead of introducing a new navigation model.
    - Settings, Connect, and status/recovery guidance are visually primary so a musician can join and start playing without external tooling.
    - Connection, sync, and stream failures are described in short plain language with an obvious next action and remain visible until the state changes.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: The final built-in JUCE layout, status presentation, and above-the-fold workflow for Phase 5 usability.
      contains: resized
    - path: src/app/session/ConnectionLifecycleController.cpp
      provides: Actionable lifecycle copy for connect, reconnect, and error states used by the editor.
      contains: makeTerminalStatus
    - path: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      provides: Editor-facing regression coverage for the final Phase 5 single-page flow and persistent status treatment.
      min_lines: 80
  key_links:
    - from: src/app/session/ConnectionLifecycleController.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Lifecycle message wording and editor presentation must remain consistent so recovery guidance is clear in the main UI.
      pattern: statusMessage
    - from: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Editor tests lock the single-page setup-first flow and persistent failure visibility expected for v1.
      pattern: refreshLifecycleStatus
---

<objective>
Finalize the built-in JUCE flow and message quality for v1 rehearsal usability.

Purpose: Make the plugin understandable and recoverable from its own UI, with setup/connect/status visually first and the mixer still available but secondary.
Output: A polished single-page editor, actionable lifecycle/sync/stream messages, and editor coverage for the final v1 flow.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-CONTEXT.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-RESEARCH.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-VALIDATION.md
@src/app/session/ConnectionLifecycleController.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_transport_ui_sync_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add editor-facing regression coverage for the final rehearsal flow</name>
  <files>tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: The built-in editor keeps setup/connect/status information visible at the top in a single-page flow.
    - Test 2: Important failure states remain visibly surfaced until state changes instead of collapsing into a generic transient status.
    - Test 3: The mixer remains available on the same page but does not displace the primary join-and-recover workflow.
  </behavior>
  <action>Create a dedicated editor-focused integration suite for Phase 5 that expresses the single-page setup-first workflow and persistent actionable status treatment. Reuse the existing editor harness patterns from transport and mixer UI tests rather than inventing a new UI test infrastructure.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_rehearsal_ui_flow],[plugin_transport_ui_sync]"'</automated></verify>
  <done>The final built-in flow is protected by editor-facing regression coverage before UI polish relies on it.</done>
</task>

<task type="auto">
  <name>Task 2: Improve lifecycle, sync, and streaming messages to be short and actionable</name>
  <files>src/app/session/ConnectionLifecycleController.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.cpp</files>
  <action>Tighten user-facing copy and presentation so connection, reconnect, timing-loss, and stream-trouble states say what happened and what the musician should do next in plain language. Keep the messaging honest to the Phase 3 timing model, avoid protocol-heavy jargon, and make failure guidance persist clearly until the state changes.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transport_ui_sync],[plugin_connection_recovery],[plugin_connection_error_recovery]"'</automated></verify>
  <done>Status and error messaging is clear, actionable, and consistent across connection and transport states.</done>
</task>

<task type="auto">
  <name>Task 3: Polish the single-page editor layout so setup and status are visually first</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <action>Refine the editor layout so settings, Connect, and persistent status guidance are the primary visual focus without introducing tabs or a new navigation structure. Keep the mixer on the same page, preserve the existing JUCE style, and ensure the resulting layout still behaves sensibly for the current mixer surface.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_transport_ui_sync]"'</automated></verify>
  <done>The built-in JUCE UI feels like a simple join-and-play workflow, with mixer controls present but clearly secondary.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_rehearsal_ui_flow],[plugin_transport_ui_sync],[plugin_connection_recovery],[plugin_connection_error_recovery],[plugin_mixer_ui]"'`

Phase-level verification criteria (applies after all Phase 5 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton rehearsal validation confirms the built-in UI is sufficient for setup, connect, recovery, and begin-playing flow without external tooling.
</verification>

<success_criteria>
- All tasks completed
- UI-01 is satisfied by a clear single-page join-and-play workflow
- UI-02 is satisfied by persistent, actionable failure messaging
- Mixer visibility remains compatible with the setup-first v1 flow
</success_criteria>

<output>
After completion, create `.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-02-SUMMARY.md`
</output>
