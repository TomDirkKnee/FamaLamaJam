---
phase: 05-ableton-reliability-v1-rehearsal-ux-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_host_lifecycle_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/integration/plugin_streaming_runtime_compat_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - HOST-01
user_setup: []
must_haves:
  truths:
    - Restored or duplicated plugin instances never silently resume a live session; they restore state but remain idle until the user explicitly connects.
    - Host-driven lifecycle transitions such as release, reprepare, and supported engine restart paths do not leave zombie reconnect timers, zombie transport state, or stale timing/remote audio state behind.
    - Phase 4 streaming and mix persistence guarantees survive Phase 5 lifecycle hardening rather than being weakened by new cleanup logic.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: The processor-side lifecycle teardown, restore, and runtime reset semantics for host reopen, duplicate-safe behavior, and engine restart handling.
      contains: applyLifecycleTransition
    - path: tests/integration/plugin_host_lifecycle_tests.cpp
      provides: Dedicated regression coverage for host-shaped lifecycle scenarios beyond generic connect/disconnect tests.
      min_lines: 80
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: State-restore assertions proving reopened or duplicated instances come back idle while retaining persisted settings.
      contains: setStateInformation
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_host_lifecycle_tests.cpp
      via: Lifecycle tests exercise processor cleanup and restore semantics through host-shaped sequences such as release/reprepare and restored second instances.
      pattern: releaseResources
    - from: tests/integration/plugin_state_roundtrip_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: State round-trip coverage guards the explicit idle-on-restore contract required for reopen and duplicate safety.
      pattern: getStateInformation
---

<objective>
Harden the plugin's host lifecycle behavior for Ableton-facing v1 reliability.

Purpose: Make reopened, duplicated, and engine-restarted instances behave predictably and safely without zombie sessions or stale transport state.
Output: Explicit processor lifecycle semantics plus regression coverage for restore-idle behavior, cleanup guarantees, and supported host runtime restart paths.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-CONTEXT.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-RESEARCH.md
@.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp
@tests/integration/plugin_streaming_runtime_compat_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for host-shaped lifecycle scenarios</name>
  <files>tests/integration/plugin_host_lifecycle_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Restored instances retain persisted settings and mix state but remain idle, with no active connection or pending reconnect.
    - Test 2: A second instance created from saved state behaves like a safe duplicate and does not auto-connect or inherit live runtime state.
    - Test 3: Connected runtime release/reprepare sequences clear stale timing, remote interval, and reconnect state before clean recovery.
  </behavior>
  <action>Create dedicated integration coverage for Ableton-shaped lifecycle expectations rather than relying only on generic connection and runtime tests. Reuse the existing processor and mini-server test harnesses where possible, but introduce a new lifecycle-focused suite that makes the idle-on-restore and no-zombie-state contracts explicit.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_lifecycle],[plugin_state_roundtrip]"'</automated></verify>
  <done>Host-shaped lifecycle expectations are encoded in integration tests before lifecycle hardening relies on them.</done>
</task>

<task type="auto">
  <name>Task 2: Harden processor teardown and restore semantics for reopen, duplicate, and engine restart</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp</files>
  <action>Audit and tighten the processor lifecycle paths so restored or duplicated instances always come back idle, reconnect timers cannot survive across host-driven teardown, and supported engine restart/reprepare paths clear stale codec, timing, queued remote audio, and transport runtime state coherently. Preserve existing Phase 4 send/receive and mix persistence behavior while making these lifecycle outcomes explicit and testable.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_lifecycle],[plugin_state_roundtrip],[plugin_streaming_runtime_compat]"'</automated></verify>
  <done>Processor lifecycle transitions are duplicate-safe, reopen-safe, and engine-restart-safe without leaving zombie session or timing state behind.</done>
</task>

<task type="auto">
  <name>Task 3: Close the lifecycle loop with a repeatable regression gate</name>
  <files>tests/integration/plugin_host_lifecycle_tests.cpp, tests/integration/plugin_streaming_runtime_compat_tests.cpp</files>
  <action>Use the new lifecycle suite together with existing state-roundtrip and runtime-compatibility coverage as the automated gate for Host requirement HOST-01. Tighten any weak assertions needed so the regression set catches stale-state or reconnect-timer leaks before the final rehearsal-validation plan runs.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure -R "phase::.*(plugin_host_lifecycle|plugin_state_roundtrip|plugin_streaming_runtime_compat)"'</automated></verify>
  <done>Phase 5 has a credible automated lifecycle gate for restore, duplicate-safe behavior, and supported runtime restart recovery.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_lifecycle],[plugin_state_roundtrip],[plugin_streaming_runtime_compat]"'`

Phase-level verification criteria (applies after all Phase 5 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Reopen and duplicate semantics remain idle-by-default in manual Ableton validation.
</verification>

<success_criteria>
- All tasks completed
- HOST-01 lifecycle semantics are explicit and regression-tested
- Reopened or duplicated instances do not auto-join sessions
- Supported host restart/reprepare paths do not leave stale runtime state behind
</success_criteria>

<output>
After completion, create `.planning/phases/05-ableton-reliability-v1-rehearsal-ux-validation/05-01-SUMMARY.md`
</output>
