---
phase: 07-chat-room-control-commands
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_room_controls_ui_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
user_setup: []
must_haves:
  truths:
    - The current single-page editor shows a compact mixed room feed, pinned topic, and always-visible composer while connected.
    - Users can submit BPM and BPI votes from direct numeric controls near the transport area without typing slash commands.
    - Vote controls show inline pending or failure state while shared room-visible outcomes continue to appear in the feed.
    - The room controls stay readable and above the mixer without turning Phase 7 into the broader Phase 9 layout rewrite.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.h
      provides: Editor-facing room UI contracts, command handlers, and testing hooks for the mixed feed, composer, and vote controls.
      contains: RoomUiState
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Compact room section layout, timer-driven room UI refresh, and direct BPM or BPI vote interaction.
      contains: refreshRoomUi
    - path: tests/integration/plugin_room_controls_ui_tests.cpp
      provides: Regression coverage for room section presence, control enablement, pending or failure copy, and section ordering above the mixer.
      min_lines: 150
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: `createEditor()` maps processor-owned room snapshots and send or vote handlers into the editor.
      pattern: getRoomUiState|sendRoomChatMessage|submitRoomVote
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: The editor polls room state on the existing timer and routes button or text-editor actions back through processor command callbacks.
      pattern: timerCallback|onClick|onReturnKey
    - from: tests/integration/plugin_room_controls_ui_tests.cpp
      to: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      via: Dedicated room-control assertions prove the new section is readable while broader rehearsal-flow tests protect top-of-page ordering above the mixer.
      pattern: room|mixer
---

<objective>
Wire the compact room feed, composer, and direct BPM or BPI vote controls into the current single-page JUCE editor.

Purpose: Deliver the user-visible Phase 7 workflow without hiding commands in typed slash syntax or triggering the later layout redesign early.
Output: Editor-facing room UI contracts, dedicated room-controls regression coverage, and a compact room section integrated into the current transport/status workflow.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-chat-room-control-commands/07-CONTEXT.md
@.planning/phases/07-chat-room-control-commands/07-RESEARCH.md
@.planning/phases/07-chat-room-control-commands/07-VALIDATION.md
@.planning/phases/07-chat-room-control-commands/07-01-PLAN.md
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/CMakeLists.txt

<interfaces>
From `src/plugin/FamaLamaJamAudioProcessorEditor.h`:
```cpp
using TransportUiGetter = std::function<TransportUiState()>;
using HostSyncAssistUiGetter = std::function<HostSyncAssistUiState()>;
using MixerStripsGetter = std::function<std::vector<MixerStripState>()>;
using CommandHandler = std::function<bool()>;
```

Use these exact Phase 7 processor contracts created in `07-01`:
```cpp
enum class RoomVoteKind { Bpm, Bpi };
enum class RoomFeedEntryKind { Chat, Topic, Presence, VoteSystem, GenericSystem };

struct RoomVoteUiState
{
    bool pending { false };
    bool failed { false };
    int requestedValue { 0 };
    std::string statusText;
};

struct RoomFeedEntry
{
    RoomFeedEntryKind kind { RoomFeedEntryKind::Chat };
    std::string author;
    std::string text;
    bool subdued { false };
};

struct RoomUiState
{
    bool connected { false };
    std::string topic;
    std::vector<RoomFeedEntry> visibleFeed;
    RoomVoteUiState bpmVote;
    RoomVoteUiState bpiVote;
};

[[nodiscard]] RoomUiState getRoomUiState() const;
bool sendRoomChatMessage(std::string text);
bool submitRoomVote(RoomVoteKind kind, int value);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED room-controls UI coverage and register the Phase 7 editor tests</name>
  <files>tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: The editor exposes one mixed room section with pinned topic, feed, composer, and direct BPM or BPI vote controls in the current single-page workflow.
    - Test 2: The room composer stays visible while connected, and disconnected-state behavior is explicit rather than silently pretending chat still sends.
    - Test 3: Vote controls sit near transport timing, use editable numeric inputs, and show inline pending or failure feedback independent of the feed list.
    - Test 4: The room section stays above the mixer and does not require a tab, popout, or Phase 9 layout rewrite to reach chat or voting.
  </behavior>
  <action>Create `tests/integration/plugin_room_controls_ui_tests.cpp` first and register it with a `[plugin_room_controls_ui]` tag. Add or update rehearsal-flow assertions in `plugin_rehearsal_ui_flow_tests.cpp` so the room section is proven to remain above the mixer and within the current single-page layout. Keep the test vocabulary grounded in the locked Phase 7 scope: one mixed feed, always-visible composer while connected, direct numeric BPM or BPI controls near transport, and no private-chat or tabbed-room UI.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow]"'</automated></verify>
  <done>Phase 7 has dedicated editor regression coverage for the compact room workflow before the UI implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire processor room snapshots and command callbacks into the editor contract</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <behavior>
    - Test 1: `createEditor()` passes room UI getters plus chat or vote handlers from the processor into the editor without exposing sockets directly.
    - Test 2: The editor can refresh room state on its existing timer and expose enough testing hooks to assert topic text, vote status text, and section ordering.
    - Test 3: Existing editor harnesses still compile and run after the constructor expands for the room-section callbacks and state.
  </behavior>
  <action>Extend `FamaLamaJamAudioProcessorEditor.h` with mirrored Phase 7 room contracts and callback types, keeping the same processor-owned snapshot pattern already used for transport, host-sync assist, and mixer strips. Update `createEditor()` in `FamaLamaJamAudioProcessor.cpp` to map the processor `RoomUiState`, `sendRoomChatMessage()`, and `submitRoomVote()` entry points into editor-friendly lambdas. Add narrow testing hooks for the room topic text, visible feed labels or kinds, pending-vote text, and control enablement instead of asserting layout by raw child index. If the constructor change breaks existing harnesses, repair `plugin_mixer_ui_tests.cpp` in the same task rather than leaving the build red.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui]"'</automated></verify>
  <done>The editor has stable Phase 7 inputs and test hooks, and the processor remains the single source of truth for room state and commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the compact mixed feed, always-visible composer, and direct vote controls</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: While connected, the editor shows a scrollable mixed feed containing public chat, topic updates, join or part presence lines, and vote-related system feedback.
    - Test 2: The composer remains visible in the room section and sends public chat through Enter or an explicit send action without requiring slash-command knowledge.
    - Test 3: BPM and BPI numeric inputs near the transport area submit direct vote commands, show inline pending or failure feedback, and clear back to neutral once server feedback confirms or rejects the vote.
    - Test 4: Join or part entries remain visible but visually subtler than normal chat, and the room topic is shown both as a pinned current-topic element and as a feed entry when the topic changes.
  </behavior>
  <action>Implement a compact room section inside the existing editor rather than adding tabs, popouts, or the later right-hand panel. Use JUCE primitives already in the project: `juce::Viewport` for the scrollable mixed feed, `juce::TextEditor` for the composer and numeric BPM or BPI fields, and plain buttons for send or vote submission. Place the pinned topic and BPM or BPI controls near the transport status and interval timing area, not inside the composer. Keep the composer always visible while connected, and use explicit disconnected-state disabling or status copy rather than hiding the section entirely. Preserve one mixed public feed only, keep join or part entries subdued, and do not add private-message or moderation UI. Choose direct numeric validation ranges that stay rehearsal-friendly, for example BPM 40 to 400 and BPI 2 to 64, unless the backend command feedback proves a stricter bound is necessary.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow]"'</automated></verify>
  <done>The Phase 7 room workflow is visible and usable in the current editor, ROOM-01 and ROOM-02 are accessible without typed slash commands, and the layout remains inside the current single-page design boundary.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow]"'`

Phase-level verification criteria (applies after all Phase 7 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton verification confirms the room section remains readable and does not crowd out transport or mixer workflows at typical plugin window sizes.
</verification>

<success_criteria>
- All tasks completed
- The editor exposes one compact room section with pinned topic, mixed feed, composer, and direct BPM or BPI vote controls
- ROOM-01 and ROOM-02 are reachable from the visible UI without manual slash-command typing
- The implementation stays inside the current single-page layout and avoids the deferred Phase 9 redesign
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-room-control-commands/07-02-SUMMARY.md`
</output>
