---
phase: 07-chat-room-control-commands
plan: "03"
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - docs/validation/phase7-room-chat-controls-matrix.md
  - .planning/phases/07-chat-room-control-commands/07-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - ROOM-01
  - ROOM-02
user_setup:
  - Open Ableton on Windows with the freshly built `FamaLamaJam.vst3`.
  - Join a live NINJAM room, exchange at least one public chat message, and observe topic plus join or part activity in the plugin feed.
  - Submit one BPM vote and one BPI vote from the plugin controls, then report whether inline pending or failure copy and room-feed outcomes were understandable.
  - Confirm the room section remains readable at the intended plugin window size without obscuring transport or mixer workflows.
must_haves:
  truths:
    - Phase 7 closes with evidence that chat and vote controls work in both automation and a real host session, not just by code inspection.
    - Verification records the exact automated test gate, the built VST3 artifact, and the manual rehearsal outcomes for room chat, topic or presence visibility, and BPM or BPI voting.
    - Any remaining uncertainty about real-server vote command semantics or UI readability is documented explicitly rather than assumed away.
    - Planning state reflects that Phase 7 is complete only after both ROOM-01 and ROOM-02 have an evidence-backed verification record.
  artifacts:
    - path: docs/validation/phase7-room-chat-controls-matrix.md
      provides: Manual Ableton matrix for public chat send or receive, topic plus presence visibility, BPM or BPI vote submission, reconnect reset honesty, and readability checks.
      contains: Phase 7 Room Chat and Controls Validation Matrix
    - path: .planning/phases/07-chat-room-control-commands/07-VERIFICATION.md
      provides: Final Phase 7 verification report combining targeted automation, full suite results, manual host outcomes, and residual risk notes.
      contains: Verification Summary
    - path: build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3
      provides: The built plugin artifact used for the manual Ableton validation pass.
  key_links:
    - from: docs/validation/phase7-room-chat-controls-matrix.md
      to: .planning/phases/07-chat-room-control-commands/07-VERIFICATION.md
      via: Manual Ableton observations roll directly into the verification report and determine whether Phase 7 can close.
      pattern: Outcome
    - from: build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3
      to: .planning/phases/07-chat-room-control-commands/07-VERIFICATION.md
      via: The report names the exact artifact used so the sign-off is tied to a real build.
      pattern: VST3
---

<objective>
Validate the room chat and room-control workflow, then close Phase 7 with an explicit verification record.

Purpose: Turn the new room features into an evidence-backed milestone result instead of assuming the fake-server tests fully cover real-room behavior.
Output: A concise Ableton validation matrix, a Phase 7 verification report, and updated planning state once the room workflow is proven.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-chat-room-control-commands/07-CONTEXT.md
@.planning/phases/07-chat-room-control-commands/07-RESEARCH.md
@.planning/phases/07-chat-room-control-commands/07-VALIDATION.md
@.planning/phases/07-chat-room-control-commands/07-01-PLAN.md
@.planning/phases/07-chat-room-control-commands/07-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the concise Phase 7 validation matrix and verification scaffold</name>
  <files>docs/validation/phase7-room-chat-controls-matrix.md, .planning/phases/07-chat-room-control-commands/07-VERIFICATION.md</files>
  <action>Turn the Phase 7 context, research, and validation notes into a short manual matrix covering the locked workflow: send public room chat, receive topic plus join or part activity in the mixed feed, submit BPM and BPI votes from direct controls, confirm inline pending or failure copy, confirm room-feed feedback for shared outcomes, and verify that reconnect or a fresh room session clears old feed history. Create a verification report template that captures the targeted automation gate, full `ctest` gate, exact built VST3 artifact path, manual Ableton outcomes, and any residual uncertainty about real-server vote semantics or UI readability.</action>
  <verify><automated>powershell -Command "if (-not (Test-Path 'docs\\validation\\phase7-room-chat-controls-matrix.md')) { exit 1 }; if (-not (Test-Path '.planning\\phases\\07-chat-room-control-commands\\07-VERIFICATION.md')) { exit 1 }"</automated></verify>
  <done>The manual matrix and verification report exist and are specific to the actual Phase 7 room workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Run the targeted Phase 7 regression gate and prepare the build record</name>
  <files>.planning/phases/07-chat-room-control-commands/07-VERIFICATION.md</files>
  <action>Run the targeted Phase 7 suites first and record their outcomes in `07-VERIFICATION.md` as the fast feedback loop for this close-out plan. Use the room-focused tags from `07-VALIDATION.md`, then reserve the full `ctest` gate and VST3 build for the plan-complete verification block below. This task is verification-only: if the targeted suites fail, record the failing tags and observed regressions in `07-VERIFICATION.md`, then stop Phase 7 close-out and route the work back to the implementation plans rather than fixing product code inside `07-03`.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_chat],[plugin_room_controls_ui]"'</automated></verify>
  <done>The targeted Phase 7 regression gate is recorded in the verification report, and any failure is captured as a close-out blocker instead of unscoped remediation work in this plan.</done>
</task>

<task type="auto">
  <name>Task 3: Capture manual Ableton outcomes and close Phase 7 with explicit residual-risk notes</name>
  <files>.planning/phases/07-chat-room-control-commands/07-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Use the user's Ableton results from `docs/validation/phase7-room-chat-controls-matrix.md` to finish the verification report with pass or fail notes for public chat, topic plus presence visibility, BPM or BPI vote submission, pending or failure clarity, reconnect reset honesty, and overall section readability. If Phase 7 meets the locked requirements, update planning state and roadmap progress for completion. If the real rehearsal server shows different vote-command semantics or unreadable room density, document those constraints explicitly instead of silently broadening scope or calling them passed.</action>
  <verify><automated>powershell -Command "$hits = Select-String -Path '.planning\\phases\\07-chat-room-control-commands\\07-VERIFICATION.md' -Pattern 'Verification Summary','Manual Outcomes','Residual Risks','Phase 7 Outcome'; if ($hits.Count -lt 4) { exit 1 }"</automated></verify>
  <done>Phase 7 closes with a traceable automated plus manual verification record and any real-server or readability gaps are documented for future phases.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug'`
- [ ] User completes `docs/validation/phase7-room-chat-controls-matrix.md` in Ableton and outcomes are recorded in `07-VERIFICATION.md`

Phase-level verification criteria (applies after all Phase 7 plans finish):
- [ ] `ctest --test-dir build-vs --output-on-failure`
- [ ] The tested VST3 artifact is recorded in `07-VERIFICATION.md`
- [ ] Manual Ableton validation covers public chat send or receive, topic plus presence visibility, BPM or BPI vote submission, pending or failure clarity, reconnect reset honesty, and room-section readability
</verification>

<success_criteria>
- All tasks completed
- ROOM-01 and ROOM-02 are backed by both automation and real host-session evidence
- The Phase 7 outcome records whether real-room vote behavior matched the expected command flow
- Future layout or parity work can build on an explicit verification record instead of re-testing Phase 7 from scratch
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-room-control-commands/07-03-SUMMARY.md`
</output>
