---
phase: 08.1-server-discovery-polish-jamtaba-parity-check
plan: "01"
subsystem: discovery-foundation
tags: [server-discovery, jamtaba-parity, room-counts, ordering, testing]
requires:
  - phase: 08-server-discovery-history
    provides: Remembered server history, combined discovery state, and basic public discovery wiring
provides:
  - richer public discovery parsing with active-user and max-user counts
  - JamTaba-informed public room ordering by activity
  - dual-source public discovery fetching with structured-data preference
affects: [08.1-02, 08.1-03, discovery-ui, manual-validation]
tech-stack:
  added:
    - src/app/session/ServerDiscoveryModel.h
    - src/app/session/ServerDiscoveryModel.cpp
    - src/infra/net/PublicServerDiscoveryClient.h
    - src/infra/net/PublicServerDiscoveryClient.cpp
  patterns:
    - structured discovery model preferred over raw users-text scraping
    - remembered-first plus busiest-public-room ordering
key-files:
  created:
    - src/app/session/ServerDiscoveryModel.h
    - src/app/session/ServerDiscoveryModel.cpp
    - src/infra/net/PublicServerDiscoveryClient.h
    - src/infra/net/PublicServerDiscoveryClient.cpp
  modified:
    - src/plugin/FamaLamaJamAudioProcessor.h
    - src/plugin/FamaLamaJamAudioProcessor.cpp
    - tests/unit/server_discovery_parser_tests.cpp
    - tests/integration/plugin_server_discovery_tests.cpp
key-decisions:
  - Prefer JamTaba-style structured login-service discovery data when available, with the legacy text feed as a fallback.
  - Sort only the public section by active users descending while still keeping remembered entries as a separate leading group.
  - Treat non-bot user counts as the primary room-activity metric rather than blindly trusting raw `usersText`.
patterns-established:
  - "Discovery parsing can consume either structured JSON room data or legacy `SERVER ... END` text without changing processor-facing contracts."
  - "Public room ordering is performed before editor rendering so UI code only consumes stable snapshots."
requirements-completed: [DISC-01, DISC-02]
duration: 1 session
completed: 2026-03-17
---

# Phase 08.1 Plan 01 Summary

**Public discovery now prefers richer room metadata, exposes believable active-user counts, and orders public rooms by activity instead of raw feed order.**

## Accomplishments

- Added a richer `ParsedPublicServerEntry` model with explicit `connectedUsers` and `maxUsers`.
- Extended discovery parsing to support JamTaba-style structured JSON room lists while keeping the legacy text feed as a fallback.
- Changed public discovery fetching to prefer `ninbot.com/app/servers.php` before falling back to `ninjam.com/serverlist.php`.
- Sorted public rooms by active users descending and updated processor labels to show clearer current-user and capacity info.

## Task Commits

1. **Task 1: Add RED discovery count and ordering coverage** - `218da29` (`test`)
2. **Task 2: Implement richer discovery parsing and sorted public room state** - `3061f5a` (`feat`)

## Verification

- `cmake --build build-vs --target famalamajam_tests --config Debug`
- `.\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"`
- Result: pass (`10` test cases / `179` assertions)

## Deviations from Plan

None - plan executed as intended.

## Issues Encountered

- The initial delegated executor was interrupted while reporting status, but it had already landed the RED test commit. The remaining implementation work was resumed locally without losing progress.

## User Setup Required

None.

## Next Phase Readiness

Wave 2 can now fix picker stability against a better discovery model instead of patching the old row/index behavior in place. The broader cramped-layout complaint remains deferred to Phase 9.
