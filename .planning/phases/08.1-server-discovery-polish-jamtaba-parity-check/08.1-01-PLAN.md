---
phase: 08.1-server-discovery-polish-jamtaba-parity-check
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/ServerDiscoveryModel.h
  - src/app/session/ServerDiscoveryModel.cpp
  - src/infra/net/PublicServerDiscoveryClient.h
  - src/infra/net/PublicServerDiscoveryClient.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/server_discovery_parser_tests.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Public-room counts should come from a richer discovery model than the current first-integer scrape when better structured data is available.
    - Public entries remain runtime-only discovery state; remembered/private history rules from Phase 8 stay intact.
    - Public rooms should sort by active users descending, while remembered entries remain a separate leading group.
    - The phase should improve discovery correctness without turning into a full UI redesign.
  artifacts:
    - path: src/app/session/ServerDiscoveryModel.h
      provides: Richer discovery-entry contracts for active-user counts, max-user counts, and public ordering semantics.
      contains: ParsedPublicServerEntry
    - path: src/app/session/ServerDiscoveryModel.cpp
      provides: Discovery parsing and ordering helpers with JamTaba-informed count semantics.
      contains: parsePublicServerList
    - path: src/infra/net/PublicServerDiscoveryClient.cpp
      provides: Public discovery fetch logic capable of serving the richer model.
      contains: requestRefresh
    - path: tests/unit/server_discovery_parser_tests.cpp
      provides: RED/GREEN coverage for count extraction, richer payload parsing, and public-room sort order.
      min_lines: 140
    - path: tests/integration/plugin_server_discovery_tests.cpp
      provides: Integration coverage for remembered-plus-public grouping and busiest-room ordering in processor state.
      min_lines: 180
  key_links:
    - from: src/infra/net/PublicServerDiscoveryClient.cpp
      to: src/app/session/ServerDiscoveryModel.cpp
      via: Discovery fetch results must populate structured room metadata rather than only raw `usersText`.
      pattern: requestRefresh|parsePublicServerList
    - from: src/app/session/ServerDiscoveryModel.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Processor-visible public entries should already be sorted and carry explicit count fields before the editor renders them.
      pattern: ParsedPublicServerEntry|connectedUsers
---

<objective>
Repair the discovery-data foundation so public-room counts and ordering are trustworthy enough for real server browsing.

Purpose: Replace the brittle first-integer room-count behavior with a richer public-room model informed by JamTaba's login-service structure, while preserving remembered-history behavior from Phase 8.
Output: Structured public discovery metadata, deterministic busiest-room ordering, and parser/integration tests proving the new semantics.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-server-discovery-history/08-VERIFICATION.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-RESEARCH.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-VALIDATION.md
@src/app/session/ServerDiscoveryModel.h
@src/app/session/ServerDiscoveryModel.cpp
@src/infra/net/PublicServerDiscoveryClient.h
@src/infra/net/PublicServerDiscoveryClient.cpp
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/unit/server_discovery_parser_tests.cpp
@tests/integration/plugin_server_discovery_tests.cpp
@C:/Users/Dirk/Documents/source/reference/JamTaba/src/Common/loginserver/LoginService.cpp
@C:/Users/Dirk/Documents/source/reference/JamTaba/src/Common/loginserver/LoginService.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED discovery tests for richer room-count semantics and busiest-room ordering</name>
  <files>tests/unit/server_discovery_parser_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp</files>
  <behavior>
    - Test 1: The parser can represent active-user count separately from max users rather than only carrying a raw `usersText` label.
    - Test 2: If a richer structured payload is available, active-user counts come from that structure instead of scraping the first integer in a human string.
    - Test 3: Public rooms are ordered by active users descending, with deterministic tie-breaks.
    - Test 4: Remembered servers still stay grouped ahead of public rooms after the public list is sorted.
  </behavior>
  <action>Extend the existing parser and integration tests before touching production code. Keep the RED surface narrow: count semantics, public ordering, and remembered-plus-public grouping. Do not put picker-selection assertions in this plan; those belong to 08.1-02.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"'</automated></verify>
  <done>The missing discovery-count and ordering expectations are captured in failing tests before the data model changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement richer public discovery parsing and sorted processor-visible public room state</name>
  <files>src/app/session/ServerDiscoveryModel.h, src/app/session/ServerDiscoveryModel.cpp, src/infra/net/PublicServerDiscoveryClient.h, src/infra/net/PublicServerDiscoveryClient.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/unit/server_discovery_parser_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp</files>
  <behavior>
    - Test 1: Public entries expose explicit current-user count and max-user count when the upstream payload provides them.
    - Test 2: The processor combines remembered and public entries without duplicates while keeping remembered entries first.
    - Test 3: The public section is sorted by active-user count descending instead of raw feed order.
    - Test 4: Label formatting no longer depends on blindly echoing a raw `1/8 users` blob when structured count data exists.
  </behavior>
  <action>Upgrade the discovery model to carry richer public-room metadata and use JamTaba's JSON login-service structure as the target shape for accurate room counts. If a fallback to the legacy text feed is needed, keep it defensive and clearly lower fidelity. Keep remembered-history persistence untouched, but change the processor's public-room merge step so sorting is deterministic and labels can distinguish current users from capacity more honestly.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"'</automated></verify>
  <done>Public discovery data is now rich enough to support believable counts and busiest-room ordering in the UI.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"'`

Phase-level verification criteria (applies after all Phase 08.1 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual validation confirms public-room counts feel believable against a live public list and remembered entries still group first.
</verification>

<success_criteria>
- All tasks completed
- DISC-01 discovery data is rich enough to express active users and max users separately when available
- Public entries are sorted by activity, not upstream feed order
- Remembered/private grouping remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-01-SUMMARY.md`
</output>
