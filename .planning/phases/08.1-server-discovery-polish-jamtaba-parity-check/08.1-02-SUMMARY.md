---
phase: 08.1-server-discovery-polish-jamtaba-parity-check
plan: "02"
subsystem: discovery-ui
tags: [server-discovery, ui, picker-selection, regression-tests]
requires:
  - phase: 08.1-server-discovery-polish-jamtaba-parity-check
    provides: Structured public-room counts and stable public ordering
provides:
  - endpoint-stable picker restore logic
  - UI regression coverage for reorder and connect-time remembered insertion
  - clearer continuity between selected room identity and rendered combo-box state
affects: [08.1-03, phase-9-layout, discovery-manual-validation]
tech-stack:
  added:
    - tests/integration/plugin_server_discovery_ui_tests.cpp
  patterns:
    - endpoint-key selection restore instead of row-index restore
key-files:
  created:
    - tests/integration/plugin_server_discovery_ui_tests.cpp
  modified:
    - src/plugin/FamaLamaJamAudioProcessorEditor.h
    - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
key-decisions:
  - Preserve discovery selection by endpoint key even when remembered insertion or public sorting changes the row order.
  - Treat the combo-box label as a user-facing truth that must remain aligned with the selected endpoint, not just with the populated host/port fields.
patterns-established:
  - "Discovery UI tests cover connect-time reordering bugs directly, not just field-prefill happy paths."
requirements-completed: [DISC-01, DISC-02]
duration: 1 session
completed: 2026-03-17
---

# Phase 08.1 Plan 02 Summary

**The server picker now stays attached to the chosen endpoint across refreshes and connect-time remembered insertion instead of drifting with row-index changes.**

## Accomplishments

- Replaced row-index-based selection restore in the editor with endpoint-key-based restore logic.
- Added discovery UI regressions for reordered public lists and connect-time remembered insertion.
- Preserved the existing manual host/port workflow while making the combo-box state trustworthy again.

## Task Commits

1. **Task 1 + Task 2: UI regression coverage and endpoint-stable picker implementation** - `6c530e4` (`fix`)

## Verification

- `cmake --build build-vs --target famalamajam_tests --config Debug`
- `.\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"`
- Result: pass (`10` test cases / `165` assertions)

## Deviations from Plan

- The Wave 2 test and implementation work were recorded in one commit instead of two clean task commits because the Phase 8 discovery UI baseline was still present as uncommitted workspace state. Splitting the history further would have produced a misleading commit boundary.

## Issues Encountered

- None beyond the existing repo-state wrinkle above.

## User Setup Required

None.

## Next Phase Readiness

Wave 3 can now focus on the final automated gate, rebuilt VST3, and the manual Ableton check for believable counts, ordering, and picker stability.
