---
phase: 08.1-server-discovery-polish-jamtaba-parity-check
plan: "02"
type: execute
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_server_discovery_ui_tests.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Picker selection must be preserved by endpoint identity, not by row position.
    - Successful connect and remembered-entry insertion must not make the combo box appear to change the user's chosen server.
    - Manual `Host` / `Port` editing remains the real connection source of truth.
    - Phase 08.1 should improve list trust and stability without trying to solve the larger cramped-layout problem.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Endpoint-stable picker refresh behavior and clearer discovery-row presentation.
      contains: refreshServerDiscoveryUi
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Server labels and combined-entry snapshots aligned with the richer count/order model.
      contains: getServerDiscoveryUiState
    - path: tests/integration/plugin_server_discovery_ui_tests.cpp
      provides: Editor-level regression coverage for selection stability after refresh/connect and continued manual editability.
      min_lines: 180
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: The editor must preserve selection against endpoint keys from processor snapshots rather than trusting stable row order.
      pattern: refreshServerDiscoveryUi|combinedEntries
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_server_discovery_ui_tests.cpp
      via: Tests prove that connect-time remembered insertion and refresh do not cause the picker to jump to another server.
      pattern: selectServerDiscoveryEntryForTesting|getVisibleServerDiscoveryLabelsForTesting
---

<objective>
Make the discovery picker stable and trustworthy in the real editor workflow.

Purpose: Fix the row-index restoration bug behind the user's reported picker jump, and surface the richer public-room metadata from 08.1-01 without weakening manual connection entry.
Output: Endpoint-stable selection behavior, clearer public-room labels, and UI tests covering connect/refresh stability.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-server-discovery-history/08-VERIFICATION.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-RESEARCH.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_server_discovery_ui_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED UI coverage for endpoint-stable selection after refresh and connect</name>
  <files>tests/integration/plugin_server_discovery_ui_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp</files>
  <behavior>
    - Test 1: Selecting a public entry and refreshing the list keeps the same endpoint selected if it still exists.
    - Test 2: Connecting to a selected public entry and then remembering that endpoint does not make the combo box jump to a different row.
    - Test 3: Manual `Host` / `Port` edits still override picker-prefilled values naturally.
  </behavior>
  <action>Extend the existing discovery UI tests with scenarios that mutate list ordering after selection. The key regression to capture is remembered insertion or public re-sort after `Connect`, because that is the behavior the user reported. Keep layout assertions minimal and defer geometry complaints to Phase 9.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery_ui]"'</automated></verify>
  <done>The picker-jump bug is captured as a failing UI regression before the editor refresh logic changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve picker selection by endpoint and surface clearer public-room labels</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_server_discovery_ui_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp</files>
  <behavior>
    - Test 1: `refreshServerDiscoveryUi()` restores the same endpoint when the combined list changes order.
    - Test 2: After a successful connect inserts a remembered entry, the picker still identifies the endpoint the user actually chose.
    - Test 3: Public row labels present active-user information more clearly than a raw upstream blob when structured data exists.
    - Test 4: Manual host/port connect remains editable and non-automatic.
  </behavior>
  <action>Replace row-index-based selection restore with endpoint-key-based restore in the editor. Preserve the selected endpoint only when it still exists in the latest snapshot, and otherwise leave the draft fields untouched. Update row labels to use the richer count fields from 08.1-01 so the list no longer looks stuck on a suspicious unchanging `1` when better data is available. Do not attempt the larger discovery-area layout redesign here.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"'</automated></verify>
  <done>The discovery picker is now endpoint-stable across refresh/connect and presents clearer public-room identity/count information.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"'`

Phase-level verification criteria (applies after all Phase 08.1 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual validation confirms the picker stays on the chosen endpoint after `Connect` and after refresh.
</verification>

<success_criteria>
- All tasks completed
- DISC-02 combined picker behavior no longer scrambles selection after reordering
- Discovery labels reflect the richer count semantics from 08.1-01
- Manual host/port workflow remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-02-SUMMARY.md`
</output>
