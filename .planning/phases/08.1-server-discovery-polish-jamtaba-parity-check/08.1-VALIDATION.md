---
phase: 08.1
slug: server-discovery-polish-jamtaba-parity-check
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-17
---

# Phase 08.1 - Validation Strategy

> Per-phase validation contract for discovery-count fidelity, ordering, and selection-stability work.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 v3.8.1 |
| **Config file** | `tests/CMakeLists.txt` |
| **Quick run command** | `.\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"` or `.\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery_ui]"` |
| **Full suite command** | `ctest --test-dir build-vs --output-on-failure` |
| **Estimated runtime** | ~30-60 seconds for targeted discovery tags; ~90 seconds for full suite |

---

## Sampling Rate

- **After every task commit:** Run the narrowest relevant discovery tag for that task.
- **After every plan completion:** Run the combined discovery gate: `.\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery],[plugin_server_discovery_ui]"`.
- **After every plan wave:** Run `ctest --test-dir build-vs --output-on-failure`.
- **Before `$gsd-verify-work`:** Full suite must be green.
- **Max feedback latency:** 60 seconds for targeted feedback, ~90 seconds for the combined discovery gate.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 08.1-01-01 | 01 | 1 | DISC-01 | unit/integration | `.\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"` | present | pending |
| 08.1-02-01 | 02 | 2 | DISC-01, DISC-02 | integration | `.\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"` | present | pending |
| 08.1-03-01 | 03 | 3 | DISC-01, DISC-02 | integration/manual | `ctest --test-dir build-vs --output-on-failure` | present | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/server_discovery_parser_tests.cpp` - extend to cover richer count semantics, JSON-style payload handling or fallback behavior, and deterministic public ordering.
- [ ] `tests/integration/plugin_server_discovery_tests.cpp` - extend for public sorting and endpoint-stable merge behavior.
- [ ] `tests/integration/plugin_server_discovery_ui_tests.cpp` - extend for selection persistence by endpoint after refresh/connect rather than row index.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Public-room counts and ordering look believable against a real public list | DISC-01 | Real server population changes and upstream discovery payloads are environment-dependent | Open the VST3 in Ableton, refresh the public list, compare top rooms and visible counts against JamTaba or a live public-room view, and confirm the busiest rooms surface first. |
| Picker does not jump to another server after `Connect` or refresh | DISC-02 | This is easiest to verify in the real editor flow where remembered insertion and refresh timing both happen naturally | Select a public entry, connect, disconnect, refresh, and confirm the picker continues to identify the same endpoint rather than a different row with the same index. |
| Discovery remains usable even though the broader layout is still compact | DISC-01, DISC-02 | Readability in the real plugin window is host- and size-dependent | Verify that the discovery section is still workable after the polish changes, while documenting any remaining crowding for Phase 9 instead of treating it as a blocker here. |

---

## Validation Sign-Off

- [x] All tasks have automated verification paths or manual criteria
- [x] Sampling continuity is maintained with targeted discovery tags
- [x] Existing infrastructure already covers the phase; no new framework install is needed
- [x] No watch-mode flags
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
