---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "01"
subsystem: auth
tags: [ninjam, password-auth, state-serialization, catch2]
requires:
  - phase: 08.1-server-discovery-polish-jamtaba-parity-check
    provides: Stable processor-owned connection settings and wrapped plugin state persistence.
provides:
  - Password-bearing session settings persisted through project save and restore.
  - Live connect now forwards stored private-room passwords into framed transport auth.
  - Controlled private-room auth success and failure coverage via MiniNinjamServer.
affects: [08.2-02, connection-ui, private-room-validation]
tech-stack:
  added: []
  patterns: [Backward-compatible optional settings fields, controlled transport auth harness]
key-files:
  created: []
  modified:
    - src/app/session/SessionSettings.h
    - src/app/session/SessionSettings.cpp
    - src/infra/state/SessionSettingsSerializer.cpp
    - src/plugin/FamaLamaJamAudioProcessor.cpp
    - tests/integration/support/MiniNinjamServer.h
    - tests/integration/plugin_experimental_transport_tests.cpp
    - tests/integration/plugin_state_roundtrip_tests.cpp
    - tests/unit/settings_validation_tests.cpp
    - tests/unit/state_serialization_tests.cpp
key-decisions:
  - "Keep password persistence in SessionSettings with the existing serializer schema and treat missing password fields as empty for older blobs."
  - "Reuse MiniNinjamServer as the controlled private-room auth harness instead of introducing a second fake transport server."
patterns-established:
  - "Password-bearing session settings can be added compatibly by defaulting missing serialized fields to empty values."
  - "Transport auth regressions should be proven with deterministic MiniNinjamServer rules for success and explicit failure text."
requirements-completed: [P082-SC4]
duration: 4min
completed: 2026-03-19
---

# Phase 08.2 Plan 01: Password State and Controlled Auth Summary

**Password-bearing session settings now survive project state round-trip and drive controlled private-room auth success or explicit failure through the existing framed transport path**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-19T13:45:28Z
- **Completed:** 2026-03-19T13:49:19Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Added RED coverage for password-bearing settings persistence and controlled private-room auth outcomes before touching production code.
- Extended `MiniNinjamServer` so tests can require a password or return explicit auth failure text on demand.
- Threaded stored passwords through `SessionSettings`, `SessionSettingsSerializer`, and `attemptLiveConnect()` while preserving disconnected-on-restore behavior.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add RED auth and password-state regression coverage** - `03e9ca7` (test)
2. **Task 2: Thread password auth through persisted settings and the live connect path** - `29daa05` (fix)

## Files Created/Modified
- `src/app/session/SessionSettings.h` - Adds password to the settings contract.
- `src/app/session/SessionSettings.cpp` - Defaults password to empty while preserving existing normalization and validation behavior.
- `src/infra/state/SessionSettingsSerializer.cpp` - Serializes and restores password with empty fallback for older blobs.
- `src/plugin/FamaLamaJamAudioProcessor.cpp` - Passes the stored password into `framedTransport_.start(...)`.
- `tests/integration/support/MiniNinjamServer.h` - Adds deterministic password validation and explicit auth failure replies.
- `tests/integration/plugin_experimental_transport_tests.cpp` - Covers correct-password success and wrong-password failure text.
- `tests/integration/plugin_state_roundtrip_tests.cpp` - Covers password-bearing settings restore without reviving live runtime state.
- `tests/unit/settings_validation_tests.cpp` - Covers visible password retention and blank public-room behavior.
- `tests/unit/state_serialization_tests.cpp` - Covers password serialization round-trip and older-blob empty fallback.

## Decisions Made
- Kept password persistence inside `SessionSettingsSerializer` without bumping the schema version because older wrapped state can safely deserialize with a missing `password` property defaulting to empty.
- Kept the controlled auth path inside `MiniNinjamServer` so later UI validation work reuses the same end-to-end transport seam rather than diverging into a second harness.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The initial RED helper approach used `if constexpr` against a missing `SessionSettings::password` member, which MSVC still rejected during name lookup. The test helpers were tightened to SFINAE overloads so RED coverage compiles and fails on behavior instead of syntax.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Password-bearing settings and controlled auth outcomes are now automated end to end, so the later UI plan can focus on exposing the field and surfacing the failure copy.
- Manual private-room UX validation remains phase-level work for the final hardening gate; this plan only closes the automation seam.

## Self-Check: PASSED

- FOUND: .planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-01-SUMMARY.md
- FOUND: 03e9ca7
- FOUND: 29daa05
