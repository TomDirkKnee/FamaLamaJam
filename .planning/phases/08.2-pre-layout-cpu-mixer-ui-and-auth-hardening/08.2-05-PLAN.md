---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "05"
type: execute
wave: 5
depends_on:
  - 08.2-04
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/net/FramedSocketTransport.h
  - src/net/FramedSocketTransport.cpp
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_experimental_transport_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
autonomous: true
requirements:
  - P082-SC4
gap_closure: true
user_setup: []
must_haves:
  truths:
    - The live private-room auth path can prove which username and password-bearing auth attempt reaches the NINJAM transport seam.
    - Controlled automated auth coverage proves the processor-to-transport handoff no longer drops or rewrites the intended username between UI-applied settings and the NINJAM auth seam.
    - Wrong-password auth still surfaces an explicit authentication failure instead of collapsing into a generic transport error.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Live-connect handoff that preserves the intended username and password through the transport auth path.
      contains: attemptLiveConnect
    - path: src/net/FramedSocketTransport.cpp
      provides: Auth-attempt capture or corrected auth payload wiring that can explain real-host failures.
      contains: waitForAuthentication
    - path: tests/integration/support/MiniNinjamServer.h
      provides: Controlled server assertions for the exact username and password auth payload received from the client.
      contains: AuthRules
    - path: tests/integration/plugin_experimental_transport_tests.cpp
      provides: Regression coverage for non-empty protocol username handoff, correct-password success, and wrong-password failure copy.
      min_lines: 220
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/net/FramedSocketTransport.cpp
      via: attemptLiveConnect must pass the same active username and password that the UI draft stored, with no blank or stale username handoff.
      pattern: makeProtocolUsername|framedTransport_\\.start|waitForAuthentication
    - from: tests/integration/support/MiniNinjamServer.h
      to: tests/integration/plugin_experimental_transport_tests.cpp
      via: The harness should capture the auth packet username and reject mismatches so automation fails if the real-host bug reappears.
      pattern: expectedUsername|lastAuthUsername|buildAuthPassHash
    - from: src/net/FramedSocketTransport.cpp
      to: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      via: Auth failure details must remain explicit enough for the inline connection label to distinguish credential problems from generic transport failures.
      pattern: authFailureReason_|Wrong room password|Authentication failed
---

<objective>
Investigate and close the real-host private-room auth mismatch that kept Phase 08.2 blocked after the first Ableton pass.

Purpose: The controlled auth harness is green, but the real plugin window still failed the correct-password path. This plan must make the live auth handoff observable enough to diagnose and fix the mismatch before the host rerun.
Output: Tightened auth-path coverage, transport or processor instrumentation for the attempted username payload, and a concrete auth fix if the live handoff is wrong.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-CONTEXT.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-RESEARCH.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-VERIFICATION.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-04-SUMMARY.md
@docs/validation/phase8.2-pre-layout-hardening-matrix.md
@src/app/session/SessionSettings.h
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/net/FramedSocketTransport.h
@src/net/FramedSocketTransport.cpp
@tests/integration/support/MiniNinjamServer.h
@tests/integration/plugin_experimental_transport_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp

<interfaces>
From src/app/session/SessionSettings.h:
```cpp
struct SessionSettings
{
    std::string serverHost;
    std::uint16_t serverPort;
    std::string username;
    std::string password;
};
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
bool applySettingsFromUi(const app::session::SessionSettings& candidate,
                         app::session::SessionSettingsValidationResult* validation = nullptr);
bool requestConnect();
app::session::SessionSettings getActiveSettings() const;
app::session::ConnectionLifecycleSnapshot getLifecycleSnapshot() const;
```

From src/net/FramedSocketTransport.h:
```cpp
bool start(std::unique_ptr<juce::StreamingSocket> socket, std::string username, std::string password);
bool waitForAuthentication(int timeoutMs,
                           std::string* failureReason = nullptr,
                           std::string* effectiveUsername = nullptr);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED auth-handoff coverage that fails on blank, stale, or rewritten usernames</name>
  <files>tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_experimental_transport_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: The controlled auth harness records the last auth username sent by the client and can fail when it is blank or does not match the expected protocol username.
    - Test 2: A connect attempt with edited UI settings reaches the processor and transport with the same username and password that were visible in the connection area.
    - Test 3: Wrong-password auth still reports explicit credential failure text after the stronger username assertions are in place.
  </behavior>
  <action>Strengthen `MiniNinjamServer` so it captures the auth-packet username and can reject unexpected usernames in addition to wrong passwords. Extend `plugin_experimental_transport_tests.cpp` to fail if the transport sends an empty or stale username, and keep the existing correct-password and wrong-password cases. Add only the rehearsal-flow regression needed to prove the connection draft applied at click time still reaches the auth path; do not broaden this into new layout work or unrelated transport coverage.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_rehearsal_ui_flow]"'</automated>
  </verify>
  <done>The auth seam now has failing automated coverage for blank, stale, or unexpected usernames before production code is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the live auth handoff observable and fix the mismatch at the processor or transport seam</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/net/FramedSocketTransport.h, src/net/FramedSocketTransport.cpp, tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_experimental_transport_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: `attemptLiveConnect()` uses the current active settings and preserves the intended username through protocol normalization into `FramedSocketTransport::start(...)`.
    - Test 2: The transport or processor exposes enough test-only auth-attempt detail to distinguish blank-username, stale-settings, and wrong-password failures without relying on debugger-only inspection.
    - Test 3: Correct-password auth succeeds and wrong-password auth still yields explicit credential failure copy after the fix lands.
  </behavior>
  <action>Fix the actual seam that can explain the real-host failure. Start by making the auth attempt observable in a stable test-only way: capture the raw settings username, the normalized protocol username, and the resulting auth failure reason somewhere reachable from the integration tests. Then correct the broken handoff so the live connect path uses the intended username and password pair consistently for initial connect and retry. If the mismatch is inside `FramedSocketTransport` rather than `attemptLiveConnect()`, fix it there and keep the server harness aligned with the real protocol contract. Keep the scope inside Phase 08.2 auth hardening only; no broader UI or transport redesign.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; cmake --build build-vs --target famalamajam_tests --config Debug &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_rehearsal_ui_flow]"'</automated>
  </verify>
  <done>The controlled auth harness proves the live connect path no longer drops or rewrites the username incorrectly, and auth failures remain explicit for wrong credentials.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_experimental_transport],[plugin_rehearsal_ui_flow]"'`

Phase-level verification criteria (applies after all Phase 08.2 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the controlled private-room path succeeds with the correct password, shows inline failure on the wrong password, and records the CPU repro result on the original machine.
</verification>

<success_criteria>
- All tasks completed
- P082-SC4 is no longer blocked by an unexplained live-auth mismatch between automation and the real plugin window
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-05-SUMMARY.md`
</output>
