---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 08.2-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_room_controls_ui_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/integration/plugin_mixer_controls_tests.cpp
autonomous: true
requirements:
  - P082-SC2
  - P082-SC3
  - P082-SC4
user_setup: []
must_haves:
  truths:
    - The room workflow lives in a fixed right-hand sidebar instead of obscuring the mixer from above.
    - The room sidebar remains visible while disconnected, keeps its own scrollable feed, and retains topic, feed, composer, and vote controls.
    - The old `Default Gain`, `Default Pan`, and `Default Muted` controls are gone from the top setup area.
    - The connection area always shows a password field and explicit inline auth failure copy near the connection controls.
    - The mixer section exposes a footer-style master output control while keeping Local Monitor as a distinct strip.
    - New local and remote strips now seed from explicit unity defaults instead of the removed top-level default-strip controls.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Fixed two-column editor layout, password field, inline auth copy, right-sidebar room workflow, and mixer footer output control.
      contains: resized
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.h
      provides: Editor contract and testing hooks for the sidebar layout and mixer footer output control.
      contains: FamaLamaJamAudioProcessorEditor
    - path: tests/integration/plugin_room_controls_ui_tests.cpp
      provides: Regression coverage for the fixed right sidebar and independent room feed viewport.
      min_lines: 220
    - path: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      provides: Rehearsal-flow coverage for always-visible password entry and inline auth failure behavior.
      min_lines: 440
    - path: tests/integration/plugin_mixer_ui_tests.cpp
      provides: Editor-level coverage that the mixer footer output control exists without turning into an extra strip.
      min_lines: 220
    - path: tests/integration/plugin_mixer_controls_tests.cpp
      provides: Final-output coverage that the mixer footer master output control scales the full plugin output path.
      min_lines: 220
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: `createEditor()` must hand the editor the new master-output getter and setter callbacks rather than misusing strip state, while unity-default strip seeding moves away from the removed top-level controls.
      pattern: createEditor|masterOutput|defaultChannel
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_room_controls_ui_tests.cpp
      via: Sidebar geometry, always-visible room area, and independent feed viewport should be asserted at the editor level.
      pattern: isRoomSectionAboveMixerForTesting|hasRoomFeedViewportForTesting
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      via: Password field visibility and auth failure copy must remain in the connection workflow instead of being discoverable only in a generic status label.
      pattern: clickConnectForTesting|password
---

<objective>
Reshape the current JUCE editor into the locked interim layout without starting Phase 9 early.

Purpose: Make the mixer usable again now by moving room workflow into a fixed right sidebar, remove the dead top-level default-strip controls, surface the password/auth flow where users actually connect, and add the unity-default plus master-output behavior that replaces those dead controls.
Output: A fixed two-column editor with a persistent room sidebar, always-visible password input, inline auth failure messaging, unity-default strip seeding, and a mixer footer master output control that leaves Local Monitor separate.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-CONTEXT.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-RESEARCH.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-VALIDATION.md
@.planning/phases/07-chat-room-control-commands/07-02-SUMMARY.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-02-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_room_controls_ui_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/integration/plugin_mixer_controls_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
using SettingsGetter = std::function<app::session::SessionSettings()>;
using MixerStripsGetter = std::function<std::vector<MixerStripState>()>;
using MixerStripSetter = std::function<bool(const std::string&, float, float, bool)>;
[[nodiscard]] bool isRoomSectionAboveMixerForTesting() const noexcept;
[[nodiscard]] bool hasRoomFeedViewportForTesting() const noexcept;
```

From src/plugin/FamaLamaJamAudioProcessorEditor.cpp:
```cpp
void FamaLamaJamAudioProcessorEditor::timerCallback()
{
    refreshLifecycleStatus();
    refreshTransportStatus();
    refreshServerDiscoveryUi();
    refreshRoomUi();
    refreshMixerStrips();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED UI coverage for the fixed sidebar, visible password flow, and mixer footer output control</name>
  <files>tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <behavior>
    - Test 1: The room workflow sits in a fixed right-hand sidebar, not above the mixer, and the room feed still uses its own viewport.
    - Test 2: The room sidebar remains visible while disconnected and keeps topic, composer, and compact BPM or BPI vote controls available in the sidebar.
    - Test 3: The connection area always shows a password field and displays auth failure copy inline near the connection controls.
    - Test 4: The old top-level default-strip controls are absent, the mixer shows a footer-style master output control rather than an extra strip, and the visible Local Monitor strip stays distinct.
  </behavior>
  <action>Rewrite the existing editor regressions that still assert the room section lives above the mixer. Capture the exact locked Phase 08.2 shape: fixed right sidebar, independent room feed scroll area, compact vote controls near the top of that sidebar, always-visible password entry, and a footer output control that is visually part of the mixer section. Keep the assertions about tabs, popouts, resizable panes, and broad Phase 9 redesign out of this plan.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui]"'</automated>
  </verify>
  <done>The current editor shape is replaced by failing automated expectations for the locked interim sidebar and mixer/auth cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the fixed right sidebar and finish mixer/auth UI cleanup</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <behavior>
    - Test 1: The editor keeps setup and transport on the left while the room workflow lives in a fixed right-hand sidebar that stays visible while disconnected.
    - Test 2: `makeDraftFromUi()` and `loadFromSettings()` include the password field, while auth failures are surfaced inline in the connection area instead of hiding behind vague generic status wording.
    - Test 3: The top setup area no longer shows `Default Gain`, `Default Pan`, or `Default Muted`, and new strips seed from explicit unity defaults instead of those removed controls.
    - Test 4: The mixer footer control reads and writes the processor-owned master output state while the existing Local Monitor strip remains a distinct strip and master output scales the final plugin output path.
  </behavior>
  <action>Refactor the editor layout to a deterministic two-column arrangement: left side for setup, transport, discovery, and mixer; right side for room topic, compact BPM/BPI vote controls, feed viewport, and composer. Use a sensible fixed sidebar width and keep the room sidebar present even when disconnected so the editor does not jump. Add a real password text editor next to Host, Port, and Username, and show auth-specific failure copy inline near those controls. Remove the dead default-strip controls entirely from the top area, replace new-strip seeding with explicit unity defaults, and add a footer-style master output control backed by processor-owned state. It must read as mixer output control, not as a new strip.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_mixer_controls]"'</automated>
  </verify>
  <done>The interim layout is usable at plugin-window sizes, password/auth is visible where users connect, new strips default to unity after the dead top controls are removed, and the mixer footer exposes master output without collapsing Local Monitor into it.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_mixer_controls]"'`

Phase-level verification criteria (applies after all Phase 08.2 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the mixer is no longer obscured by the room workflow in a practical plugin window.
</verification>

<success_criteria>
- All tasks completed
- P082-SC2 fixed-sidebar room layout no longer obscures the mixer workflow
- P082-SC3 dead default-strip UI is gone and the master output footer control exists
- P082-SC4 password/auth controls are visible and explicit in the connection workflow
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-02-SUMMARY.md`
</output>
