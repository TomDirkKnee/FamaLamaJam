---
phase: 08.2
slug: pre-layout-cpu-mixer-ui-and-auth-hardening
status: in_progress
created: 2026-03-19
updated: 2026-03-19
---

# Verification Summary

Phase 08.2 automation is green and the Debug VST3 for the manual hardening pass has been rebuilt. Phase closeout remains blocked on the real Ableton/private-room verification pass because the sidebar usability, wrong-password UX, master-output behavior, and every-four-beat CPU-spike outcome still need host-level confirmation.

## Automated Results

- `cmake --build build-vs --target famalamajam_tests --config Debug` - pass (2026-03-19)
- `famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_mixer_controls],[plugin_experimental_transport]"` - pass (`28` test cases / `302` assertions, 2026-03-19)
- `ctest --test-dir build-vs --output-on-failure` - pass (`111/111` tests, 2026-03-19)
- `cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug` - pass (2026-03-19)

## VST3 Under Test

- `build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3` - rebuilt on 2026-03-19 for the Phase 08.2 hardening validation pass

## Manual Outcomes

Source matrix: `docs/validation/phase8.2-pre-layout-hardening-matrix.md`

| ID | Outcome | Notes |
|----|---------|-------|
| P8.2-SIDEBAR-01 | pending | Need Ableton confirmation that the fixed right sidebar remains visible while disconnected and no longer obscures the mixer or stream monitoring controls. |
| P8.2-MIXER-02 | pending | Need host confirmation that the mixer footer master-output control is present, the dead default controls are gone, and Local Monitor remains distinct. |
| P8.2-AUTH-03 | pending | Need controlled private-room validation for both correct-password success and explicit wrong-password failure near the connection controls. |
| P8.2-CPU-04 | pending | Need the original repro machine to confirm whether the prior every-four-beat spike is materially reduced or remains as documented residual machine-sensitive behavior. |

## Current Verification Position

- Targeted automated coverage for the Phase 08.2 scope is green, including CPU diagnostics, right-sidebar layout, private-room password auth, and mixer footer master-output behavior.
- Full-suite regression coverage is green, so the urgent hardening pass is not currently breaking earlier phases.
- The manual pass must use the rebuilt Debug VST3 above rather than inferred test behavior.
- Mixer baseline evidence now comes from the footer master-output path and its automated coverage, not from the removed top-level default gain/pan/muted controls.

## Residual Constraints

- CPU-spike improvement is still only partially closed until the same Ableton repro machine confirms whether the observed every-four-beat spike is materially reduced.
- Password/auth UX is covered in automation, but the inline wrong-password experience still needs a real controlled private-room check before the phase can be marked complete.
- Phase 08.2 should not be declared complete until the manual matrix records the sidebar, mixer footer, auth, and CPU outcomes explicitly.
