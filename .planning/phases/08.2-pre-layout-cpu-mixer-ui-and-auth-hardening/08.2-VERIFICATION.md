---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
verified: 2026-03-20T12:05:39Z
status: gaps_found
score: 3/4 must-haves verified
re_verification:
  previous_status: blocked
  previous_score: 2/4
  gaps_closed:
    - "The real plugin-window private-room auth rerun now passes for both the correct-password and wrong-password paths."
  gaps_remaining:
    - "The original every-four-beat Ableton CPU repro is still not exercised on the user's machine."
  regressions: []
gaps:
  - truth: "CPU spikes around interval or beat boundaries are investigated, reproduced, and materially reduced versus the current build."
    status: partial
    reason: "The code and automated diagnostics show a real editor-churn mitigation, but the required Ableton repro on the original machine was deferred, so material host-level reduction is still unverified."
    artifacts:
      - path: "src/plugin/FamaLamaJamAudioProcessorEditor.cpp"
        issue: "Dirty-checked room-feed and mixer refresh mitigation exists, but the real-host CPU outcome is not yet confirmed."
      - path: "docs/validation/phase8.2-pre-layout-hardening-matrix.md"
        issue: "P8.2-CPU-05 is recorded as not exercised."
    missing:
      - "Rerun the every-four-beat CPU repro in Ableton on the original machine."
      - "Record whether the remaining spikes are materially reduced or still disruptive enough to keep the phase blocked."
---

# Phase 08.2: Pre-Layout CPU, Mixer, UI, and Auth Hardening Verification Report

**Phase Goal:** Urgently harden CPU spikes, interim UI usability, mixer control cleanup, and password/auth validation enough to unblock the later layout work.
**Verified:** 2026-03-20T12:05:39Z
**Status:** gaps_found
**Re-verification:** Yes - prior verification existed, and this rerun closed the real-host auth gap but still left the same-machine CPU follow-up unexercised.

Targeted automation is currently green:

- `famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_mixer_controls],[plugin_experimental_transport]"` - pass (`28` test cases / `302` assertions) on 2026-03-19
- `ctest --test-dir build-vs --output-on-failure` - pass (`111/111` tests) on 2026-03-19

The codebase substantively implements the intended sidebar, mixer, password-threading, and CPU-diagnostics work. Phase 08.2 still does **not** achieve its goal because the recorded manual host evidence now clears the private-room auth rerun but still leaves the CPU repro unverified on the actual Ableton machine.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | CPU spikes around interval or beat boundaries are investigated, reproduced, and materially reduced versus the current build. | ? PARTIAL | Deterministic diagnostics and editor dirty-checking exist in `plugin_pre_layout_cpu_diagnostics` and `src/plugin/FamaLamaJamAudioProcessorEditor.cpp`, but `docs/validation/phase8.2-pre-layout-hardening-matrix.md` records `P8.2-CPU-05` as `not exercised`. |
| 2 | The chat area no longer obscures the mixer or stream-monitoring workflow in practical Ableton window sizes. | VERIFIED | The editor lays out connection/mixer content on the left and the room workflow in a fixed right sidebar, with the room feed in its own viewport. The layout is covered by `plugin_room_controls_ui` and the manual matrix records `P8.2-SIDEBAR-01` as `pass`. |
| 3 | Non-working `Default Gain`, `Default Pan`, and `Default Muted` controls are removed, and a clear master output control exists inside the mixer section. | VERIFIED | The old default-strip controls are absent, the editor adds a footer-style `Master Output` slider, the processor applies that gain in the audio path, `plugin_mixer_ui` and `plugin_mixer_controls` pass, and the manual matrix records `P8.2-MIXER-02` as `pass`. |
| 4 | Password/authentication workflow is visible in the UI and validated against a local or controlled test server path. | VERIFIED | Password storage, restore, connect wiring, visible password entry, inline auth failure copy, and controlled auth tests all exist, and the manual matrix now records both `P8.2-AUTH-03` and `P8.2-AUTH-04` as `pass`. |

**Score:** 3/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/app/session/SessionSettings.h` | Settings contract carries password end to end. | VERIFIED | `SessionSettings` includes `password`. |
| `src/infra/state/SessionSettingsSerializer.cpp` | Save and restore password-bearing settings payloads. | VERIFIED | Serializer writes and reads the `password` property and validates the restored struct before use. |
| `src/plugin/FamaLamaJamAudioProcessor.cpp` | Live connect uses stored password, master output reaches audio path, and new strips seed from unity defaults. | VERIFIED | `attemptLiveConnect()` passes `settings.password` into `framedTransport_.start()`, `createEditor()` wires master-output getters/setters, `processBlock()` applies master-output gain, and new strips use `makeUnityMixerStripMixState()`. |
| `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` | Fixed sidebar layout, visible password field, inline auth copy, mixer footer output control, and CPU mitigation. | VERIFIED | Layout places connection and mixer on the left, room workflow on the right, always shows the password row, emits inline auth copy for auth failures, and dirty-checks room/mixer refresh work. |
| `tests/integration/plugin_experimental_transport_tests.cpp` | Controlled auth coverage for correct and incorrect passwords. | VERIFIED | Contains passing good-password and wrong-password cases against `MiniNinjamServer`. |
| `tests/integration/plugin_room_controls_ui_tests.cpp` | Regression coverage for fixed right sidebar and independent room-feed viewport. | VERIFIED | Covers disconnected sidebar visibility and explicit right-sidebar placement. |
| `tests/integration/plugin_rehearsal_ui_flow_tests.cpp` | Coverage for visible password entry, inline auth copy, and Connect applying the current draft. | VERIFIED | Covers password visibility, inline auth copy placement, and Connect applying the active draft before connecting. |
| `tests/integration/plugin_mixer_ui_tests.cpp` | Coverage for dead-control removal and footer-style master output. | VERIFIED | Verifies the old controls are gone and `Master Output` is not represented as a mixer strip. |
| `tests/integration/plugin_mixer_controls_tests.cpp` | Coverage that master output scales the full plugin output path. | VERIFIED | Verifies attenuation for both mixed playback and metronome-only output. |
| `tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp` | Deterministic CPU-diagnostic coverage for editor churn and processor hot-path counters. | VERIFIED | Verifies unchanged room-feed and mixer refreshes stay off the old unconditional update path and records a JamTaba-informed diagnosis note. |
| `docs/validation/phase8.2-pre-layout-hardening-matrix.md` | Manual Ableton and controlled-private-room validation artifact. | VERIFIED | The artifact exists and now shows the corrected auth rerun passing while the CPU repro remains explicitly unexercised. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/infra/state/SessionSettingsSerializer.cpp` | `src/plugin/FamaLamaJamAudioProcessor.cpp` | Restored settings must carry the saved password into the next live connect attempt. | WIRED | Password is serialized, deserialized, applied to active settings, and then consumed by `attemptLiveConnect()`. |
| `src/plugin/FamaLamaJamAudioProcessor.cpp` | `tests/integration/plugin_experimental_transport_tests.cpp` | The processor should pass the stored password into the transport auth seam. | WIRED | The controlled auth tests pass for both correct and incorrect passwords. |
| `src/plugin/FamaLamaJamAudioProcessor.cpp` | `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` | `createEditor()` must hand the editor the master-output callbacks and use current settings on Connect. | WIRED | `createEditor()` passes master-output getters/setters, and the editor applies the current draft before `connectHandler_()`. |
| `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` | `tests/integration/plugin_room_controls_ui_tests.cpp` | Sidebar geometry and room-feed viewport should be asserted at editor level. | WIRED | The tests assert right-sidebar placement and room-feed viewport behavior. |
| `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` | `tests/integration/plugin_rehearsal_ui_flow_tests.cpp` | Password field visibility and inline auth copy must remain near the connection workflow. | WIRED | The tests assert the password row, Connect button, and inline auth label location. |
| `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` | `tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp` | CPU regressions should assert reduced avoidable UI work counts, not vague timing. | WIRED | The tests assert no room-feed rebuilds and no unchanged-strip updates during repeated refreshes. |
| `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-VERIFICATION.md` | `docs/validation/phase8.2-pre-layout-hardening-matrix.md` | Manual outcomes should cite the focused matrix directly. | WIRED | This report now mirrors `P8.2-SIDEBAR-01` through `P8.2-CPU-05` exactly. |
| `.planning/ROADMAP.md` | `.planning/STATE.md` | Final status should agree on whether Phase 08.2 actually closed. | WIRED | Both documents now mark Phase 08.2 blocked only by the still-unexercised same-machine CPU follow-up. |

### Requirements Coverage

`REQUIREMENTS.md` does not currently define `P082-SC1`, `P082-SC2`, `P082-SC3`, or `P082-SC4`, so these phase-scoped IDs were verified against `ROADMAP.md` and the 08.2 plan frontmatter instead. Their absence from `REQUIREMENTS.md` was **not** treated as completion.

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `P082-SC1` | `08.2-03`, `08.2-04` | Investigate, reproduce, and materially reduce CPU spikes. | ? PARTIAL | CPU diagnostics and dirty-check mitigation exist and tests pass, but `P8.2-CPU-05` is still `not exercised` in the recorded Ableton matrix. |
| `P082-SC2` | `08.2-02`, `08.2-04` | Keep the room/chat workflow from obscuring the mixer. | SATISFIED | The editor layout and `plugin_room_controls_ui` coverage match the fixed-sidebar contract, and `P8.2-SIDEBAR-01` passed manually. |
| `P082-SC3` | `08.2-02`, `08.2-04` | Remove dead default controls and add a clear mixer master output. | SATISFIED | The editor and processor wire a footer-style master output, the old controls are gone, tests pass, and `P8.2-MIXER-02` passed manually. |
| `P082-SC4` | `08.2-01`, `08.2-02`, `08.2-04` | Make password/auth workflow visible and validate it against a controlled path. | SATISFIED | Code and automation are present, and the recorded Ableton rerun now marks both `P8.2-AUTH-03` and `P8.2-AUTH-04` as `pass`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| none | - | No `TODO`, `FIXME`, placeholder text, or empty phase-specific implementations were found in the inspected 08.2 implementation and test files. | INFO | No obvious stub markers were found; the blockers are evidence gaps, not placeholder code. |

### Human Verification Required

### 1. Original Every-Four-Beat CPU Repro

**Test:** Reproduce the original CPU-spike scenario on the same Ableton machine used for the report that triggered Phase 08.2.  
**Expected:** Spikes are materially reduced, or the remaining behavior is documented explicitly as still disruptive and machine-sensitive.  
**Why human:** The measured code-side mitigation exists, but the original repro is host- and machine-sensitive and was explicitly deferred.

### Gaps Summary

Phase 08.2 produced substantive implementation work: password state is threaded into transport auth, the editor now exposes a fixed right sidebar and footer-style master output, and the CPU pass replaced vague timing claims with deterministic diagnostics plus UI dirty-checking. Automated coverage remains strong and the full suite is green.

That is still not enough to claim goal achievement. The goal was to harden the build enough to unblock later layout work, and the recorded manual artifacts now say only part of that remains open: the real plugin-window auth rerun passed for both the correct-password and wrong-password paths, but the original CPU repro remains unexercised on the user machine. The phase therefore remains blocked before Phase 9 on the narrow host-CPU evidence gap alone.

---

_Verified: 2026-03-20T12:05:39Z_  
_Verifier: Codex (gsd-verifier)_
