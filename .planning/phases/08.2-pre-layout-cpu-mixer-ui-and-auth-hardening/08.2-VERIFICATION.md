---
phase: 08.2
slug: pre-layout-cpu-mixer-ui-and-auth-hardening
status: blocked
created: 2026-03-19
updated: 2026-03-19
---

# Verification Summary

Phase 08.2 automation is green and the Debug VST3 for the manual hardening pass was rebuilt, but the manual Ableton/private-room pass only closed the sidebar and mixer baseline checks. The controlled private-room correct-password attempt failed, the wrong-password inline error was not exercised after that failure, and the every-four-beat CPU repro was deferred for now, so Phase 08.2 remains blocked.

## Automated Results

- `cmake --build build-vs --target famalamajam_tests --config Debug` - pass (2026-03-19)
- `famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_mixer_ui],[plugin_mixer_controls],[plugin_experimental_transport]"` - pass (`28` test cases / `302` assertions, 2026-03-19)
- `ctest --test-dir build-vs --output-on-failure` - pass (`111/111` tests, 2026-03-19)
- `cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug` - pass (2026-03-19)

## VST3 Under Test

- `build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3` - rebuilt on 2026-03-19 for the Phase 08.2 hardening validation pass

## Manual Outcomes

Source matrix: `docs/validation/phase8.2-pre-layout-hardening-matrix.md`

| ID | Outcome | Notes |
|----|---------|-------|
| P8.2-SIDEBAR-01 | pass | User confirmed the fixed right-hand sidebar stays visible and no longer hides the mixer. |
| P8.2-MIXER-02 | pass | User confirmed the mixer footer master output exists, the old `Default Gain`/`Default Pan`/`Default Muted` controls are gone, and output scaling behaves correctly. |
| P8.2-AUTH-03 | fail | Controlled private-room auth with the correct password was declined in the real plugin flow. The user suspects the username may be sent as `""`, but that remains a manual observation rather than a confirmed root cause. |
| P8.2-AUTH-04 | not exercised | The wrong-password inline auth error was not exercised because the correct-password path already failed. |
| P8.2-CPU-05 | not exercised | The prior every-four-beat CPU-spike repro was deferred for now, so residual machine-sensitive CPU risk remains open. |

## Current Verification Position

- Targeted automated coverage for the Phase 08.2 scope is green, including CPU diagnostics, right-sidebar layout, private-room password auth, and mixer footer master-output behavior.
- Full-suite regression coverage is green, so the urgent hardening pass is not currently breaking earlier phases.
- Manual host evidence now confirms the fixed sidebar and mixer footer master-output baseline in Ableton.
- Manual host evidence does not confirm the auth path: the controlled private-room correct-password attempt failed before the wrong-password inline error could be exercised.
- The manual CPU repro did not run in this pass, so the phase still lacks host confirmation for how much of the prior every-four-beat spike remains machine-sensitive after the editor-churn mitigation.

## Residual Constraints

- CPU-spike improvement is still only partially closed until the same Ableton repro machine confirms whether the observed every-four-beat spike is materially reduced or still materially disruptive.
- Password/auth UX now has conflicting evidence: automation is green, but the real controlled private-room correct-password path failed, so Phase 08.2 cannot be declared complete.
- The wrong-password inline error remains unverified because the manual session stopped at the correct-password failure.
- Phase 08.2 should stay blocked until the auth failure is investigated and the deferred wrong-password and CPU checks are rerun explicitly.
