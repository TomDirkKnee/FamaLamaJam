---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/SessionSettings.h
  - src/app/session/SessionSettings.cpp
  - src/infra/state/SessionSettingsSerializer.cpp
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/settings_validation_tests.cpp
  - tests/unit/state_serialization_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/integration/plugin_experimental_transport_tests.cpp
  - tests/integration/support/MiniNinjamServer.h
autonomous: true
requirements:
  - P082-SC4
user_setup: []
must_haves:
  truths:
    - Private-room passwords are no longer dropped before transport auth starts.
    - Wrong-password and correct-password outcomes are both reproducible against a controlled server path.
  artifacts:
    - path: src/app/session/SessionSettings.h
      provides: Connection settings contract that can carry a password end to end.
      contains: SessionSettings
    - path: src/infra/state/SessionSettingsSerializer.cpp
      provides: Save and restore behavior for password-bearing settings payloads.
      contains: serialize
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Password-aware live connect path that no longer hardcodes an empty password before transport auth.
      contains: attemptLiveConnect
    - path: tests/integration/support/MiniNinjamServer.h
      provides: Controlled auth success or failure harness for real password-path regression coverage.
      contains: MiniNinjamServer
    - path: tests/integration/plugin_experimental_transport_tests.cpp
      provides: Automated auth-path coverage for correct and incorrect passwords.
      min_lines: 160
  key_links:
    - from: src/infra/state/SessionSettingsSerializer.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Restored settings must carry the saved password into the next live connect attempt.
      pattern: SessionSettingsSerializer|attemptLiveConnect
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_experimental_transport_tests.cpp
      via: The processor should pass the stored password into the existing transport auth seam instead of hardcoding an empty string.
      pattern: framedTransport_\\.start|password
---

<objective>
Create the missing auth and password-state seams for Phase 08.2 before any editor layout work lands.

Purpose: Finish the end-to-end password/auth path and lock it down with controlled automated coverage so later UI work is wiring rather than guesswork.
Output: Password-aware settings/state/auth coverage and controlled private-room auth harness behavior ready for the later editor and verification work.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-CONTEXT.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-RESEARCH.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-VALIDATION.md
@.planning/phases/01-plugin-foundation-session-configuration/01-02-SUMMARY.md
@src/app/session/SessionSettings.h
@src/app/session/SessionSettings.cpp
@src/infra/state/SessionSettingsSerializer.cpp
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/net/FramedSocketTransport.cpp
@tests/integration/support/MiniNinjamServer.h
@tests/integration/plugin_experimental_transport_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp
@tests/unit/settings_validation_tests.cpp
@tests/unit/state_serialization_tests.cpp

<interfaces>
From src/app/session/SessionSettings.h:
```cpp
struct SessionSettings
{
    std::string serverHost;
    std::uint16_t serverPort;
    std::string username;
};
```

From src/net/FramedSocketTransport.cpp:
```cpp
bool FramedSocketTransport::start(std::unique_ptr<juce::StreamingSocket> socket,
                                  std::string username,
                                  std::string password)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED auth and password-state regression coverage</name>
  <files>tests/unit/settings_validation_tests.cpp, tests/unit/state_serialization_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/integration/plugin_experimental_transport_tests.cpp, tests/integration/support/MiniNinjamServer.h</files>
  <behavior>
    - Test 1: Session settings accept a visible password field, preserve blank-public-room behavior, and serialize or deserialize it without corrupt fallback.
    - Test 2: Plugin state round-trip restores password-bearing settings separately from runtime-only discovery or live transport state.
    - Test 3: Controlled transport auth passes with the right password and fails explicitly with the wrong password or configured server failure text.
  </behavior>
  <action>Extend the existing unit and integration suites before changing production code. Reuse `MiniNinjamServer` instead of inventing a second auth harness: add just enough configurability to express correct-password acceptance, wrong-password rejection, and explicit failure text. Keep the RED surface on auth and password-bearing state round-trip only; do not pull editor layout or master-output assertions into this plan.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[settings_validation],[state_serialization],[plugin_state_roundtrip],[plugin_experimental_transport]"'</automated>
  </verify>
  <done>Missing password/auth behavior is captured in failing automated coverage with a controlled private-room harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread password auth through persisted settings and the live connect path</name>
  <files>src/app/session/SessionSettings.h, src/app/session/SessionSettings.cpp, src/infra/state/SessionSettingsSerializer.cpp, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/unit/settings_validation_tests.cpp, tests/unit/state_serialization_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/integration/plugin_experimental_transport_tests.cpp, tests/integration/support/MiniNinjamServer.h</files>
  <behavior>
    - Test 1: `attemptLiveConnect()` passes the stored password into the existing transport auth seam rather than a hardcoded empty string.
    - Test 2: Password-bearing settings survive project save or restore and stay disconnected on restore, matching the existing Phase 1 policy.
  </behavior>
  <action>Add `password` to `SessionSettings`, normalization, validation, and serialization, then thread it through the processor's live-connect path into `FramedSocketTransport::start(...)`. Keep backward compatibility practical: preserve safe fallback behavior for older state blobs and continue treating restore as disconnected until the user explicitly reconnects.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[settings_validation],[state_serialization],[plugin_state_roundtrip],[plugin_experimental_transport]"'</automated>
  </verify>
  <done>Password auth and restored private-room settings are both green in automation.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[settings_validation],[state_serialization],[plugin_state_roundtrip],[plugin_experimental_transport]"'`

Phase-level verification criteria (applies after all Phase 08.2 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual validation confirms the rebuilt plugin can connect to a controlled private-room path with a correct password and surfaces a wrong-password failure inline.
</verification>

<success_criteria>
- All tasks completed
- P082-SC4 password workflow is automated end to end against a controlled auth path
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-01-SUMMARY.md`
</output>
