---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "05"
subsystem: auth
tags: [ninjam, auth, transport, juce, catch2, ableton]
requires:
  - phase: 08.2-04
    provides: Blocked manual validation evidence showing the real-host private-room auth path failed despite green automation.
provides:
  - Passworded NINJAM auth now preserves the applied username instead of forcing anonymous-mode usernames.
  - MiniNinjamServer can capture and assert the exact auth username that reaches the transport seam.
  - Processor-backed auth-attempt snapshots expose raw settings usernames, normalized protocol usernames, and explicit auth failure reasons to integration tests.
affects: [08.2-06, ableton-validation, auth-follow-up]
tech-stack:
  added: []
  patterns:
    - password-bearing auth preserves the applied username while anonymous normalization remains a blank-password path
    - controlled auth harnesses should assert the exact username received, not only password success or failure
key-files:
  created: []
  modified:
    - src/plugin/FamaLamaJamAudioProcessor.h
    - src/plugin/FamaLamaJamAudioProcessor.cpp
    - tests/integration/support/MiniNinjamServer.h
    - tests/integration/plugin_experimental_transport_tests.cpp
    - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
key-decisions:
  - "Treat passworded auth as a distinct protocol path that must keep the applied username unchanged instead of forcing the anonymous prefix."
  - "Expose auth-attempt snapshots from the processor so tests can distinguish stale settings, rewritten usernames, and real credential failures without debugger-only inspection."
patterns-established:
  - "When a real host contradicts green harness coverage, tighten the harness to assert the exact transport payload rather than infer correctness from success alone."
  - "Auth regressions should prove both the editor draft handoff and the processor transport seam in automated coverage."
requirements-completed: [P082-SC4]
duration: 23 min
completed: 2026-03-19
---

# Phase 08.2 Plan 05: Private-Room Auth Handoff Summary

**Passworded NINJAM auth now keeps the applied username intact, with transport-seam username assertions and processor auth snapshots closing the anonymous-prefix mismatch that blocked the real host pass.**

## Performance

- **Duration:** 23 min
- **Started:** 2026-03-19T20:00:00Z
- **Completed:** 2026-03-19T20:22:56Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added RED coverage that made the real blocker reproducible in automation by asserting the exact username received by `MiniNinjamServer`.
- Fixed the live connect seam so passworded auth uses the applied username directly instead of rewriting it to `anonymous:*`.
- Added processor-side auth-attempt snapshots so integration tests can inspect the raw settings username, protocol username, and failure reason after each connect attempt.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add RED auth-handoff coverage that fails on blank, stale, or rewritten usernames** - `8fb40bf` (test)
2. **Task 2: Make the live auth handoff observable and fix the mismatch at the processor or transport seam** - `120840a` (fix)

**Plan metadata:** Pending summary commit

## Files Created/Modified

- `src/plugin/FamaLamaJamAudioProcessor.h` - Adds the test-only live auth attempt snapshot contract.
- `src/plugin/FamaLamaJamAudioProcessor.cpp` - Preserves applied usernames for passworded auth and records auth-attempt details around live connects.
- `tests/integration/support/MiniNinjamServer.h` - Captures the last auth username and can reject unexpected usernames in the controlled harness.
- `tests/integration/plugin_experimental_transport_tests.cpp` - Proves passworded auth keeps the intended username and still reports wrong-password failures explicitly.
- `tests/integration/plugin_rehearsal_ui_flow_tests.cpp` - Proves the current editor draft reaches the live auth seam at Connect time.

## Decisions Made

- Preserved the old anonymous normalization only for blank-password connects so existing guest-style behavior stays intact while passworded auth matches real-host expectations.
- Kept the observability seam in the processor instead of adding debugger-only logging, because the plan needed stable integration assertions for both success and failure paths.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The existing controlled harness only validated password hashes, which allowed the real-host username rewrite bug to stay invisible until manual Ableton validation failed.
- The first narrowed build timed out under the default shell timeout and was rerun successfully with a longer limit; no code change was required.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 08.2-05 closes the unexplained auth mismatch with automated proof at the editor, processor, and controlled server seams.
- Phase 08.2 still needs the real Ableton reruns captured by plan 08.2-06: correct-password auth, wrong-password inline failure copy, and the deferred CPU repro.

## Self-Check

PASSED

- FOUND: `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-05-SUMMARY.md`
- FOUND: `8fb40bf`
- FOUND: `120840a`

---
*Phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening*
*Completed: 2026-03-19*
