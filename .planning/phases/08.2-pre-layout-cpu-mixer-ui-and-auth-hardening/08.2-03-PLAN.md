---
phase: 08.2-pre-layout-cpu-mixer-ui-and-auth-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - 08.2-02
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp
  - .planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-03-SUMMARY.md
autonomous: true
requirements:
  - P082-SC1
user_setup: []
must_haves:
  truths:
    - Periodic spike work is diagnosis-first: the phase can explain what code path was changed and why.
    - The diagnosis must explicitly compare the current hot path against likely JamTaba behavior before deciding whether the issue is code-driven, machine-sensitive, or both.
    - If the spike source is editor refresh churn, slightly less-smooth meter or room updates are acceptable before touching core chat, sync, or audio semantics.
    - If the spike source is in the processor hot path, the mitigation should reduce that identified work without regressing server-authoritative sync or room behavior.
    - The phase should document whether the remaining spike risk looks code-driven, machine-sensitive, or both.
  artifacts:
    - path: tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp
      provides: Deterministic regression coverage for the measured spike contributors and the chosen mitigation path.
      min_lines: 180
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Measured processor or output-path mitigation for the identified periodic spike source.
      contains: processBlock
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Dirty-checking or throttling for room or mixer refresh work when UI churn is the spike source.
      contains: timerCallback
    - path: .planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-03-SUMMARY.md
      provides: Recorded JamTaba-informed diagnosis and the final explanation of whether the spike is code-driven, machine-sensitive, or both.
      contains: JamTaba
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp
      via: CPU regressions should assert reduced avoidable UI work counts, not vague wall-clock timing.
      pattern: refreshRoomUi|refreshMixerStrips
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp
      via: Diagnostics should show whether processor hot-path work changed in the targeted periodic scenario.
      pattern: processBlock|diagnostic
---

<objective>
Diagnose and materially reduce the periodic CPU spikes without widening scope into Phase 9 or rewriting core timing/audio behavior blindly.

Purpose: Prove whether the every-four-beat spike is driven by code in the plugin path, editor refresh churn, or both, compare that diagnosis against likely JamTaba behavior, and then take the least invasive mitigation that materially reduces the code-driven part.
Output: Deterministic CPU diagnostic coverage, measured instrumentation around the suspected spike paths, a JamTaba-informed diagnosis artifact, and a targeted mitigation that preserves the existing rehearsal workflow.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-CONTEXT.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-RESEARCH.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_mixer_controls_tests.cpp
@tests/integration/plugin_room_controls_ui_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.cpp:
```cpp
void FamaLamaJamAudioProcessor::processBlock(juce::AudioBuffer<float>& buffer, juce::MidiBuffer&)
{
    ensureLocalMonitorMixerStrip();
    resetMixerStripMeters();
    ...
}
```

From src/plugin/FamaLamaJamAudioProcessorEditor.cpp:
```cpp
void FamaLamaJamAudioProcessorEditor::timerCallback()
{
    refreshLifecycleStatus();
    refreshTransportStatus();
    refreshServerDiscoveryUi();
    refreshRoomUi();
    refreshMixerStrips();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED diagnostics for the periodic spike contributors</name>
  <files>tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessorEditor.h</files>
  <behavior>
    - Test 1: The diagnostics can observe repeated work in the suspected periodic scenario without relying on flaky wall-clock assertions.
    - Test 2: Room feed rebuilds and mixer refresh work are visible as measurable counters so unnecessary editor churn can be reduced safely.
    - Test 3: Processor-side diagnostics can distinguish the targeted hot path from unrelated lifecycle or chat behavior.
    - Test 4: The phase has enough evidence to compare the current hot path against likely JamTaba behavior and record that diagnosis explicitly.
  </behavior>
  <action>Create a dedicated `plugin_pre_layout_cpu_diagnostics` integration suite that expresses the spike problem in deterministic work-count terms instead of absolute milliseconds. Add only the testing hooks needed to observe the relevant work: room feed rebuild counts, mixer refresh cadence, and processor diagnostic counters around the suspected periodic mix path. Use those observations plus the already-researched JamTaba reference behavior to frame the diagnosis you will record in the plan summary; keep the scope narrow and do not build a permanent in-product profiler UI.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics]"'</automated>
  </verify>
  <done>The every-four-beat spike concern is represented by failing deterministic diagnostics rather than guesswork.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the measured CPU mitigation on the identified hot path</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_pre_layout_cpu_diagnostics_tests.cpp</files>
  <behavior>
    - Test 1: Avoidable room or mixer UI refresh work no longer rebuilds or updates at the old unconditional cadence when nothing meaningful changed.
    - Test 2: If the chosen mitigation touches the processor path, the diagnostics prove the targeted work was reduced without changing core sync or room semantics.
    - Test 3: Existing room and mixer behavior still pass their regressions after the mitigation.
    - Test 4: The final summary records whether the remaining risk is code-driven, machine-sensitive, or both, and ties that conclusion back to the JamTaba-informed comparison.
  </behavior>
  <action>Use the diagnostics from Task 1 to choose the narrowest effective mitigation. Start with the explicitly allowed tradeoff from the context: reduce or dirty-check realtime meter, room feed, or mixer refresh cadence before touching core chat, sync, or audio semantics. If the measured hot path is in `processBlock()`, optimize only that identified work and keep the server-authoritative model intact. Record the conclusion in code comments only where the mitigation would otherwise be non-obvious, and in `08.2-03-SUMMARY.md` capture the JamTaba-informed diagnosis explicitly so the phase cannot close on vague CPU claims.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" &gt;nul &amp;&amp; .\build-vs\tests\famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics],[plugin_mixer_controls],[plugin_room_controls_ui],[plugin_mixer_ui]"'</automated>
  </verify>
  <done>The code-driven periodic spike contributors are measurably reduced or clearly isolated, and the mitigation keeps the existing rehearsal behavior green.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_pre_layout_cpu_diagnostics],[plugin_mixer_controls],[plugin_room_controls_ui],[plugin_mixer_ui]"'`

Phase-level verification criteria (applies after all Phase 08.2 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation on the same repro machine confirms whether the every-four-beat spikes are materially reduced or now clearly machine-specific residual risk.
</verification>

<success_criteria>
- All tasks completed
- P082-SC1 periodic spike work is diagnosed with deterministic evidence rather than hand-waving
- The JamTaba-informed comparison is explicitly recorded in the plan summary, not left as implicit researcher context
- The chosen mitigation reduces the code-driven spike source or narrows the remaining issue to documented machine-sensitive residual risk
- Room and mixer regressions stay green after the CPU pass
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-03-SUMMARY.md`
</output>
