---
phase: 08.3.3-stem-capture-export
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/StemCaptureWriter.h
  - src/audio/StemCaptureWriter.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/stem_capture_writer_tests.cpp
  - tests/integration/plugin_stem_capture_tests.cpp
autonomous: true
requirements:
  - REC-01
gap_closure: true
user_setup: []
must_haves:
  truths:
    - Local and remote sources in voice mode never create or extend a stem file.
    - If a source returns from voice mode to interval mode while recording stays armed, interval capture resumes only from the next interval boundary.
    - The already-passing interval export and mixed-room voice playback behavior remain intact while voice export is removed.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Processor-owned voice exclusion and boundary-delayed interval resume logic for stem capture.
      contains: captureLocalVoiceStem
    - path: src/audio/StemCaptureWriter.h
      provides: Writer command surface for retiring or rotating a single source without ending the whole recording run.
      contains: StemCaptureWriter
    - path: tests/integration/plugin_stem_capture_tests.cpp
      provides: Regression coverage for local and remote voice exclusion plus interval-only resume.
      min_lines: 260
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/audio/StemCaptureWriter.cpp
      via: Voice transitions must retire only the affected source writer so interval capture resumes in a new interval file instead of appending across a voice gap.
      pattern: captureLocalVoiceStem|captureRemoteStem|submitFrame
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_stem_capture_tests.cpp
      via: The processor must prove both local and remote voice-mode sources are skipped and that interval capture restarts only after the next boundary when mode returns.
      pattern: voice|boundary|interval
---

<objective>
Close the remaining product bug from manual validation: voice chat must not be recorded or exported at all.

Purpose: Narrow the gap fix to voice-mode exclusion and interval-only resume semantics without reopening the already-passing interval export workflow.
Output: Per-source voice exclusion, boundary-delayed interval resume after returning from voice mode, and focused automation proving both local and remote behavior.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-CONTEXT.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-RESEARCH.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-03-SUMMARY.md
@docs/validation/phase8.3.3-stem-capture-matrix.md
@src/audio/StemCaptureWriter.h
@src/audio/StemCaptureWriter.cpp
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/unit/stem_capture_writer_tests.cpp
@tests/integration/plugin_stem_capture_tests.cpp

<interfaces>
From src/audio/StemCaptureWriter.h:
```cpp
void beginSession(const SessionConfig& config);
void pauseSession();
void endSession();
void submitFrame(const Frame& frame);
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
void armStemCaptureForNextBoundary();
void requestStemCaptureStop();
void handleStemCaptureBoundary();
void captureLocalVoiceStem(const juce::AudioBuffer<float>& voiceChunk);
void captureRemoteStem(const std::string& sourceId,
                       const juce::AudioBuffer<float>& audio,
                       bool voiceMode);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for voice exclusion and interval-only resume</name>
  <files>tests/unit/stem_capture_writer_tests.cpp, tests/integration/plugin_stem_capture_tests.cpp</files>
  <behavior>
    - Test 1: With recording armed, switching the local source into voice mode produces no voice stem file, and switching back to interval mode resumes capture only after the next interval boundary.
    - Test 2: A remote source flagged for voice mode produces no exported stem file while voice playback still works, and returning that source to interval mode resumes export only from the next boundary.
    - Test 3: Retiring a single source writer creates a fresh collision-suffixed interval file on resume instead of appending across the voice gap.
  </behavior>
  <action>Add focused RED coverage for the validated product rule from `STATE.md` and `08.3.3-VERIFICATION.md`: voice chat must not be written at all, even though the original `08.3.3-CONTEXT.md` explored voice export. Keep the tests narrow to the remaining gap. Reuse the existing stem-capture harness and temp-directory assertions rather than introducing new end-to-end scaffolding.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture]"</automated>
  </verify>
  <done>Automation fails specifically on the current voice-export behavior and the missing interval-only resume semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement per-source voice exclusion and boundary-delayed interval resume</name>
  <files>src/audio/StemCaptureWriter.h, src/audio/StemCaptureWriter.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/unit/stem_capture_writer_tests.cpp, tests/integration/plugin_stem_capture_tests.cpp</files>
  <behavior>
    - Test 1: Local voice upload continues to use the live voice transport path, but the stem path writes nothing while local voice mode is active.
    - Test 2: Remote voice chunks remain audible in mixed rooms, but no remote voice stem file is created or extended.
    - Test 3: When a source returns to interval mode, the next interval boundary starts a fresh interval file in the same session folder.
  </behavior>
  <action>Change the stem-capture policy so voice-mode sources are excluded entirely from export. Keep the live voice transport path unchanged. Add only the writer/runtime seams required to retire the affected source when it enters voice mode, then reopen a fresh interval file on the next eligible boundary if that source later returns to interval mode. Do not replan interval alignment, reconnect continuity, folder naming, or a broader voice-mode redesign.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>No voice stem files are produced for local or remote voice-mode sources, and interval capture resumes only at the next boundary after a source returns to interval mode.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_voice_mode_transport]"`
</verification>

<success_criteria>
- REC-01 no longer exports voice chat stems for any source
- Interval stems still start on valid boundaries and resume only on the next boundary after voice-mode exit
- The fix stays inside the existing stem-capture and mixed-room transport seams without broadening scope
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.3-stem-capture-export/08.3.3-04-SUMMARY.md`
</output>
