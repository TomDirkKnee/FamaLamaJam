# 08.3.3-03 Summary

## Outcome

Wave 3 automation is complete and the debug VST3 is rebuilt. The phase is now sitting at the manual export-validation checkpoint.

## Verification

Automated:
- `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_rehearsal_ui_flow]"`
- `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_state_roundtrip],[plugin_voice_mode_transport]"`
- `cmake --build build-vs-2026 --target famalamajam_plugin_VST3 --config Debug -- /m:1 /v:minimal`

Results:
- focused phase gate: pass, `148` assertions across `15` test cases
- broader compatibility slice: pass, `198` assertions across `12` test cases

## Ready / not ready

Ready:
- boundary-aligned stem writer foundation
- record/folder UI flow
- reconnect continuity in the same session folder
- rebuilt debug VST3 for Ableton validation

Still needed:
- real Ableton export/import pass using `docs/validation/phase8.3.3-stem-capture-matrix.md`
