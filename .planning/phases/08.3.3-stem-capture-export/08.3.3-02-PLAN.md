---
phase: 08.3.3-stem-capture-export
plan: "02"
type: execute
wave: 2
depends_on:
  - 08.3.3-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - src/infra/state/SessionSettingsSerializer.h
  - src/infra/state/SessionSettingsSerializer.cpp
  - tests/integration/plugin_stem_capture_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
autonomous: true
requirements:
  - REC-01
user_setup: []
must_haves:
  truths:
    - Stem recording is controlled by one global UI toggle plus folder picker.
    - Recording cannot arm without a chosen writable folder and shows inline feedback when blocked.
    - Disconnect/reconnect keeps the same session folder while boundary-gating any new files after reconnect.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Record stems UI, folder picker flow, and inline blocked-state feedback.
      contains: Record stems
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Recording arm/disarm, reconnect continuity, and serialized export settings.
      contains: stem
    - path: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      provides: UI workflow coverage for folder-required arming and session continuity.
      min_lines: 260
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: The editor must drive processor-owned record state and folder selection rather than inventing editor-only recording truth.
      pattern: Record stems|folder
---

<objective>
Finish the user-facing recording workflow and the persisted export settings in the current single-page UI.

Purpose: Make stem export usable in practice by giving the user an obvious arm/folder workflow, preserving session continuity across reconnect, and keeping the implementation inside the existing processor/editor ownership model.
Output: Global record UI, folder-required validation, persisted export settings, reconnect continuity, and end-to-end integration coverage.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED UI and state coverage for the global stem-record workflow</name>
  <files>tests/integration/plugin_stem_capture_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <action>Add focused integration/UI coverage for a single global `Record stems` control plus folder picker, blocked arming when no folder is chosen, reconnect continuity in the same session folder, and stop-on-next-boundary semantics when recording is turned off mid-session.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow]"</automated>
  </verify>
  <done>Tests fail specifically on the missing record workflow, folder validation, and reconnect continuity behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the recording controls, folder picker flow, and serialized export settings</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, src/infra/state/SessionSettingsSerializer.h, src/infra/state/SessionSettingsSerializer.cpp, tests/integration/plugin_stem_capture_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <action>Wire a global recording toggle and folder selection flow into the existing compact session UI, keep the blocked state explicit until a folder exists, persist the chosen export state through the normal plugin/session state path, and ensure reconnect reuses the same session folder while new files restart from the next eligible boundary.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>Focused tests pass and the stem workflow is usable without introducing a second settings surface or violating the boundary-aligned export rules.</done>
</task>

</tasks>

<verification>
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow]"`
- [ ] `cmake --build build-vs-2026 --target famalamajam_plugin_VST3 --config Debug -- /m:1 /v:minimal`
</verification>

<output>
After completion, create `.planning/phases/08.3.3-stem-capture-export/08.3.3-02-SUMMARY.md`
</output>
