---
phase: 08.3.3-stem-capture-export
plan: "06"
type: execute
wave: 3
depends_on:
  - 08.3.3-04
  - 08.3.3-05
files_modified:
  - docs/validation/phase8.3.3-stem-capture-matrix.md
  - .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
  - .planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
autonomous: false
requirements:
  - REC-01
gap_closure: true
user_setup:
  - Load the rebuilt Debug VST3 in Ableton and verify that voice-mode sources produce no stem files, interval capture resumes on the next boundary after returning from voice mode, and the explicit new-run action creates a fresh session folder without reconnecting.
must_haves:
  truths:
    - Phase 08.3.3 closes only if Ableton validation confirms voice chat is excluded from export and the explicit new-run control works without reconnecting.
    - The previously passing interval alignment and reconnect continuity evidence is carried forward unless the focused rerun finds a real regression.
    - Validation and verification artifacts agree on the exact gap-closure outcome.
  artifacts:
    - path: docs/validation/phase8.3.3-stem-capture-matrix.md
      provides: Focused manual matrix for voice exclusion, interval-only resume, and explicit new-run rollover.
      contains: P8.3.3-REC-03
    - path: .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
      provides: Updated automated and manual evidence after the gap closure.
      contains: build-vs-2026
    - path: .planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
      provides: Validation strategy aligned with the superseded voice-export assumption removed.
      contains: Gap-closure addendum
  key_links:
    - from: docs/validation/phase8.3.3-stem-capture-matrix.md
      to: .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
      via: The focused manual matrix rows for voice exclusion and new-run rollover must match the verification summary exactly.
      pattern: P8\\.3\\.3-REC-03|P8\\.3\\.3-REC-05|voice|new run
    - from: .planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
      to: .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
      via: The validation strategy must point at the same build-vs-2026 gate and focused manual checks that the closeout document records.
      pattern: build-vs-2026|voice|new run
---

<objective>
Re-run the focused Phase 08.3.3 gate on `build-vs-2026`, then record the real Ableton result honestly and close the phase only if the remaining gaps are actually gone.

Purpose: The gap work is only complete if voice export is gone in the real host workflow and the explicit new-run control behaves predictably without reconnecting.
Output: Refreshed automated evidence, a rebuilt Debug VST3, an updated manual matrix, and aligned validation and verification notes.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-03-SUMMARY.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-04-PLAN.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-05-PLAN.md
@docs/validation/phase8.3.3-stem-capture-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the focused build-vs-2026 artifacts and refresh automated evidence</name>
  <files>.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md</files>
  <action>Rebuild `famalamajam_tests` on `build-vs-2026`, rerun the focused stem-capture slice that covers voice exclusion, interval-only resume, the explicit new-run workflow, and mixed-room voice compatibility, then rebuild the Debug VST3. Update `08.3.3-VERIFICATION.md` with exact commands, assertion counts, and a note that the already-passing interval export rows are being carried forward unless the focused rerun exposes a new regression.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_rehearsal_ui_flow],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>The focused automation is green on `build-vs-2026` and the Debug VST3 is rebuilt for the final Ableton checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify voice exclusion and explicit new-run rollover in Ableton</name>
  <action>Use the rebuilt Debug VST3 to re-run only the remaining host checks: voice-mode sources must not create stem files, interval capture must resume on the next boundary after returning from voice mode, and the explicit new-run action must create a fresh session folder without reconnecting.</action>
  <what-built>Voice-excluded stem capture, boundary-delayed interval resume after voice mode, and an explicit in-session new-run rollover control.</what-built>
  <how-to-verify>
    1. Load the rebuilt Debug `FamaLamaJam.vst3` in Ableton using the existing `build-vs-2026` artifact.
    2. Arm stem capture into a writable base folder and confirm the existing interval-stem behavior still aligns as before.
    3. Put the local channel into voice mode, confirm no new stem file is created for that source while voice audio still works, then switch back to interval mode and confirm a fresh interval file begins only on the next interval boundary.
    4. Repeat the same rule for a remote voice-mode source if available: no voice stem file while the peer is in voice mode, then interval capture resumes only after the next boundary when that peer returns to interval mode.
    5. Trigger the explicit new-run action during an active recording run and confirm a new session folder is created without disconnecting, while the previous run folder and files remain intact.
    6. If any of the already-passing interval or reconnect checks regress, note the exact row; otherwise carry them forward as pass evidence.
  </how-to-verify>
  <resume-signal>Type "approved" if the focused Ableton rerun passes, or describe the exact failing row and observed behavior.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update the focused matrix, validation addendum, and verification closeout</name>
  <files>docs/validation/phase8.3.3-stem-capture-matrix.md, .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md, .planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md</files>
  <action>Update the manual matrix, verification summary, and validation strategy to match the checkpoint result exactly. If the rerun is approved, flip the voice-export row to pass, record the interval-only resume behavior, record the explicit new-run rollover as pass, and remove the superseded voice-export expectation from the validation notes. If the rerun still fails, capture the precise remaining behavior without reopening unrelated stem, mixer, or Phase 9 scope.</action>
  <verify>
    <automated>rg -n "P8\\.3\\.3-REC-03|P8\\.3\\.3-REC-05|voice|new run|build-vs-2026" docs/validation/phase8.3.3-stem-capture-matrix.md .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md .planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md</automated>
  </verify>
  <done>The focused closeout artifacts all describe the same real outcome, with no lingering reference to voice stems as a desired phase behavior.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_rehearsal_ui_flow],[plugin_voice_mode_transport]"`
- [ ] `cmake --build build-vs-2026 --target famalamajam_plugin_VST3 --config Debug -- /m:1 /v:minimal`
- [ ] Human checkpoint confirms voice chat is excluded from export and the explicit new-run action works without reconnecting, or the docs record the exact remaining blocker.
</verification>

<success_criteria>
- REC-01 closes with voice chat excluded from export and an explicit new-run rollover path verified in Ableton
- Previously passing interval export evidence is preserved unless the focused rerun finds a regression
- Validation and verification artifacts no longer describe voice stems as a desired output
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.3-stem-capture-export/08.3.3-06-SUMMARY.md`
</output>
