---
phase: 08.3.3-stem-capture-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/CMakeLists.txt
  - tests/unit/stem_capture_writer_tests.cpp
  - tests/integration/plugin_stem_capture_tests.cpp
  - src/app/session/StemCaptureSettings.h
  - src/app/session/StemCaptureSettings.cpp
  - src/audio/StemCaptureWriter.h
  - src/audio/StemCaptureWriter.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
autonomous: true
requirements:
  - REC-01
user_setup: []
must_haves:
  truths:
    - Stem export remains usable during live sessions without destabilizing the realtime audio path.
    - Supported source types produce dry stems that begin on valid interval boundaries without fake silence backfill.
    - Session folders and filenames are predictable enough for later DAW import, including BPM/BPI-plus-timestamp folders and voice-only mode suffixes.
  artifacts:
    - path: src/audio/StemCaptureWriter.h
      provides: Background writer/session lifecycle seam for WAV export.
      contains: StemCaptureWriter
    - path: tests/unit/stem_capture_writer_tests.cpp
      provides: Temp-folder and file naming coverage for the writer service.
      min_lines: 140
    - path: tests/integration/plugin_stem_capture_tests.cpp
      provides: Boundary start/stop and dry capture integration coverage.
      min_lines: 180
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/audio/StemCaptureWriter.cpp
      via: The processor must enqueue immutable dry-audio work items to the writer instead of writing files inline.
      pattern: submit|enqueue|boundary
---

<objective>
Build the realtime-safe stem-capture foundation and the first boundary-aware export seams.

Purpose: Establish a writer architecture that can safely own session folders, WAV files, and source-specific exports without destabilizing the live plugin.
Output: RED tests, a background writer service, and processor-side capture gating for dry local/remote audio paths.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED writer and processor coverage for boundary-aligned stem capture</name>
  <files>tests/CMakeLists.txt, tests/unit/stem_capture_writer_tests.cpp, tests/integration/plugin_stem_capture_tests.cpp</files>
  <action>Create focused unit and integration coverage that describes the full export contract: WAV-only dry stems, one session folder per recording run named from BPM/BPI plus timestamp, username-plus-channel naming with suffix collision handling, voice-only filename suffixes, source-native channel format preservation, start on the next interval boundary, no silence backfill for late-joining remote users, and stop on the next interval boundary when recording is disabled. Use temp folders for filesystem assertions and keep tests deterministic.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture]"</automated>
  </verify>
  <done>Focused tests compile and fail on the missing writer service / boundary-capture behavior instead of relying on manual-only validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the background writer service and processor capture gating</name>
  <files>src/app/session/StemCaptureSettings.h, src/app/session/StemCaptureSettings.cpp, src/audio/StemCaptureWriter.h, src/audio/StemCaptureWriter.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/unit/stem_capture_writer_tests.cpp, tests/integration/plugin_stem_capture_tests.cpp</files>
  <action>Add a dedicated stem writer service that owns session folder creation, per-source WAV writers, filename collision handling, voice-only mode suffixing, source-native channel format preservation, and clean file finalization. In the processor, introduce boundary-aware recording state and enqueue the dry audio taps for local interval, local voice, remote interval, and remote voice at the correct pre-mix seams without doing file IO on the realtime thread, while ensuring late-joining remote sources start at their first actual audio with no silence backfill.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>Focused tests pass and the codebase has a safe writer seam plus dry processor enqueue points for every supported export path.</done>
</task>

</tasks>

<verification>
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture]"`
</verification>

<output>
After completion, create `.planning/phases/08.3.3-stem-capture-export/08.3.3-01-SUMMARY.md`
</output>
