---
phase: 08.3.3-stem-capture-export
plan: "03"
type: execute
wave: 3
depends_on:
  - 08.3.3-02
files_modified:
  - .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
  - docs/validation/phase8.3.3-stem-capture-matrix.md
autonomous: false
requirements:
  - REC-01
user_setup:
  - Ableton manual export smoke that records at least one local interval contribution and one remote contribution, then checks the exported files in a DAW or file browser
must_haves:
  truths:
    - The phase closes with both focused automation and a real export/import manual checkpoint.
    - The final write-up says clearly whether stems are practically importable and aligned enough for later DAW mixing.
  artifacts:
    - path: docs/validation/phase8.3.3-stem-capture-matrix.md
      provides: Manual stem-export validation checklist.
      contains: P8.3.3-REC-01
    - path: .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
      provides: Honest closeout and final evidence for REC-01.
      contains: REC-01
---

<objective>
Finish the phase with focused automation, a rebuilt plugin, and a real export usability decision.

Purpose: Prove that the stem export path is not just implemented, but practically useful for later DAW work.
Output: Green focused gate, rebuilt debug VST3, short Ableton export matrix, and final verification note.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Run the focused automated gate and rebuild the debug VST3</name>
  <files>.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md</files>
  <action>Run the focused `build-vs-2026` gate for the new stem capture suites and any touched workflow coverage, then rebuild the debug VST3 artifact and record exact commands, counts, and caveats in the verification note.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_rehearsal_ui_flow]"</automated>
  </verify>
  <done>Automation and the debug artifact are ready for human export validation.</done>
</task>

<task type="manual">
  <name>Task 2: Run the Ableton stem-export matrix and record the outcome honestly</name>
  <files>docs/validation/phase8.3.3-stem-capture-matrix.md, .planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md</files>
  <action>Create a short manual matrix covering folder-required arming, local interval export, local voice export, remote export naming, reconnect continuity, stop-on-next-boundary closeout, late-joining remote behavior without silence backfill, BPM/BPI-plus-timestamp session folders, voice-only filename suffixing, and preservation of native source channel format. Record the results exactly as observed and state whether the stems are practically usable for later DAW import/editing.</action>
  <verify>
    <manual>`docs/validation/phase8.3.3-stem-capture-matrix.md` contains pass/fail results for folder arming, boundary start, late-join no-silence behavior, session-folder naming, voice-only suffixing, native channel format, reconnect continuity, and `08.3.3-VERIFICATION.md` states clearly whether the exports are practically DAW-usable.</manual>
  </verify>
  <done>The phase closes with an explicit usability decision backed by automated and manual evidence.</done>
</task>

</tasks>

<verification>
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[stem_capture],[plugin_stem_capture],[plugin_rehearsal_ui_flow]"`
- [ ] `cmake --build build-vs-2026 --target famalamajam_plugin_VST3 --config Debug -- /m:1 /v:minimal`
</verification>

<output>
After completion, create:
- `.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md`
- `.planning/phases/08.3.3-stem-capture-export/08.3.3-03-SUMMARY.md`
- `docs/validation/phase8.3.3-stem-capture-matrix.md`
</output>
