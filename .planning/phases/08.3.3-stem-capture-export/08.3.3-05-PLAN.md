---
phase: 08.3.3-stem-capture-export
plan: "05"
type: execute
wave: 2
depends_on:
  - 08.3.3-04
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_stem_capture_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
autonomous: true
requirements:
  - REC-01
gap_closure: true
user_setup: []
must_haves:
  truths:
    - The user has an explicit way to start a brand-new recording run and session folder without disconnecting.
    - Triggering the new-run action preserves the chosen base export folder but rolls into a fresh BPM/BPI-plus-timestamp session folder on the next interval boundary.
    - Earlier stems from the previous run remain intact, and the control fits inside the existing compact session UI.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Explicit new-run control and inline status copy in the existing stem-capture UI.
      contains: Record stems
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Processor-owned session-folder rollover state that starts a new run without reconnecting.
      contains: handleStemCaptureBoundary
    - path: tests/integration/plugin_stem_capture_tests.cpp
      provides: Integration coverage proving a fresh session folder is created without mutating the prior run.
      min_lines: 320
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: The explicit new-run action must queue processor-owned rollover state instead of inventing editor-only folder truth.
      pattern: Record stems|new run|boundary
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_stem_capture_tests.cpp
      via: Rolling to a new run must be proven by two different session folders under the same chosen base directory with earlier files preserved.
      pattern: session folder|new run|boundary
---

<objective>
Add the follow-up UX improvement requested during manual validation: an explicit way to start a fresh recording run folder without relying on reconnect.

Purpose: Keep the workflow practical during long rehearsals by letting the user roll into a new export run intentionally while preserving the current compact UI and folder-selection model.
Output: A processor-owned new-run rollover action, a small UI control for it, and deterministic automation proving the new session folder behavior.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-CONTEXT.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VALIDATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-VERIFICATION.md
@.planning/phases/08.3.3-stem-capture-export/08.3.3-04-PLAN.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_stem_capture_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
app::session::StemCaptureSettings getStemCaptureSettings() const;
bool applyStemCaptureSettings(app::session::StemCaptureSettings candidate);
void armStemCaptureForNextBoundary();
void requestStemCaptureStop();
void handleStemCaptureBoundary();
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
struct StemCaptureUiState
{
    bool enabled { false };
    std::string outputDirectory;
    std::string statusText;
};

bool applyStemCaptureSettingsFromUi(bool enabled);
void launchStemCaptureFolderChooser();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for explicit new-run rollover without reconnect</name>
  <files>tests/integration/plugin_stem_capture_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: The editor exposes an explicit new-run action only when stem capture has a valid base folder and recording is enabled.
    - Test 2: Clicking the action keeps recording armed but creates a fresh session folder on the next interval boundary without disconnecting.
    - Test 3: Files from the previous run remain in the old folder and are not overwritten when the new run starts.
  </behavior>
  <action>Add narrow UI and integration coverage for an explicit `New stem run` style action in the existing session settings area. Keep the tests centered on folder rollover behavior, not on redesigning the surrounding layout. Reuse the temp-folder stem harness so the assertions are deterministic on `build-vs-2026`.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow]"</automated>
  </verify>
  <done>The new-run workflow is specified in failing automation before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement processor-owned session-folder rollover and the explicit UI control</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_stem_capture_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: The new-run action queues a rollover for the next interval boundary rather than cutting files mid-interval.
    - Test 2: The rollover reuses the chosen base export directory but generates a fresh session folder with the normal naming rules.
    - Test 3: Reconnect continuity still keeps one session folder when no explicit new-run action was requested.
  </behavior>
  <action>Add a processor-owned rollover request that closes the current run on the next interval boundary and immediately starts a new session folder in the same base directory while keeping stem capture enabled. Surface that through a small explicit control beside the existing `Record stems` flow, with short status text that makes the pending rollover obvious. Do not add a second folder chooser, do not persist a one-shot rollover request into saved state, and do not pull in broader Phase 9 layout work.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>The user can intentionally roll into a brand-new recording run folder without disconnecting, and the existing reconnect behavior still works unchanged when the new action is not used.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_stem_capture],[plugin_rehearsal_ui_flow],[plugin_voice_mode_transport]"`
</verification>

<success_criteria>
- REC-01 includes an explicit in-session way to start a fresh recording run folder
- The new-run action rolls over only at a boundary and preserves earlier stems
- The current compact session UI stays intact without spilling into mixer or Phase 9 redesign work
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.3-stem-capture-export/08.3.3-05-SUMMARY.md`
</output>
