---
phase: 08.3.4-host-multi-io-routing-research
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - tests/integration/plugin_host_multi_io_routing_tests.cpp
  - docs/validation/phase8.3.4-host-multi-io-routing-matrix.md
  - .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md
autonomous: true
requirements:
  - ROUTE-01
user_setup: []
must_haves:
  truths:
    - The fixed-bus proof is locked to one extra stereo input bus and one extra stereo output pair before any broader routing work begins.
    - Automation defines the expected main-versus-aux bus isolation and one-selected-source routed-output behavior before processor code changes.
    - The Ableton checkpoint has explicit pass, fail, and fallback rows instead of relying on ad-hoc host notes.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Minimal proof-facing routing contract and test seam for the fixed-bus experiment.
      contains: HostRoutingProof
    - path: tests/integration/plugin_host_multi_io_routing_tests.cpp
      provides: RED integration coverage for bus declaration, layout acceptance, aux-input isolation, and routed-output isolation.
      min_lines: 180
    - path: docs/validation/phase8.3.4-host-multi-io-routing-matrix.md
      provides: Manual Ableton matrix with explicit ergonomics and fallback decision rows.
      contains: P8.3.4-ROUTE-01
  key_links:
    - from: tests/integration/plugin_host_multi_io_routing_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.h
      via: The tests must compile against an explicit proof contract instead of hidden debug globals or editor-only controls.
      pattern: HostRoutingProof|plugin_host_multi_io_routing
    - from: docs/validation/phase8.3.4-host-multi-io-routing-matrix.md
      to: .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md
      via: The manual matrix and validation strategy must describe the same fixed-bus scope and fallback rule.
      pattern: Ableton|fallback|per-user|fixed-bus
---

<objective>
Define the fixed-bus proof contract before implementation: one extra stereo aux input bus, one extra stereo aux output pair, focused RED coverage, and a manual Ableton matrix that can answer the go or no-go decision honestly.

Purpose: Keep the next execution phase narrow and evidence-led instead of drifting into a dynamic routing project without host proof.
Output: A proof-facing routing contract, failing integration coverage, and a manual validation matrix with explicit fallback criteria.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-CONTEXT.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-RESEARCH.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/net/FramedSocketTransport.cpp
@tests/integration/plugin_host_sync_assist_tests.cpp
@tests/integration/plugin_voice_mode_transport_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
void prepareToPlay(double sampleRate, int samplesPerBlock) override;
bool isBusesLayoutSupported(const BusesLayout& layouts) const override;
void processBlock(juce::AudioBuffer<float>& buffer, juce::MidiBuffer& midiMessages) override;
[[nodiscard]] TransportUiState getTransportUiState() const noexcept;
[[nodiscard]] bool getMixerStripSnapshot(const std::string& sourceId, MixerStripSnapshot& snapshot) const;
void registerRemoteSourceForTesting(const std::string& sourceId,
                                    const std::string& userName,
                                    const std::string& channelName,
                                    std::uint8_t channelFlags);
```

From src/net/FramedSocketTransport.cpp:
```cpp
info.groupId = normalizeUserIdentity(info.userName);
info.sourceId = info.groupId + "#" + std::to_string(static_cast<unsigned>(channelIndex));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the proof contract and write RED routing coverage</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, tests/integration/plugin_host_multi_io_routing_tests.cpp</files>
  <behavior>
    - Test 1: The processor declares stereo main input/output plus exactly one optional stereo aux input and one optional stereo aux output pair.
    - Test 2: `isBusesLayoutSupported()` accepts only stereo main I/O and stereo optional proof buses, rejecting mono or mismatched layouts.
    - Test 3: The proof contract exposes enough test state to distinguish main-path activity, aux-input activity, and the selected routed remote source without introducing editor UI.
  </behavior>
  <action>Add the smallest processor-facing contract needed for this proof: a fixed-bus routing proof snapshot and a test/debug route-selection seam in `FamaLamaJamAudioProcessor.h`, then add RED integration coverage in a new `plugin_host_multi_io_routing_tests.cpp`. Reuse the existing processor-plus-server integration style from `plugin_host_sync_assist_tests.cpp` and `plugin_voice_mode_transport_tests.cpp`. Do not add dynamic bus counts, do not plan around plugin-owned Ableton track enumeration, and do not start a larger routing matrix UI here.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"</automated>
  </verify>
  <done>The host-routing proof tests compile against explicit contracts and fail on the current single-bus implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the Ableton proof matrix and fallback decision criteria</name>
  <files>docs/validation/phase8.3.4-host-multi-io-routing-matrix.md, .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md</files>
  <action>Create the manual Ableton matrix before implementation with explicit rows for sidechain-style extra-input exposure, routed extra-output exposure, signal isolation, and an ergonomics verdict. Record the fallback rule directly in the matrix and `08.3.4-VALIDATION.md`: if Ableton routing is too awkward or too hidden for normal use, document the exact blocker and recommend either per-user output routing or no follow-on implementation phase instead of expanding the fixed-bus scope by assumption.</action>
  <verify>
    <automated>rg -n "P8\\.3\\.4-ROUTE-01|P8\\.3\\.4-ROUTE-02|fallback|per-user|Ableton" docs/validation/phase8.3.4-host-multi-io-routing-matrix.md .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md</automated>
  </verify>
  <done>The phase has an explicit manual matrix, decision row, and fallback rule before any processor routing code is changed.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"`
- [ ] `rg -n "P8\\.3\\.4-ROUTE-01|P8\\.3\\.4-ROUTE-02|fallback|per-user|Ableton" docs/validation/phase8.3.4-host-multi-io-routing-matrix.md .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md`
</verification>

<success_criteria>
- ROUTE-01 has executable failing coverage for the one-extra-input and one-extra-output proof target
- The fixed-bus scope and fallback decision rule are documented before implementation starts
- No task assumes dynamic bus growth, track enumeration inside FLJ, or a broader mixer redesign
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-01-SUMMARY.md`
</output>
