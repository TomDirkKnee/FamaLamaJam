---
phase: 08.3.4-host-multi-io-routing-research
plan: "01"
subsystem: testing
tags: [juce, vst3, ableton, catch2, routing]
requires:
  - phase: 08.3.3-stem-capture-export
    provides: Processor-owned remote-source identity and validation patterns reused for routing-proof coverage.
provides:
  - Header-only fixed-bus routing proof seam for task-focused RED coverage
  - Failing integration suite for proof bus declaration, layout gating, aux-input isolation, and routed-output isolation
  - Manual Ableton routing matrix with explicit fallback decisions before implementation
affects: [08.3.4-02, 08.3.4-03, phase9-layout, ROUTE-01]
tech-stack:
  added: []
  patterns: [header-only proof seam, focused RED integration gate, matrix-driven host verdict]
key-files:
  created: [tests/integration/plugin_host_multi_io_routing_tests.cpp, docs/validation/phase8.3.4-host-multi-io-routing-matrix.md, .planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md]
  modified: [src/plugin/FamaLamaJamAudioProcessor.h, tests/CMakeLists.txt]
key-decisions:
  - "Keep plan 01 at RED only: expose a proof seam and failing fixed-bus coverage without starting processor routing implementation early."
  - "Lock the Ableton verdict to explicit fallback outcomes: per-user output routing or no follow-on phase if host ergonomics are weak."
patterns-established:
  - "Routing research starts with one fixed aux input bus and one fixed routed output pair before any dynamic-bus discussion."
  - "Host-feasibility work must carry a manual Ableton matrix with explicit blocker and fallback rows before implementation expands scope."
requirements-completed: [ROUTE-01]
duration: 21min
completed: 2026-04-04
---

# Phase 08.3.4 Plan 01: Host Multi-I/O Routing Research Summary

**Fixed-bus routing proof seam, failing host-routing integration coverage, and an Ableton matrix that locks fallback decisions before implementation**

## Performance

- **Duration:** 21 min
- **Started:** 2026-04-04T19:40:00Z
- **Completed:** 2026-04-04T20:01:25Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added a header-only `HostRoutingProof` seam plus explicit proof bus names so routing tests compile against a stable processor contract instead of hidden globals.
- Added a new `[plugin_host_multi_io_routing]` RED suite that builds and fails on the current one-bus processor for the right reasons: missing proof buses and permissive aux-layout acceptance.
- Created the manual Ableton proof matrix and updated the phase validation contract so the follow-on decision must land on fixed-bus approval, per-user fallback, or no follow-on phase.

## Task Commits

1. **Task 1: Define the proof contract and write RED routing coverage** - `c797244` (`test`)
2. **Task 2: Lock the Ableton proof matrix and fallback decision criteria** - `e86360d` (`docs`)

## Files Created/Modified

- `src/plugin/FamaLamaJamAudioProcessor.h` - Adds fixed-bus proof constants and the header-only routing proof seam.
- `tests/integration/plugin_host_multi_io_routing_tests.cpp` - Adds failing proof-bus, layout, aux-input, and routed-output integration coverage.
- `tests/CMakeLists.txt` - Registers the new focused routing suite in the test target.
- `docs/validation/phase8.3.4-host-multi-io-routing-matrix.md` - Defines the manual Ableton proof matrix and fallback rule.
- `.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-VALIDATION.md` - Locks the fixed-bus validation contract and host-verdict rules before implementation.

## Decisions Made

- Keep plan 01 strictly at the proof-contract and RED-coverage level; the actual fixed-bus processor work stays in plan 02.
- Require the Ableton check to end in an explicit verdict, not open-ended notes: approve fixed-bus follow-on, fall back to per-user routing, or stop the follow-on entirely.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Issue] Registered the new integration file in the test target**
- **Found during:** Task 1 (Define the proof contract and write RED routing coverage)
- **Issue:** `tests/CMakeLists.txt` enumerates integration sources manually, so the new RED suite would not build without a target entry.
- **Fix:** Added `tests/integration/plugin_host_multi_io_routing_tests.cpp` to the `famalamajam_tests` target.
- **Files modified:** `tests/CMakeLists.txt`
- **Verification:** `cmake --build build-vs-2026 --target famalamajam_tests --config Debug`
- **Committed in:** `c797244`

**2. [Rule 1 - Bug] Corrected the new constant-buffer helper so the future proof tests inject non-zero audio**
- **Found during:** Task 1 (Define the proof contract and write RED routing coverage)
- **Issue:** The helper cleared the buffer and then multiplied silence, which would have masked routed-output behavior later.
- **Fix:** Rewrote the helper to write explicit sample values into every channel.
- **Files modified:** `tests/integration/plugin_host_multi_io_routing_tests.cpp`
- **Verification:** Rebuilt and reran `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing]"`
- **Committed in:** `c797244`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes were necessary to keep the RED suite buildable and trustworthy. No scope creep.

## Issues Encountered

- A transient `.git/index.lock` blocked staging briefly. No live Git process remained, and a retry succeeded without altering repository contents.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 now has a concrete proof contract, a focused RED gate, and a locked manual matrix to implement against.
- ROUTE-01 is not host-validated yet; plan 03 still needs the fixed-bus implementation rerun plus the real Ableton verdict before the phase can recommend broader routing work.

## Self-Check: PASSED

- Found `.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-01-SUMMARY.md`
- Found commit `c797244`
- Found commit `e86360d`
