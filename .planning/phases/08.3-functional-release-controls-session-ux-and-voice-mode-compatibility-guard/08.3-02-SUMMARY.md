# Plan 08.3-02 Summary

Private-room recall is now persisted and reusable across saved state.

Completed:
- Remembered server entries now store `host`, `port`, `username`, and `password`.
- Successful connections are written back into processor-owned remembered history.
- The editor restores remembered credentials through the combined picker and reuses a masked password placeholder instead of discarding the saved secret.
- State roundtrip and discovery UI coverage were extended so remembered entries preserve username/password correctly.

Result:
- Users can select a remembered private room and reconnect without manually retyping the password, while the UI still shows a masked password field.
