---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "03"
type: execute
wave: 2
depends_on:
  - 08.3-01
  - 08.3-02
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_transmit_controls_tests.cpp
  - tests/integration/plugin_transmit_controls_ui_tests.cpp
  - tests/integration/plugin_experimental_transport_tests.cpp
autonomous: true
requirements:
  - P083-03
  - P083-04
user_setup: []
must_haves:
  truths:
    - The local strip exposes a three-state transmit control owned by the processor, not an editor-only toggle.
    - Connect and reconnect enter amber warmup first, then transition to active transmit automatically when healthy.
    - Clicking during amber cancels warmup immediately, and switching to red stops only upload while the rest of the session keeps working.
    - Turning transmit back on re-enters the warmup path instead of silently jumping straight to green.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Processor-owned transmit enum, warmup transitions, and upload-only gating in the runtime path.
      contains: processBlock
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Local-strip transmit control, wording, and color-state projection inside the mixer strip.
      contains: rebuildMixerStripWidgets
    - path: tests/integration/plugin_transmit_controls_tests.cpp
      provides: Processor coverage for transmit lifecycle, cancel, reconnect warmup, and upload-only gating.
      min_lines: 120
    - path: tests/integration/plugin_transmit_controls_ui_tests.cpp
      provides: UI coverage that the control lives on the local strip with red or amber or green behavior.
      min_lines: 120
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_transmit_controls_tests.cpp
      via: Boundary-driven warmup and upload-only gating must be proven from the processor seam rather than from widget text alone.
      pattern: transmit|warmup|submitInput
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: The editor must render the local-strip transmit state from processor snapshots instead of driving its own timers.
      pattern: MixerStripSnapshot|MixerStripState
---

<objective>
Add the local-strip transmit lifecycle and enforce upload-only gating without breaking connection, timing, or room behavior.

Purpose: Turn the existing implicit warmup mechanic into an explicit release-ready transmit control that musicians can understand, while keeping the server session and local monitoring semantics intact.
Output: Processor-owned red or amber or green transmit state, a local-strip transmit control, reconnect warmup replay, and upload-only gating covered by integration tests.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-CONTEXT.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-RESEARCH.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-VALIDATION.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-01-SUMMARY.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-02-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_transmit_controls_tests.cpp
@tests/integration/plugin_transmit_controls_ui_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
bool requestConnect();
bool requestDisconnect();
void processBlock(juce::AudioBuffer<float>& buffer, juce::MidiBuffer& midiMessages) override;
int transmitWarmupIntervalsRemaining_ { 0 };
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
using MixerStripsGetter = std::function<std::vector<MixerStripState>()>;
using MixerStripSetter = std::function<bool(const std::string&, float, float, bool)>;
void refreshMixerStrips();
void rebuildMixerStripWidgets(const std::vector<MixerStripState>& visibleStrips);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish RED transmit lifecycle assertions against the new Wave 0 suites</name>
  <files>tests/integration/plugin_transmit_controls_tests.cpp, tests/integration/plugin_transmit_controls_ui_tests.cpp, tests/integration/plugin_experimental_transport_tests.cpp</files>
  <behavior>
    - Test 1: After a healthy connect, transmit shows amber warmup first and only later turns green.
    - Test 2: Clicking during amber cancels warmup immediately and leaves upload off.
    - Test 3: Turning transmit red suppresses outbound upload but leaves remote receive, room state, sync, local monitor, and metronome alive.
    - Test 4: After disconnect or reconnect, transmit restarts at amber rather than restoring straight to green.
  </behavior>
  <action>Complete the failing assertions in the new transmit suites so they describe the locked behavior precisely at both processor and UI levels. Reuse `plugin_experimental_transport_tests.cpp` where it already proves upload or receive behavior, but keep the new dedicated transmit suites as the primary spec. Do not implement runtime logic in this task.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_experimental_transport]"'</automated>
  </verify>
  <done>The transmit suites fail only on the missing lifecycle behavior and clearly specify upload-only gating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement processor-owned transmit state and local-strip upload gating</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_transmit_controls_tests.cpp, tests/integration/plugin_transmit_controls_ui_tests.cpp, tests/integration/plugin_experimental_transport_tests.cpp</files>
  <behavior>
    - Test 1: The processor owns explicit transmit state transitions and projects them into the local strip UI.
    - Test 2: Warmup stays boundary-driven, but the user sees amber state and can cancel it explicitly.
    - Test 3: Upload submission is gated independently from receive, chat, sync, monitor, and metronome behavior.
    - Test 4: Re-enabling transmit or reconnecting replays warmup rather than reviving the previous green state blindly.
  </behavior>
  <action>Replace the implicit `transmitWarmupIntervalsRemaining_`-only model with an explicit processor-owned transmit enum that later plans can reuse in strip snapshots. Add a local-strip control that reflects red or amber or green status and lets the user cancel amber or active transmit directly from the local strip. Gate only the outbound upload submission or interval-enqueue path in `processBlock()`, leaving connection lifecycle, inbound frames, room events, sync, monitor, and metronome untouched.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_experimental_transport]"'</automated>
  </verify>
  <done>The local strip now exposes the locked transmit lifecycle, and turning transmit off only stops upload.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_experimental_transport]"'`

Phase-level verification criteria (applies after all Phase 08.3 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure -L phase-quick'`
</verification>

<success_criteria>
- P083-03 and P083-04 are automated end to end
- The transmit control lives on the local strip and follows the locked red or amber or green lifecycle
- Upload-only gating is proven without regressing the rest of the connected session
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-03-SUMMARY.md`
</output>
