---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - src/infra/state/RememberedServerStore.h
  - src/infra/state/RememberedServerStore.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/remembered_server_store_tests.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
autonomous: true
requirements:
  - P083-01
  - P083-02
gap_closure: true
user_setup: []
must_haves:
  truths:
    - A successful private-room connection survives removing the plugin and opening a brand-new instance in the same OS user profile.
    - Remembered private entries still appear at the top of the combined picker and expose host, username, and reusable masked password recall.
    - Wrapped plugin state and the new app-level remembered-server store do not erase each other when both exist.
    - Failed connects and draft-only edits still do not pollute remembered private history.
  artifacts:
    - path: src/infra/state/RememberedServerStore.h
      provides: Small persisted-store contract for globally remembered private servers.
      contains: RememberedServerStore
    - path: src/infra/state/RememberedServerStore.cpp
      provides: File-backed load and save implementation under user app data for remembered private entries.
      contains: makeRememberedServerStore
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Processor startup, restore, and successful-connect wiring that keeps global and project-scoped remembered history aligned.
      contains: rememberSuccessfulServer
    - path: tests/unit/remembered_server_store_tests.cpp
      provides: Deterministic coverage for missing, corrupt, and round-trip remembered-server store behavior.
      min_lines: 80
    - path: tests/integration/plugin_server_discovery_tests.cpp
      provides: Cross-instance global recall coverage through the existing discovery surface.
      min_lines: 180
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/infra/state/RememberedServerStore.cpp
      via: Processor constructor and successful-connect remember flow must load or persist global remembered entries without inventing editor-owned state.
      pattern: makeRememberedServerStore|rememberSuccessfulServer
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_server_discovery_tests.cpp
      via: Brand-new-instance recall must be proven by creating a second processor instance that reads the same persisted store.
      pattern: getServerDiscoveryUiState|requestConnect
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_state_roundtrip_tests.cpp
      via: Wrapped project state restore must merge forward into the new global store instead of regressing the existing saved-project path.
      pattern: getStateInformation|setStateInformation
---

<objective>
Close the real Phase 08.3 recall gap by adding a small app-level remembered-private-server store and wiring it into the existing processor-owned discovery flow.

Purpose: The current credential-aware recall works only inside plugin or project state. This plan adds the narrow missing persistence surface the user actually needs, while preserving the combined picker, remembered-first ordering, and masked password reuse UX already validated in 08.3-02.
Output: A file-backed remembered-server store, processor merge logic for global plus project-scoped history, and deterministic automation that proves cross-instance recall.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-CONTEXT.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-RESEARCH.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-VERIFICATION.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-02-SUMMARY.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-05-SUMMARY.md
@src/app/session/ServerDiscoveryModel.h
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_server_discovery_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp

<interfaces>
From src/app/session/ServerDiscoveryModel.h:
```cpp
struct RememberedServerEntry
{
    std::string host;
    std::uint16_t port { 0 };
    std::string username;
    std::string password;

    bool operator==(const RememberedServerEntry& other) const = default;
};

void rememberSuccessfulServer(std::vector<RememberedServerEntry>& history,
                              RememberedServerEntry entry,
                              std::size_t maxEntries = 12);
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct ServerDiscoveryUiState
{
    bool fetchInProgress { false };
    bool hasStalePublicData { false };
    std::string statusText;
    std::vector<ServerDiscoveryEntry> combinedEntries;
};

explicit FamaLamaJamAudioProcessor(bool enableLiveTransport = false,
                                   bool enableExperimentalStreaming = false);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a deterministic app-level remembered-server store seam</name>
  <files>CMakeLists.txt, tests/CMakeLists.txt, src/infra/state/RememberedServerStore.h, src/infra/state/RememberedServerStore.cpp, tests/unit/remembered_server_store_tests.cpp</files>
  <behavior>
    - Test 1: Saving remembered private entries writes a stable on-disk representation and loading a second store instance from the same path returns the same entries.
    - Test 2: Missing store files load as an empty history instead of throwing or fabricating data.
    - Test 3: Corrupt store content falls back to an empty history safely.
  </behavior>
  <action>Create a small `RememberedServerStore` seam in `src/infra/state` and a JUCE file-backed implementation that persists remembered private entries under the current user app-data directory. Use a versioned `ValueTree` or XML payload, not a full credential-manager redesign. Add a deterministic temp-path unit test so the store can be proven without touching the real user path, and update CMake sources only as needed for the new store or tests.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[remembered_server_store]"'</automated>
  </verify>
  <done>A file-backed remembered-server store exists, handles empty or corrupt files safely, and is covered by deterministic automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire global remembered history into processor startup, successful-connect save, and project-state restore</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_server_discovery_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp</files>
  <behavior>
    - Test 1: After a successful private-room connection, destroying the processor and creating a brand-new processor instance under the same test store still shows the remembered private entry in `getServerDiscoveryUiState()`.
    - Test 2: The restored entry still carries username and password data so the existing masked-password picker flow can reuse it unchanged.
    - Test 3: `setStateInformation()` merges remembered entries from wrapped project state with the global store instead of dropping either source.
    - Test 4: Failed connects still do not write remembered private entries into the global store.
  </behavior>
  <action>Inject the new remembered-server store into the processor with a default file-backed implementation and a testing seam. Load global remembered history during processor construction, persist back to the store only after successful connects, and merge wrapped project-state entries through the existing `rememberSuccessfulServer()` helper so endpoint de-duplication and newest-successful-entry rules stay intact. Do not move recall behavior into the editor and do not broaden this into a full credential-manager or Phase 9 UX redesign.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[remembered_server_store],[plugin_server_discovery],[plugin_state_roundtrip]"'</automated>
  </verify>
  <done>Remembered private-room recall survives brand-new plugin instances via the new app-level store while the existing project-state restore path stays compatible.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[remembered_server_store],[plugin_server_discovery],[plugin_state_roundtrip]"'`
- [ ] Existing masked-password and combined-picker behavior remains covered indirectly by the unchanged 08.3 UI tests in the follow-up closeout plan.
</verification>

<success_criteria>
- P083-01 and P083-02 are satisfied for both saved-project restore and brand-new-instance recall
- Successful private-room connections persist into a small app-level store without creating a second UX or credential-management system
- Global and wrapped remembered history merge safely instead of competing
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-06-SUMMARY.md`
</output>
