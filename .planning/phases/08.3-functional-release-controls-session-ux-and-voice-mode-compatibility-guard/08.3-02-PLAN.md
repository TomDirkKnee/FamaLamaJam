---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/ServerDiscoveryModel.h
  - src/app/session/ServerDiscoveryModel.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
  - tests/integration/plugin_server_discovery_ui_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
autonomous: true
requirements:
  - P083-01
  - P083-02
user_setup: []
must_haves:
  truths:
    - Successful private-room connections are remembered with enough data to reconnect later without retyping credentials.
    - Remembered private entries still lead the combined picker and preserve endpoint-stable selection behavior.
    - Recalled passwords are shown masked in the UI but the real saved password is reused on connect until the user actually edits it.
    - Failed private-room attempts do not pollute remembered history.
  artifacts:
    - path: src/app/session/ServerDiscoveryModel.h
      provides: Remembered private-entry schema that can hold endpoint and credential identity.
      contains: RememberedServerEntry
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Wrapped plugin-state persistence and successful-connect remember logic for private entries.
      contains: rememberSuccessfulServer
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Combined-picker recall flow with masked password provenance and endpoint-stable autofill.
      contains: selectedServerDiscoveryEndpointKey_
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: State roundtrip coverage for remembered private credentials across plugin save or restore.
      min_lines: 200
    - path: tests/integration/plugin_server_discovery_ui_tests.cpp
      provides: UI coverage that recalled private entries keep selection stability and masked password reuse.
      min_lines: 180
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/ServerDiscoveryModel.cpp
      via: Successful-auth remember logic should still flow through the shared discovery helper rather than ad hoc history mutation in the editor.
      pattern: rememberSuccessfulServer
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_server_discovery_ui_tests.cpp
      via: Endpoint-keyed picker restore must remain stable while recalled credentials autofill the connection draft.
      pattern: selectedServerDiscoveryEndpointKey_|password
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_rehearsal_ui_flow_tests.cpp
      via: Masked password display must not overwrite the actual connection password when Connect is pressed.
      pattern: makeDraftFromUi|loadFromSettings
---

<objective>
Implement remembered private-server credential persistence and recall inside the existing discovery and state architecture.

Purpose: Make private-room reconnects practical without inventing a second credential store, while preserving the endpoint-stable picker and the successful-connect-only history rule already established in earlier phases.
Output: Credential-aware remembered private entries in wrapped plugin state, masked-password picker recall, and regression coverage for successful-only remember behavior and reconnect reuse.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-CONTEXT.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-RESEARCH.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-VALIDATION.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-02-SUMMARY.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-01-SUMMARY.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-05-SUMMARY.md
@src/app/session/ServerDiscoveryModel.h
@src/app/session/ServerDiscoveryModel.cpp
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp

<interfaces>
From src/app/session/ServerDiscoveryModel.h:
```cpp
struct RememberedServerEntry
{
    std::string host;
    std::uint16_t port { 0 };

    bool operator==(const RememberedServerEntry& other) const = default;
};

void rememberSuccessfulServer(std::vector<RememberedServerEntry>& history,
                              RememberedServerEntry entry,
                              std::size_t maxEntries = 12);
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
struct ServerDiscoveryEntry
{
    Source source { Source::Remembered };
    std::string label;
    std::string host;
    std::uint16_t port { 0 };
    int connectedUsers { -1 };
    bool stale { false };
};

bool selectServerDiscoveryEntryForTesting(int index);
[[nodiscard]] juce::String getSelectedServerDiscoveryLabelForTesting() const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend remembered private-entry persistence and successful-connect history coverage</name>
  <files>src/app/session/ServerDiscoveryModel.h, src/app/session/ServerDiscoveryModel.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp</files>
  <behavior>
    - Test 1: A successful authenticated private-room connection persists host, port, username, and password into the wrapped plugin state.
    - Test 2: Restore reloads remembered private entries without reviving a live session, and older state blobs still default missing private fields safely.
    - Test 3: Failed connection attempts or draft edits do not create remembered private entries.
    - Test 4: History de-duplication still respects endpoint identity while preserving the newest successful private entry.
  </behavior>
  <action>Expand `RememberedServerEntry` and the plugin-state read or write path so remembered private entries carry endpoint plus credential identity. Reuse the existing wrapped plugin `ValueTree` schema and `rememberSuccessfulServer()` helper instead of creating a second store. Preserve backward compatibility for older blobs by defaulting missing private fields safely, and keep the successful-connect-only remember rule intact.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_state_roundtrip],[plugin_server_discovery]"'</automated>
  </verify>
  <done>Remembered private credentials survive plugin save or restore and only successful private-room connections enter history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement masked picker recall and password provenance handling in the editor</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_server_discovery_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: Selecting a remembered private entry fills host, port, and username while showing a masked password placeholder in the field.
    - Test 2: Pressing Connect without editing the masked field reuses the real stored password, not the literal mask text.
    - Test 3: Editing the password field replaces the stored password provenance so future connects use the new value.
    - Test 4: Endpoint-stable picker selection survives reconnect or reorder scenarios while remembered entries still appear ahead of public entries.
  </behavior>
  <action>Implement picker recall by extending the current combined discovery/history flow, not by bypassing it. Track recalled-password provenance separately from the visible password text so `makeDraftFromUi()` never serializes or reconnects with the mask placeholder. Keep remembered entries first, preserve endpoint-key selection restore from Phase 08.1, and only let successful connects refresh remembered private data.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui],[plugin_rehearsal_ui_flow]"'</automated>
  </verify>
  <done>Remembered private entries are recallable through the combined picker with masked password reuse and no risk of saving the mask string as the real password.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_state_roundtrip],[plugin_server_discovery],[plugin_server_discovery_ui],[plugin_rehearsal_ui_flow]"'`

Phase-level verification criteria (applies after all Phase 08.3 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure -L phase-quick'`
</verification>

<success_criteria>
- P083-01 and P083-02 are automated end to end
- Successful private-room connects update remembered history with credential-aware entries
- Picker recall stays endpoint-stable and masked-password reuse never writes the mask as the real password
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-02-SUMMARY.md`
</output>
