---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "04"
type: execute
wave: 3
depends_on:
  - 08.3-03
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_mixer_controls_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/integration/plugin_voice_mode_guard_tests.cpp
  - tests/integration/plugin_room_controls_ui_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
  - tests/integration/plugin_transport_ui_sync_tests.cpp
autonomous: true
requirements:
  - P083-05
  - P083-06
  - P083-07
user_setup: []
must_haves:
  truths:
    - Solo uses standard multi-solo behavior without rewriting persisted mute choices.
    - Unsupported remote voice-mode channels are never mixed as interval audio and are called out visibly in strip or session UI.
    - Diagnostics and server settings can collapse, but the page stays single-screen and the interval bar remains a fixed full-width footer.
    - Host sync assist stays visible inline with connect or disconnect, the metronome keeps a compact adjacent volume control, and the room feed wraps and scrolls without reintroducing unconditional churn.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Multi-solo audibility rules and early unsupported-voice gating before decode or mix.
      contains: submitInboundEncoded
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Single-page session polish with collapsible sections, inline host-sync assist, compact metronome control, wrapped room feed, and fixed footer interval bar.
      contains: resized
    - path: tests/integration/plugin_mixer_controls_tests.cpp
      provides: Multi-solo regression coverage without mute-state corruption.
      min_lines: 240
    - path: tests/integration/plugin_voice_mode_guard_tests.cpp
      provides: User-facing unsupported voice-mode coverage plus no-playback assertions.
      min_lines: 120
    - path: tests/integration/plugin_transport_ui_sync_tests.cpp
      provides: Inline host-sync assist placement and disabled-reason coverage in the refreshed transport row.
      min_lines: 220
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_voice_mode_guard_tests.cpp
      via: Voice-mode detection must gate before decode or mix and still leave visible unsupported state in exported strip snapshots.
      pattern: channelFlags|submitInboundEncoded
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_mixer_controls_tests.cpp
      via: Solo must be derived audibility logic, not a destructive rewrite of `muted`.
      pattern: muted|solo
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_room_controls_ui_tests.cpp
      via: Wrapped feed rows and collapsible sections must preserve readable single-page room workflow without tabbing or Phase 9 redesign.
      pattern: roomFeed|diagnostics|server
---

<objective>
Finish the remaining mixer and session UX polish while adding the voice-mode compatibility guard.

Purpose: Deliver the rest of the release-critical single-page UX pass without drifting into Phase 9, and make unsupported remote voice-mode peers explicit instead of glitchy or silent.
Output: Multi-solo support, unsupported voice-mode guard, collapsible diagnostics and server settings, inline host-sync assist, compact metronome volume, wrapped room feed, and a fixed footer interval bar with automated coverage.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-CONTEXT.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-RESEARCH.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-VALIDATION.md
@.planning/phases/07-chat-room-control-commands/07-02-SUMMARY.md
@.planning/phases/08.1-server-discovery-polish-jamtaba-parity-check/08.1-02-SUMMARY.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-02-SUMMARY.md
@.planning/phases/08.2-pre-layout-cpu-mixer-ui-and-auth-hardening/08.2-03-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_mixer_controls_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/integration/plugin_voice_mode_guard_tests.cpp
@tests/integration/plugin_room_controls_ui_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp
@tests/integration/plugin_transport_ui_sync_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
struct HostSyncAssistUiState
{
    bool armable { false };
    bool armed { false };
    bool blocked { false };
    HostSyncAssistBlockReason blockReason { HostSyncAssistBlockReason::None };
    HostSyncAssistFailureReason failureReason { HostSyncAssistFailureReason::None };
};

struct RoomUiState
{
    bool connected { false };
    std::string topic;
    std::vector<RoomFeedEntry> visibleFeed;
    RoomVoteUiState bpmVote;
    RoomVoteUiState bpiVote;
};
```

From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct RemoteReceiveDiagnosticRuntime
{
    std::size_t lastEncodedBytes { 0 };
    int lastDecodedSamples { 0 };
    double lastDecodedSampleRate { 0.0 };
    std::uint8_t channelFlags { 0 };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for solo, unsupported voice-mode surfacing, and session UX polish</name>
  <files>tests/integration/plugin_mixer_controls_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_voice_mode_guard_tests.cpp, tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp</files>
  <behavior>
    - Test 1: Soloed strips mute unsoloed strips while any solo is active, and releasing solo restores the user's underlying mute choices.
    - Test 2: Unsupported remote voice-mode peers remain visible with explicit unsupported wording and never appear as normal interval playback.
    - Test 3: Diagnostics and server settings collapse cleanly, host sync assist stays inline with connect or disconnect, and the interval bar remains a fixed footer.
    - Test 4: The metronome gets a compact adjacent volume control, room feed rows wrap to width, and vote controls stay compact numeric plus button interactions.
  </behavior>
  <action>Update the existing mixer, room, rehearsal, transport, and voice-guard suites so they precisely describe the locked Phase 08.3 UX and compatibility behavior. Reuse narrow editor helpers where possible; do not regress into fragile child-index layout assertions or ask for Phase 9 visuals. This task should end with failing tests that isolate missing behavior across solo, voice guard, and the single-page polish pass.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls],[plugin_mixer_ui],[plugin_voice_mode_guard],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_transport_ui_sync]"'</automated>
  </verify>
  <done>The missing solo, voice guard, and session-polish behavior is represented by targeted failing integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-solo behavior and the unsupported voice-mode guard</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_mixer_controls_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_voice_mode_guard_tests.cpp</files>
  <behavior>
    - Test 1: Solo is stored separately from mute and only changes effective audibility while a solo is active.
    - Test 2: Unsupported voice-mode frames are rejected before decode or mix, so they do not consume the normal interval playback path.
    - Test 3: Remote strips and status copy surface an explicit unsupported state rather than hiding the issue in diagnostics only.
    - Test 4: Existing mixer restore semantics stay intact after solo and voice-guard state are added.
  </behavior>
  <action>Implement non-destructive solo in the processor's strip runtime and exported snapshots so effective audibility becomes `!muted && (no solo active || strip.soloed)`. Keep persisted mute state untouched. Add a centralized voice-mode predicate from `channelFlags` and gate flagged remote channels before decode submission or mix. Export enough strip or status metadata for the editor to surface the unsupported state visibly in normal workflow.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls],[plugin_mixer_ui],[plugin_voice_mode_guard],[plugin_state_roundtrip]"'</automated>
  </verify>
  <done>Solo and unsupported voice-mode handling are processor-owned, automated, and visible to the editor without corrupting existing mute or restore behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply the single-page session UX polish without starting Phase 9</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_room_controls_ui_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp, tests/integration/plugin_mixer_ui_tests.cpp</files>
  <behavior>
    - Test 1: Server settings and diagnostics are collapsible, with server summary copy still visible when collapsed.
    - Test 2: Host sync assist sits inline with Connect and Disconnect, stays visible when unavailable, and shows clear disabled reasons.
    - Test 3: The metronome toggle gains a compact adjacent volume knob, the room feed wraps within its viewport, and the progress bar stays fixed as a full-width footer.
    - Test 4: The page remains the same single-page editor with compact session copy rather than a tabbed or broader Phase 9 layout rewrite.
  </behavior>
  <action>Reshuffle the current single-page layout only as far as the locked release polish requires. Keep diagnostics hidden by default and collapsible, add a collapsible `Server settings` section with compact server plus status summary, move host sync assist inline with connect controls while keeping it always visible and disabled with reasons when needed, add a lightweight metronome volume control next to the toggle, wrap room-feed text to width with scroll, and anchor the interval bar as a fixed full-width footer. Preserve the 08.2 dirty-checking pattern so wrapped feed or collapse behavior does not reintroduce unconditional UI churn.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls],[plugin_mixer_ui],[plugin_voice_mode_guard],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_transport_ui_sync]"'</automated>
  </verify>
  <done>The current editor feels like a release-polish pass: compact, explicit, and single-page, with no Phase 9 redesign bleed.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_mixer_controls],[plugin_mixer_ui],[plugin_voice_mode_guard],[plugin_room_controls_ui],[plugin_rehearsal_ui_flow],[plugin_transport_ui_sync]"'`

Phase-level verification criteria (applies after all Phase 08.3 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure -L phase-quick'`
</verification>

<success_criteria>
- P083-05, P083-06, and P083-07 are automated end to end
- Solo works without mutating persisted mute state
- Unsupported remote voice-mode channels are explicit and never mixed as interval audio
- The session UI is tighter and more usable without starting the broader Phase 9 redesign
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-04-SUMMARY.md`
</output>
