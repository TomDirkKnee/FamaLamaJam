---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/CMakeLists.txt
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_transmit_controls_tests.cpp
  - tests/integration/plugin_transmit_controls_ui_tests.cpp
  - tests/integration/plugin_voice_mode_guard_tests.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
autonomous: true
requirements:
  - P083-03
  - P083-07
  - P083-08
user_setup: []
must_haves:
  truths:
    - The phase has automated seams for transmit lifecycle and unsupported voice-mode behavior before later plans modify the runtime.
    - Remote voice-flag simulation is possible in the existing MiniNinjamServer harness instead of staying manual-only.
    - Processor and editor strip contracts explicitly reserve room for transmit, solo, and unsupported-voice state so later plans do not invent editor-local behavior.
  artifacts:
    - path: tests/integration/support/MiniNinjamServer.h
      provides: Voice-flag-aware remote peer harness support for automated transport scenarios.
      contains: channelFlags
    - path: tests/integration/plugin_transmit_controls_tests.cpp
      provides: Processor lifecycle coverage for red or amber or green transmit behavior and upload-only gating.
      min_lines: 120
    - path: tests/integration/plugin_transmit_controls_ui_tests.cpp
      provides: Local-strip transmit UI coverage for warmup, cancel, and reconnect behavior.
      min_lines: 120
    - path: tests/integration/plugin_voice_mode_guard_tests.cpp
      provides: Transport and UI coverage for unsupported remote voice-mode channels.
      min_lines: 120
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Processor-side transmit or solo or voice-guard contract additions for downstream implementation.
      contains: MixerStripSnapshot
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.h
      provides: Editor-side strip-state and testing-hook contracts for downstream implementation.
      contains: MixerStripState
  key_links:
    - from: tests/integration/support/MiniNinjamServer.h
      to: tests/integration/plugin_voice_mode_guard_tests.cpp
      via: The harness must be able to advertise per-peer voice flags so the guard is proven automatically.
      pattern: RemotePeer|channelFlags
    - from: src/plugin/FamaLamaJamAudioProcessor.h
      to: src/plugin/FamaLamaJamAudioProcessorEditor.h
      via: New transmit or solo or unsupported-voice strip fields must be projected through processor-owned snapshots, not invented in the editor.
      pattern: MixerStripSnapshot|MixerStripState
---

<objective>
Create the missing Wave 0 test and contract seams for Phase 08.3 before runtime behavior is changed.

Purpose: Give downstream plans stable automated coverage and explicit processor/editor interfaces for transmit lifecycle and voice-mode compatibility work instead of relying on manual-only validation or editor-local state.
Output: New transmit and voice-guard integration suites, MiniNinjamServer voice-flag support, and explicit strip-state contracts in the processor/editor headers.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-CONTEXT.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-RESEARCH.md
@.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@tests/integration/support/MiniNinjamServer.h

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct MixerStripMixState
{
    float gainDb { 0.0f };
    float pan { 0.0f };
    bool muted { false };
};

struct MixerStripSnapshot
{
    MixerStripDescriptor descriptor;
    MixerStripMixState mix;
    MixerStripMeter meter;
};
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
struct MixerStripState
{
    MixerStripKind kind { MixerStripKind::RemoteDelayed };
    std::string sourceId;
    std::string displayName;
    std::string subtitle;
    float gainDb { 0.0f };
    float pan { 0.0f };
    bool muted { false };
    bool active { false };
    bool visible { false };
};
```

From tests/integration/support/MiniNinjamServer.h:
```cpp
struct RemotePeer
{
    std::string username { "peer" };
    std::uint8_t channelIndex { 0 };
    int startAfterUploadBegins { 0 };
};

void setRemotePeers(std::vector<RemotePeer> peers);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 harness support and failing transmit or voice guard suites</name>
  <files>tests/CMakeLists.txt, tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_transmit_controls_tests.cpp, tests/integration/plugin_transmit_controls_ui_tests.cpp, tests/integration/plugin_voice_mode_guard_tests.cpp</files>
  <behavior>
    - Test 1: The harness can advertise a remote peer with arbitrary `channelFlags`, including the current voice-mode value used by diagnostics.
    - Test 2: A processor-focused suite describes red or amber or green transmit lifecycle, cancelable warmup, reconnect warmup replay, and upload-only gating.
    - Test 3: A UI-focused suite describes local-strip transmit wording and control behavior without putting the control back in the transport row.
    - Test 4: A voice-guard suite describes that unsupported voice-mode channels never mix as interval audio and surface explicit unsupported state in normal UI snapshots.
  </behavior>
  <action>Extend `MiniNinjamServer::RemotePeer` and its `sendUserInfo()` path so tests can simulate remote voice flags deterministically. Register the new suites in `tests/CMakeLists.txt`. Create the transmit and voice-guard integration files with failing expectations that match the locked Phase 08.3 behavior; do not implement runtime behavior yet. Keep the tests targeted to the existing processor/editor seams and avoid building a second fake server or manual-only placeholders.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_voice_mode_guard]"'</automated>
  </verify>
  <done>The new suites compile, run, and fail on the missing Phase 08.3 behavior instead of being absent from the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Define processor and editor contracts for transmit, solo, and unsupported voice strips</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessorEditor.h, tests/integration/plugin_transmit_controls_tests.cpp, tests/integration/plugin_transmit_controls_ui_tests.cpp, tests/integration/plugin_voice_mode_guard_tests.cpp</files>
  <action>Update the processor and editor headers to reserve explicit strip-state fields and testing hooks for later plans: processor-owned transmit state, non-destructive solo state, and unsupported-voice visibility. Keep this task contract-only: headers, enums, structs, callback signatures, and test helper declarations may change, but runtime logic should remain for later plans. The goal is interface-first execution so later plans implement against known contracts instead of rediscovering them.</action>
  <verify>
    <automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_voice_mode_guard]"'</automated>
  </verify>
  <done>The headers expose stable contracts for downstream transmit and voice-guard work, and the new suites still fail on runtime behavior rather than on missing types or hooks.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transmit_controls],[plugin_transmit_controls_ui],[plugin_voice_mode_guard]"'`

Phase-level verification criteria (applies after all Phase 08.3 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure -L phase-quick'`
</verification>

<success_criteria>
- Wave 0 gaps for transmit and voice guard are replaced with real automated suites
- `MiniNinjamServer` can simulate remote voice flags for later plans
- Processor/editor strip-state contracts exist before implementation begins
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-01-SUMMARY.md`
</output>
