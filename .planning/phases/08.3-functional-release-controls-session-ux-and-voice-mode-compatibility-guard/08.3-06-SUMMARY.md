---
phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
plan: "06"
subsystem: infra
tags: [juce, persistence, xml, vst3, integration-tests]
requires:
  - phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard
    provides: Credential-aware remembered-server entries in plugin/project state from 08.3-02.
provides:
  - App-data remembered private server persistence outside project/plugin state
  - Processor merge logic that keeps global and wrapped remembered history aligned
  - Automated cross-instance recall coverage for remembered private rooms
affects: [server-discovery, session-state, release-controls, phase-08.3-closeout]
tech-stack:
  added: [JUCE XML ValueTree persistence]
  patterns: [global remembered-server store injected into the processor with deterministic temp-file test seams]
key-files:
  created:
    - src/infra/state/RememberedServerStore.h
    - src/infra/state/RememberedServerStore.cpp
    - tests/unit/remembered_server_store_tests.cpp
  modified:
    - CMakeLists.txt
    - tests/CMakeLists.txt
    - src/plugin/FamaLamaJamAudioProcessor.h
    - src/plugin/FamaLamaJamAudioProcessor.cpp
    - tests/integration/plugin_server_discovery_tests.cpp
    - tests/integration/plugin_state_roundtrip_tests.cpp
key-decisions:
  - "Persist remembered private rooms in a small JUCE XML store under user app data instead of broadening this into a credential-manager redesign."
  - "Keep processor-owned discovery/history as the only integration point: load the global store on construction, persist only after successful connects, and merge wrapped project history during restore."
patterns-established:
  - "Global-plus-project recall: merge wrapped remembered entries into the app-level store with rememberSuccessfulServer() so endpoint de-duplication and newest-successful ordering stay consistent."
  - "Verification seam: use explicit temp-file store factories so cross-instance persistence is testable without touching the real profile path."
requirements-completed: [P083-01, P083-02]
duration: 13min
completed: 2026-03-21
---

# Phase 08.3 Plan 06: Remembered private-room recall via app-data store and processor merge wiring

**App-data remembered private server persistence with cross-instance recall, wrapped-state merge safety, and deterministic store plus processor regression coverage**

## Performance

- **Duration:** 13 min
- **Started:** 2026-03-21T19:40:26Z
- **Completed:** 2026-03-21T19:53:19Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Added a small file-backed `RememberedServerStore` seam that persists remembered private-room entries under user app data and safely treats missing or corrupt files as empty history.
- Wired the processor to load global remembered history on startup, persist only successful private-room connections, and merge wrapped project-state history back into the global store without dropping either source.
- Extended automation to prove cross-instance recall, credential reuse data preservation, failed-connect non-persistence, and wrapped-state/global-store merge behavior.

## Task Commits

1. **Task 1: Add a deterministic app-level remembered-server store seam** - `4d717e2` (`feat`)
2. **Task 2: Wire global remembered history into processor startup, successful-connect save, and project-state restore** - not committed atomically because the required files already carried overlapping uncommitted Phase 08.3 edits unrelated to this plan, and staging the full files would have disturbed that work.

## Files Created/Modified
- `src/infra/state/RememberedServerStore.h` - Small persisted-store interface plus default and temp-file factory seams.
- `src/infra/state/RememberedServerStore.cpp` - XML-backed load/save implementation rooted in the current user app-data profile.
- `tests/unit/remembered_server_store_tests.cpp` - Stable-save, missing-file, and corrupt-payload coverage for the store seam.
- `src/plugin/FamaLamaJamAudioProcessor.h` - Processor injection seam and store persistence helpers.
- `src/plugin/FamaLamaJamAudioProcessor.cpp` - Startup load, successful-connect save, and wrapped-state/global merge wiring.
- `tests/integration/plugin_server_discovery_tests.cpp` - Cross-instance recall and failed-connect non-persistence coverage.
- `tests/integration/plugin_state_roundtrip_tests.cpp` - Wrapped-state/global merge coverage for remembered private-room history.

## Decisions Made
- Used the new store only as a narrow processor dependency so the existing combined picker and masked-password reuse UX stay unchanged.
- Persisted merged remembered history back into the global store during `setStateInformation()` so saved-project restores do not erase global recall and global recall does not erase wrapped state.
- Used the pre-existing `build-vs-2026` Visual Studio generator for verification because the repository’s `build-vs` NMake tree currently fails on this machine with `U1076 name too long` while rebuilding `famalamajam_tests`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Switched verification from `build-vs` NMake to the existing `build-vs-2026` Visual Studio tree**
- **Found during:** Task 1 verification
- **Issue:** `cmake --build build-vs --target famalamajam_tests --config Debug` failed locally with `nmake` error `U1076: name too long`, which blocked the required test pass.
- **Fix:** Reused the repository’s existing `build-vs-2026` Visual Studio generator tree for build and test verification.
- **Files modified:** None
- **Verification:** `cmake --build build-vs-2026 --target famalamajam_tests --config Debug` and the required Catch2 tag slices passed.
- **Committed in:** none

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Verification path changed, but scope stayed the same and the required coverage ran successfully.

## Issues Encountered
- Atomic staging for Task 2 was not safe because the processor and integration test files already contained overlapping uncommitted Phase 08.3 edits; the implementation remains in the working tree and verified, but only Task 1 was committed cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Global remembered private-room recall is implemented and covered by deterministic automation.
- The remaining follow-up is repository hygiene: either separate or commit the pre-existing uncommitted Phase 08.3 changes in the overlapping processor/test files before attempting a clean metadata closeout commit for this plan.

## Self-Check: PASSED

- Verified `.planning/phases/08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard/08.3-06-SUMMARY.md` exists.
- Verified task commit `4d717e2` exists in repository history.
- `STATE.md` and `ROADMAP.md` were updated in the working tree to reflect plan 06 completion and plan 07 as the next step.
- No metadata docs commit was created because the planning files and Task 2 source files already carried overlapping uncommitted work, and forcing a commit would have disturbed that state.

---
*Phase: 08.3-functional-release-controls-session-ux-and-voice-mode-compatibility-guard*
*Completed: 2026-03-21*
