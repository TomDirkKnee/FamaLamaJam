---
phase: 01-plugin-foundation-session-configuration
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - src/app/session/SessionSettingsController.h
  - src/app/session/SessionSettingsController.cpp
  - tests/integration/plugin_apply_flow_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - docs/validation/phase1-ableton-matrix.md
autonomous: true
requirements:
  - SESS-01
  - HOST-02
user_setup: []
must_haves:
  truths:
    - User can edit endpoint, port, username, and channel defaults in the plugin UI, and Apply commits only valid values.
    - Ableton project save/restore round-trips core session settings into a fresh plugin instance with no manual re-entry.
    - State restore never auto-connects and invalid persisted payloads reset to safe defaults with warning status.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Settings form with draft editing and explicit Apply workflow.
      contains: Apply
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: getStateInformation/setStateInformation wiring through serializer and disconnected restore behavior.
      contains: setStateInformation
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: Processor-level save/restore, duplicate-instance, and invalid-state integration checks.
      min_lines: 30
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: src/app/session/SessionSettingsController.cpp
      via: Apply action submits draft settings for validation and commit.
      pattern: apply
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/infra/state/SessionSettingsSerializer.cpp
      via: getStateInformation/setStateInformation serialize and restore persisted settings.
      pattern: StateInformation
    - from: tests/integration/plugin_state_roundtrip_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Integration tests instantiate processor and assert round-trip behavior.
      pattern: FamaLamaJamAudioProcessor
---

<objective>
Deliver the user-facing settings flow and host-state integration for Phase 1.

Purpose: Fulfill SESS-01 and HOST-02 with a non-blocking configuration path and deterministic Ableton persistence behavior.
Output: Settings editor and apply controller, plugin state hook integration, integration tests, and a phase manual validation matrix.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-foundation-session-configuration/01-CONTEXT.md
@.planning/phases/01-plugin-foundation-session-configuration/01-RESEARCH.md
@.planning/phases/01-plugin-foundation-session-configuration/01-VALIDATION.md
@.planning/phases/01-plugin-foundation-session-configuration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement settings editor draft/apply flow on the non-RT control path</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, src/app/session/SessionSettingsController.h, src/app/session/SessionSettingsController.cpp</files>
  <action>Build a minimal JUCE settings UI for endpoint host/port, username, and channel defaults. Keep edits in a draft model and route Apply through SessionSettingsController validation/commit on the message thread so no per-keystroke or RT-thread state mutation occurs.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_apply_flow</automated></verify>
  <done>Valid Apply updates active settings and invalid Apply keeps prior valid state while exposing a user-visible warning.</done>
</task>

<task type="auto">
  <name>Task 2: Wire processor state hooks for deterministic save/restore behavior</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_state_roundtrip_tests.cpp</files>
  <action>Implement `getStateInformation` and `setStateInformation` to serialize only core configuration fields through SessionSettingsSerializer. Ensure restore is idempotent, non-throwing, and explicitly forces disconnected runtime session status after project reload.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R plugin_state_roundtrip</automated></verify>
  <done>Integration tests prove cross-instance round-trip consistency, duplicate-track equivalence, and safe fallback for invalid state blobs.</done>
</task>

<task type="auto">
  <name>Task 3: Codify manual Ableton matrix and run the phase gate commands</name>
  <files>docs/validation/phase1-ableton-matrix.md</files>
  <action>Document manual validation steps for insert/edit/save/reopen/duplicate and disconnected-on-restore behavior, consuming the `phase1-quick` label wiring already established in 01-01 so post-task and end-of-phase checks remain executable and repeatable.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -L phase1-quick</automated></verify>
  <done>Manual matrix exists with expected pass criteria and automated quick/full suites are runnable for phase sign-off.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake --build build --config Debug --target famalamajam_plugin`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_apply_flow`
- [ ] `ctest --test-dir build --output-on-failure -R plugin_state_roundtrip`
- [ ] `ctest --test-dir build --output-on-failure -L phase1-quick`
- [ ] `ctest --test-dir build --output-on-failure`

Phase-level verification criteria (must be true to close Phase 1):
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] `pluginval --strictness-level 5 --validate-in-process --validate "build/VST3/Debug/FamaLamaJam.vst3"` (or equivalent validator command documented in repo)
- [ ] Ableton manual matrix passes for: reopen restore, track duplicate clone, corrupt-state fallback, and disconnected restore state.
- [ ] `SESS-01` and `HOST-02` both remain traceable in Phase 1 summaries.
</verification>

<success_criteria>
- All tasks completed
- UI edit/apply flow is validated and non-blocking to audio processing
- Persisted configuration round-trips in host state APIs with schema-safe behavior
- Phase 1 verification gates are executable and aligned to 01-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation-session-configuration/01-02-SUMMARY.md`
</output>



