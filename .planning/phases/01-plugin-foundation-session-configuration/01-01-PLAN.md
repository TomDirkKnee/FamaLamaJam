---
phase: 01-plugin-foundation-session-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - cmake/dependencies.cmake
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/app/session/SessionSettings.h
  - src/app/session/SessionSettings.cpp
  - src/infra/state/SessionSettingsSerializer.h
  - src/infra/state/SessionSettingsSerializer.cpp
  - tests/unit/settings_validation_tests.cpp
  - tests/unit/state_serialization_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - SESS-01
  - HOST-02
user_setup: []
must_haves:
  truths:
    - Users can run the documented CMake commands on Windows x64 and get a loadable Debug VST3 baseline with pinned JUCE dependencies.
    - Users see sane default values for endpoint, port, username, and channels, and invalid values are rejected before settings become active.
    - On project restore, invalid or corrupt persisted settings fall back to safe defaults instead of applying broken values.
  artifacts:
    - path: src/app/session/SessionSettings.h
      provides: Versioned session settings model and validation contract.
      contains: struct SessionSettings
    - path: src/infra/state/SessionSettingsSerializer.cpp
      provides: ValueTree-based save and restore implementation with schemaVersion checks.
      contains: schemaVersion
    - path: tests/unit/state_serialization_tests.cpp
      provides: Round-trip and corrupt-state fallback tests for persisted configuration.
      min_lines: 25
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/app/session/SessionSettings.h
      via: Processor stores and reads active settings snapshot type.
      pattern: SessionSettings
    - from: src/infra/state/SessionSettingsSerializer.cpp
      to: src/app/session/SessionSettings.h
      via: Serializer maps ValueTree fields to SessionSettings fields and back.
      pattern: SessionSettings
---

<objective>
Create the Phase 1 foundation needed to support editable session configuration and project-state persistence in later plans.

Purpose: Establish a stable VST3 scaffold and a validated settings/serialization core that keeps all configuration changes outside the real-time audio path.
Output: Buildable plugin baseline, SessionSettings domain model, versioned serializer, and unit coverage for validation and persistence primitives.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-foundation-session-configuration/01-CONTEXT.md
@.planning/phases/01-plugin-foundation-session-configuration/01-RESEARCH.md
@.planning/phases/01-plugin-foundation-session-configuration/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold JUCE 8 VST3 baseline with layered source layout</name>
  <files>CMakeLists.txt, cmake/dependencies.cmake, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/CMakeLists.txt</files>
  <action>Create a CMake-first JUCE 8.0.12 VST3-only baseline targeting MSVC 2022 x64, define plugin/core/test targets (including initial `phase1-quick` label wiring in `tests/CMakeLists.txt`), and pre-create layered directories (`src/plugin`, `src/app`, `src/audio`, `src/net`, `src/infra`) so later phases do not require structural refactors.</action>
  <verify><automated>cmake -S . -B build -G "Visual Studio 17 2022" -A x64</automated></verify>
  <done>Debug VST3 build succeeds from CMake and repository layout matches Phase 1 architectural decision boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement versioned session settings model and validation rules</name>
  <files>src/app/session/SessionSettings.h, src/app/session/SessionSettings.cpp, tests/unit/settings_validation_tests.cpp</files>
  <action>Define SessionSettings and validation logic for host, port, username, and channel defaults using explicit bounds and normalized input. Expose non-RT-safe application-facing interfaces only; do not couple these rules to any networking or connection lifecycle behavior.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R settings_validation</automated></verify>
  <done>Validation tests cover valid and invalid values, and only valid snapshots can be promoted to active settings.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema-versioned persistence serializer with safe fallback behavior</name>
  <files>src/infra/state/SessionSettingsSerializer.h, src/infra/state/SessionSettingsSerializer.cpp, tests/unit/state_serialization_tests.cpp</files>
  <action>Implement ValueTree-based serialization/deserialization with `schemaVersion = 1`, ignore unknown keys, and apply defaults when state payload parsing or validation fails. Keep runtime connection state out of payload by design.</action>
  <verify><automated>ctest --test-dir build --output-on-failure --timeout 30 -R state_serialization</automated></verify>
  <done>Round-trip tests pass for expected payloads and corrupt payload tests confirm deterministic fallback to defaults without crashes.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmake -S . -B build -G "Visual Studio 17 2022" -A x64`
- [ ] `cmake --build build --config Debug --target famalamajam_plugin`
- [ ] `ctest --test-dir build --output-on-failure -R settings_validation`
- [ ] `ctest --test-dir build --output-on-failure -R state_serialization`

Phase-level verification criteria (applies after all Phase 1 plans finish):
- [ ] `ctest --test-dir build --output-on-failure -L phase1-quick`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] `pluginval --strictness-level 5 --validate-in-process --validate "build/VST3/Debug/FamaLamaJam.vst3"` (or equivalent validator command documented in repo)
- [ ] Ableton reopen + duplicate matrix confirms persisted settings restore while plugin remains disconnected.
</verification>

<success_criteria>
- All tasks completed
- Build and unit verification commands pass
- SESS-01 and HOST-02 prerequisites are established without implementing out-of-scope connection or streaming behavior
- Persistence foundation is migration-safe (`schemaVersion`) and failure-safe (defaults fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation-session-configuration/01-01-SUMMARY.md`
</output>



