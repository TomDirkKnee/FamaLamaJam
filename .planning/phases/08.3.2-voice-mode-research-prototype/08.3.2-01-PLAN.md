---
phase: 08.3.2-voice-mode-research-prototype
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/CMakeLists.txt
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_voice_mode_guard_tests.cpp
  - tests/integration/plugin_voice_mode_transport_tests.cpp
  - src/net/FramedSocketTransport.h
  - src/net/FramedSocketTransport.cpp
  - src/audio/CodecStreamBridge.h
  - src/audio/CodecStreamBridge.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
autonomous: true
requirements:
  - VOICE-02
user_setup: []
must_haves:
  truths:
    - Mixed rooms can carry interval and voice channels at the same time.
    - Voice receive is chunked and near-realtime rather than being fed through the interval scheduler.
    - Transport and harness coverage can simulate remote voice-mode peers deterministically.
  artifacts:
    - path: tests/integration/support/MiniNinjamServer.h
      provides: Mixed-peer voice and interval harness support.
      contains: channelFlags
    - path: tests/integration/plugin_voice_mode_transport_tests.cpp
      provides: Voice send/receive transport and processor coverage.
      min_lines: 140
    - path: src/net/FramedSocketTransport.cpp
      provides: Per-channel voice framing and chunk-oriented transport handling.
      contains: channelFlags
    - path: src/audio/CodecStreamBridge.cpp
      provides: Distinct voice encode/decode submission path.
      contains: voice
  key_links:
    - from: tests/integration/support/MiniNinjamServer.h
      to: tests/integration/plugin_voice_mode_transport_tests.cpp
      via: The harness must express voice and interval peers in the same room.
      pattern: RemotePeer|channelFlags
    - from: src/net/FramedSocketTransport.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Voice frames must branch before interval queueing so receive stays near-realtime.
      pattern: channelFlags|voice
---

<objective>
Build the protocol/runtime foundation for true per-channel voice mode.

Purpose: Replace the current unsupported-voice guard with a mixed-room-capable transport and processor seam that can carry voice and interval channels simultaneously.
Output: Harness support, focused transport tests, and a working chunked voice send/receive path that stays out of the interval scheduler.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED mixed-room transport coverage for voice mode</name>
  <files>tests/CMakeLists.txt, tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_voice_mode_guard_tests.cpp, tests/integration/plugin_voice_mode_transport_tests.cpp</files>
  <action>Extend the integration harness so remote peers can advertise interval or voice mode and create focused transport tests that describe mixed-room send/receive behavior. Keep the existing guard suite only where it still applies, but pivot expectations from "unsupported" toward "chunked voice works alongside interval peers" for the new phase.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_voice_mode_transport],[plugin_voice_mode_guard]"</automated>
  </verify>
  <done>New focused tests compile and fail on the missing mixed-room voice behavior rather than relying on manual-only validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chunked voice transport and processor plumbing</name>
  <files>src/net/FramedSocketTransport.h, src/net/FramedSocketTransport.cpp, src/audio/CodecStreamBridge.h, src/audio/CodecStreamBridge.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_voice_mode_transport_tests.cpp</files>
  <action>Introduce the first true voice-mode plumbing path. Voice send should use lower-latency chunk submission instead of interval buffering, and voice receive should promote/play chunked audio without waiting for interval boundaries. Keep interval behavior unchanged for non-voice channels in the same room.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_voice_mode_transport],[plugin_experimental_transport]"</automated>
  </verify>
  <done>Focused transport tests pass and mixed-room voice traffic no longer depends on the interval scheduler.</done>
</task>

</tasks>

<verification>
- [ ] `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_voice_mode_transport],[plugin_voice_mode_guard]"`
</verification>

<output>
After completion, create `.planning/phases/08.3.2-voice-mode-research-prototype/08.3.2-01-SUMMARY.md`
</output>
