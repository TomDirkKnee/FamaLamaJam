# Phase 08.3.2 Verification

## Status

Complete with deferred mixer-layout follow-up.

## Automated evidence

Validation path:
- `build-vs-2026`

Commands run:
- `cmake --build build-vs-2026 --target famalamajam_tests --config Debug -- /m:1 /v:minimal`
- `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_voice_mode_transport],[plugin_voice_mode_toggle],[plugin_voice_mode_guard],[plugin_transmit_controls_ui]"`
- `cmake --build build-vs-2026 --target famalamajam_plugin_VST3 --config Debug -- /m:1 /v:minimal`

Results:
- focused Wave 2 slice: pass, `109` assertions across `12` test cases
- phase closeout gate: pass, `73` assertions across `9` test cases
- debug VST3 rebuilt successfully

Artifact:
- `build-vs-2026/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3`

## What automation now proves

- remote voice peers play through the normal mixer path instead of being blocked as unsupported
- the local strip exposes a dedicated voice toggle beside transmit
- local voice mode can be armed and switched live
- switching from voice back to interval returns through the normal interval warmup path
- remote voice strips keep the normal strip controls and orange voice semantics

## Manual evidence still needed

Manual Ableton/mixed-room results:
- `P8.3.2-VOICE-01`: pass, with a layout caveat. The local strip now shows both `Transmit` and `Voice`, but the added controls squeeze the existing pan/gain row too hard.
- `P8.3.2-VOICE-02`: pass. Local voice quality is materially improved and usable.
- `P8.3.2-VOICE-03`: pass. Remote voice strips stay orange and the voice text no longer flickers.
- `P8.3.2-VOICE-04`: pass. Mixed interval and voice peers worked together in real-room testing.
- `P8.3.2-VOICE-05`: pass. Switching from local voice back to interval mode resumed normal interval behavior in user testing.

Readiness decision:
- `08.3.2` is good enough to close as a working voice-mode prototype.
- The remaining issue is no longer core voice transport behavior. It is mixer density/layout pressure caused by fitting both transmit and voice controls into the current strip design.
- That layout work should move into the next mixer/UI phase rather than block voice-mode completion here.
