---
phase: 09.1-strip-only-mixer-rebuild-on-stable-layout
plan: "04"
subsystem: ui
tags: [juce, mixer, layout, footer, ableton]
requires:
  - phase: 09.1-03
    provides: Compact strip density pass plus explicit Ableton shell verdicts that identified the server-settings and footer regressions
provides:
  - Compact expanded server-settings shell with shared field alignment and always-visible connect or disconnect actions
  - Wider default editor width for a larger stable-shell mixer viewport
  - Full-width footer geometry with restored metronome volume beside transport and master output
affects: [phase-09.1-05, phase-09.1-06, shell-layout, ableton-validation]
tech-stack:
  added: []
  patterns: [JUCE compact shell geometry, aligned settings columns, full-width footer regression coverage]
key-files:
  created:
    - .planning/phases/09.1-strip-only-mixer-rebuild-on-stable-layout/09.1-04-SUMMARY.md
  modified:
    - src/plugin/FamaLamaJamAudioProcessorEditor.h
    - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
    - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
    - tests/integration/plugin_room_controls_ui_tests.cpp
    - tests/integration/plugin_transport_ui_sync_tests.cpp
    - .planning/phases/09.1-strip-only-mixer-rebuild-on-stable-layout/09.1-VALIDATION.md
key-decisions:
  - "Widen the editor to a 1350 px default while keeping the pre-Phase-9 page shell instead of reopening a broader top-bar/sidebar redesign."
  - "Compact the expanded server-settings shell into a left column and vertically offset the room sidebar so room chat still reads beside, not under, the settings rows."
patterns-established:
  - "Wave 4 shell regressions are enforced through geometry assertions on shared field columns, room-sidebar coexistence, and full-width footer components."
  - "Stable-shell recovery work can move the expanded settings shell without moving footer ownership or the right-hand room workflow out of the pre-Phase-9 frame."
requirements-completed: [LAYOUT-01, LAYOUT-03]
duration: 10 min
completed: 2026-04-08
---

# Phase 09.1 Plan 04: Stable Shell Recovery Summary

**Compact expanded server settings, a 1350 px default editor shell, and a full-width footer with restored metronome volume on the stable page layout**

## Performance

- **Duration:** 10 min
- **Started:** 2026-04-08T08:21:50Z
- **Completed:** 2026-04-08T08:32:11Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Added RED regressions that pin the shell recovery to concrete geometry: aligned settings fields, visible connect or disconnect controls, readable room chat beside the shell, and a widened default editor plus full-width footer.
- Reworked the expanded settings layout into a compact left shell while keeping the stable right-hand room sidebar and the pre-Phase-9 overall page structure intact.
- Restored the widened default editor and full-width footer layout so the metronome volume knob sits beside transport and master output again.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add RED shell regressions for compact settings geometry and full-width footer recovery** - `d307893` (`test`)
2. **Task 2: Implement compact settings-shell, wider default width, and full-width footer recovery** - `a27c20e` (`fix`)

## Files Created/Modified

- `src/plugin/FamaLamaJamAudioProcessorEditor.h` - Added stable-shell default editor size constants for the widened host-guided baseline.
- `src/plugin/FamaLamaJamAudioProcessorEditor.cpp` - Compacted the expanded settings shell into aligned left-column rows, widened the default editor, preserved room-sidebar coexistence, and kept the footer full width.
- `tests/integration/plugin_rehearsal_ui_flow_tests.cpp` - Locked visibility and alignment coverage for expanded settings and connect or disconnect actions.
- `tests/integration/plugin_room_controls_ui_tests.cpp` - Added sidebar coexistence coverage so room chat stays readable beside the compact shell.
- `tests/integration/plugin_transport_ui_sync_tests.cpp` - Locked the widened default editor and full-width footer with restored metronome volume geometry.
- `.planning/phases/09.1-strip-only-mixer-rebuild-on-stable-layout/09.1-VALIDATION.md` - Marked both Wave 4 shell tasks green while leaving the broader host verdict blocked until Plan 09.1-06.

## Decisions Made

- Kept the fix inside `resized()` and shell constants only, so the stable pre-Phase-9 page structure remains the boundary for this recovery work.
- Moved the room sidebar start below the summary row when settings are expanded, which preserves diagnostics precedence and keeps room chat visually aligned with the compact settings rows.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first compact-shell pass left the room sidebar anchored to the top of the editor, which broke both the diagnostics ordering assertion and the new "chat beside expanded settings" regression. Adjusting the sidebar top inset resolved both failures and kept the Wave 4 gate narrow.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 4 shell recovery is now green on the focused automation gate and ready for the Plan 09.1-05 strip-height and control-density follow-up.
- Phase 09.1 still depends on the later combined gate and Ableton rerun in Plan 09.1-06 before the blocked host verdict can be replaced.

## Self-Check: PASSED

- `FOUND: .planning/phases/09.1-strip-only-mixer-rebuild-on-stable-layout/09.1-04-SUMMARY.md`
- `FOUND: d307893`
- `FOUND: a27c20e`
