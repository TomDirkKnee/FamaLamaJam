---
phase: 06-ableton-sync-assist-research-prototype
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - docs/validation/phase6-ableton-sync-matrix.md
  - .planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - HSYNC-01
  - HSYNC-02
user_setup:
  - Open Ableton on Windows with the freshly built `FamaLamaJam.vst3`.
  - Run the cases in `docs/validation/phase6-ableton-sync-matrix.md`.
  - Report pass or fail notes for armed start, tempo-mismatch blocking, cancel-on-timing-loss, cancel-before-play, failed-start requiring manual re-arm, and one-shot reset after success.
must_haves:
  truths:
    - Phase 6 closes with a documented feasibility decision about Ableton host-start sync assist, not just code changes and assumptions.
    - Verification records the exact built VST3 artifact, the automated test gate, and the manual Ableton outcomes for the locked Phase 6 workflow.
    - Any remaining host-data limitations or UX constraints are written down explicitly so future work can decide whether broader host-sync investment is justified.
    - Manual validation proves that a failed start clears the armed trigger and that later host Play events remain inert until the user explicitly re-arms the assist.
  artifacts:
    - path: docs/validation/phase6-ableton-sync-matrix.md
      provides: The concise manual Ableton matrix for armed start, blocked arming, timing-loss cancellation, pre-play cancel, failed-start manual re-arm behavior, and one-shot reset.
      contains: Phase 6 Ableton Sync Assist Validation Matrix
    - path: .planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md
      provides: The final Phase 6 verification report combining automated gates, manual Ableton findings, and the feasibility conclusion.
      contains: Verification Summary
    - path: build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3
      provides: The built plugin artifact used for the manual Ableton validation pass.
  key_links:
    - from: docs/validation/phase6-ableton-sync-matrix.md
      to: .planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md
      via: Manual matrix outcomes roll into the verification report and determine whether Phase 6 can close as a successful prototype.
      pattern: Outcome
    - from: build-vs/famalamajam_plugin_artefacts/Debug/VST3/FamaLamaJam.vst3
      to: .planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md
      via: The report must name the exact artifact tested so the feasibility conclusion is tied to a real build, not a stale binary.
      pattern: VST3
---

<objective>
Validate the prototype in the full build path and record the feasibility outcome for future host-sync work.

Purpose: Turn the Phase 6 prototype into an evidence-backed decision about what Ableton host-sync assistance is actually practical in this plugin.
Output: A concise Ableton validation matrix, a verification report with automated and manual outcomes, and updated planning state reflecting the Phase 6 result.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-CONTEXT.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-RESEARCH.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-VALIDATION.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-01-PLAN.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the concise Ableton validation matrix and verification report scaffold</name>
  <files>docs/validation/phase6-ableton-sync-matrix.md, .planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md</files>
  <action>Turn the Phase 6 context, research, and validation notes into a short manual matrix that covers the exact locked workflow: successful armed start with matching tempo, blocked arming on tempo mismatch, armed-state cancellation on timing loss, explicit cancel before pressing Play, failed-start behavior that clears the one-shot arm until the user explicitly re-arms, and one-shot reset after a successful start. Create a verification report template that captures the automated gate, exact VST3 artifact path, manual outcomes, and a short feasibility conclusion about whether broader host-sync work should continue.</action>
  <verify><automated>powershell -Command "Test-Path docs\\validation\\phase6-ableton-sync-matrix.md; Test-Path .planning\\phases\\06-ableton-sync-assist-research-prototype\\06-VERIFICATION.md"</automated></verify>
  <done>The manual matrix and verification report exist and are specific to the actual Phase 6 prototype behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run the targeted sync-assist regression gate and prepare the manual-validation build record</name>
  <files>.planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md</files>
  <action>Run the targeted host-sync suites first and record those outcomes in `06-VERIFICATION.md` as the fast execution-time feedback loop for this plan. Reserve the full `ctest` gate and the VST3 build for the plan-complete verification block below, so the executor can iterate quickly before paying the full build cost. If the targeted suites reveal that the prototype still implies generic host control, breaks authoritative room timing, or fails the manual re-arm-after-failure rule, fix that before proceeding to the final gate.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_sync_assist],[plugin_transport_ui_sync]"'</automated></verify>
  <done>The targeted host-sync regression gate is green and the verification report is ready for the final full gate and build recording step.</done>
</task>

<task type="auto">
  <name>Task 3: Capture manual Ableton outcomes and close Phase 6 with an explicit feasibility result</name>
  <files>.planning/phases/06-ableton-sync-assist-research-prototype/06-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Use the user's Ableton results from `docs/validation/phase6-ableton-sync-matrix.md` to complete the verification report with pass or fail notes, missing-host-data observations, and a concise recommendation on whether future host-sync work should stay narrow or continue into broader research. Explicitly record the failed-start retry behavior by confirming that once a start attempt fails, later host Play presses do nothing until the user manually re-arms the assist. If the prototype meets the locked expectations, update planning state and roadmap progress for Phase 6 completion. If constraints remain, document them clearly instead of silently broadening scope or hiding failure modes.</action>
  <verify><automated>powershell -Command "Select-String -Path .planning\\phases\\06-ableton-sync-assist-research-prototype\\06-VERIFICATION.md -Pattern 'Verification Summary|Manual Outcomes|Feasibility Conclusion|Residual Constraints'"</automated></verify>
  <done>Phase 6 closes with a traceable automated plus manual verification record and a clear feasibility conclusion for future host-sync work.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug'`
- [ ] User completes `docs/validation/phase6-ableton-sync-matrix.md` in Ableton and outcomes are recorded in `06-VERIFICATION.md`

Phase-level verification criteria (applies after all Phase 6 plans finish):
- [ ] `ctest --test-dir build-vs --output-on-failure`
- [ ] The tested VST3 artifact is recorded in `06-VERIFICATION.md`
- [ ] Manual Ableton validation covers bar-start and mid-bar armed starts, blocked arming on tempo mismatch, timing-loss cancellation, explicit cancel, failed-start requiring manual re-arm before later Play presses do anything, and one-shot reset after success
</verification>

<success_criteria>
- All tasks completed
- HSYNC-01 and HSYNC-02 are backed by both automation and real Ableton usage evidence
- The Phase 6 outcome states what is feasible in Ableton and what remains constrained
- Future work can build on a concrete feasibility record instead of repeating the same research
</success_criteria>

<output>
After completion, create `.planning/phases/06-ableton-sync-assist-research-prototype/06-03-SUMMARY.md`
</output>
