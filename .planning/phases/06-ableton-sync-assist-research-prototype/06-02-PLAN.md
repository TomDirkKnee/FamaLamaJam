---
phase: 06-ableton-sync-assist-research-prototype
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_transport_ui_sync_tests.cpp
  - tests/integration/plugin_rehearsal_ui_flow_tests.cpp
autonomous: true
requirements:
  - HSYNC-02
user_setup: []
must_haves:
  truths:
    - The editor shows host-sync assist as an explicit optional transport-adjacent workflow, not a hidden background mode or a large separate panel.
    - The arm control is disabled with a short inline explanation whenever room timing is unavailable or Ableton tempo does not already match room BPM.
    - Armed, canceled, failed, and ready states are communicated in plain language, and the same control can cancel the waiting state before Ableton playback starts.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.h
      provides: Editor contracts and test helpers for the host-sync assist button, status copy, and enablement state.
      contains: clickHostSyncAssistForTesting
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Transport-area host-sync assist layout, button wiring, room BPM/BPI display, and inline status rendering.
      contains: refreshTransportStatus
    - path: tests/integration/plugin_transport_ui_sync_tests.cpp
      provides: Editor-facing regression coverage for ready, blocked, armed, canceled, and failed host-sync assist states.
      min_lines: 180
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: The processor createEditor wiring must pass the new host-sync assist getter and command handler into the editor instead of duplicating state in UI code.
      pattern: createEditor
    - from: tests/integration/plugin_transport_ui_sync_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Editor regression tests prove the UI wording and enablement rules match the processor snapshot rather than ad-hoc editor assumptions.
      pattern: getHostSyncAssist
---

<objective>
Expose the host-sync assist as a clear transport-adjacent Ableton workflow in the JUCE editor.

Purpose: Make the Phase 6 behavior explicit, optional, and understandable without overpowering the existing setup/connect/status flow.
Output: Editor wiring for the new processor snapshot/command, transport-area sync-assist controls, and UI regression coverage for blocked and armed states.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-CONTEXT.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-RESEARCH.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-VALIDATION.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-01-PLAN.md
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_transport_ui_sync_tests.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
using TransportUiGetter = std::function<TransportUiState()>;
using CommandHandler = std::function<bool()>;

[[nodiscard]] juce::String getTransportStatusTextForTesting() const;
void refreshForTesting();
```

Plan 01 creates these processor contracts that this plan must consume directly:
```cpp
[[nodiscard]] HostSyncAssistUiState getHostSyncAssistUiState() const noexcept;
bool toggleHostSyncAssistArm();
```

Add these exact editor test hooks in `src/plugin/FamaLamaJamAudioProcessorEditor.h`:
```cpp
[[nodiscard]] juce::String getHostSyncAssistStatusTextForTesting() const;
[[nodiscard]] juce::String getHostSyncAssistButtonTextForTesting() const;
[[nodiscard]] bool isHostSyncAssistEnabledForTesting() const noexcept;
void clickHostSyncAssistForTesting();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add editor-facing regression coverage for host-sync assist copy and enablement</name>
  <files>tests/integration/plugin_transport_ui_sync_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <behavior>
    - Test 1: When room timing is healthy and host tempo matches room BPM, the editor shows a ready arm control with room BPM/BPI context.
    - Test 2: When timing is absent or tempo mismatches, the arm control is disabled and the editor shows a short inline explanation instead of a vague generic error.
    - Test 3: Armed, canceled, and failed-start states render explicit plain-language guidance, and the same control can cancel the waiting state.
  </behavior>
  <action>Extend the existing transport/editor test harnesses instead of inventing a new UI test framework. Use the new testing hooks named in this plan so the UI state can be asserted directly. Keep the tests focused on wording, button label/enabled state, and the fact that host-sync assist remains part of the top transport/status workflow rather than becoming a separate view.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transport_ui_sync],[plugin_rehearsal_ui_flow]"'</automated></verify>
  <done>The editor-facing expectations for the Phase 6 workflow are locked before UI implementation relies on them.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the editor to the new processor host-sync assist snapshot and command path</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h</files>
  <action>Update `createEditor()` and the editor constructor surface so the UI consumes the processor-owned host-sync assist snapshot and a single toggle-style arm-or-cancel command. Add the testing helpers named in this plan. Keep the processor as the single source of truth; do not create a second editor-owned state machine for armed, blocked, or failed host-sync assist states.</action>
  <verify><automated>powershell -Command "Select-String -Path src\\plugin\\FamaLamaJamAudioProcessorEditor.h,src\\plugin\\FamaLamaJamAudioProcessor.cpp -Pattern 'getHostSyncAssistUiState|toggleHostSyncAssistArm|getHostSyncAssistStatusTextForTesting|clickHostSyncAssistForTesting'"</automated></verify>
  <done>The editor receives all Phase 6 host-sync state and commands from the processor contract with no duplicated UI-only sync logic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the transport-adjacent arm or cancel UI with blocked and armed messaging</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_transport_ui_sync_tests.cpp, tests/integration/plugin_rehearsal_ui_flow_tests.cpp</files>
  <action>Add the visible but secondary `Arm Sync to Ableton Play` workflow near the existing transport/status area. Show room BPM/BPI next to the control, disable the arm button with a short inline explanation when timing is absent or tempo mismatches, render explicit armed guidance such as telling the user to press Play in Ableton, surface canceled or failed-start states plainly, and keep the rest of the single-page join/connect workflow intact. Do not imply always-on sync mode, host tempo push, or loop-region control.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transport_ui_sync],[plugin_rehearsal_ui_flow]"'</automated></verify>
  <done>The Phase 6 behavior is visible, understandable, and honestly framed as an optional one-shot assist inside the existing JUCE editor.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_transport_ui_sync],[plugin_rehearsal_ui_flow]"'`

Phase-level verification criteria (applies after all Phase 6 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the transport-area wording makes the armed-start workflow obvious and the blocked state understandable.
</verification>

<success_criteria>
- All tasks completed
- HSYNC-02 is satisfied by explicit UI affordance, honest blocked-state messaging, and plain-language armed guidance
- The editor consumes processor-owned state instead of inventing a UI-only sync model
- Phase 6 UI does not over-promise generic DAW transport control
</success_criteria>

<output>
After completion, create `.planning/phases/06-ableton-sync-assist-research-prototype/06-02-SUMMARY.md`
</output>
