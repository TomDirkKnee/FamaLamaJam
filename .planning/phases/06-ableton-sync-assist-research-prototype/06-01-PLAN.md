---
phase: 06-ableton-sync-assist-research-prototype
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/integration/plugin_host_sync_assist_tests.cpp
  - tests/integration/plugin_server_timing_sync_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - HSYNC-01
user_setup: []
must_haves:
  truths:
    - The processor only allows the host-sync assist to arm when real room timing exists and Ableton tempo already matches the room BPM.
    - While the assist is armed and waiting for Ableton playback, NINJAM interval playback and metronome output are held instead of free-running in the background.
    - When the host transport starts with usable musical-position data, the processor computes an aligned one-shot start from host position, clears the armed state after success, and never falls back to fake timing if alignment cannot be completed.
    - After a failed aligned start, the armed trigger is cleared and later host Play transitions do nothing until the user explicitly re-arms the one-shot assist.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Processor-side contracts for host transport snapshots, host-sync assist state, and explicit arm-or-cancel commands exposed to the editor and tests.
      contains: getHostSyncAssistUiState
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Host playhead observation and the server-authoritative armed-start state machine inside the processor audio path.
      contains: processBlock
    - path: tests/integration/plugin_host_sync_assist_tests.cpp
      provides: Injected playhead regression coverage for arming, host-start detection, offset alignment, cancellation, failure, and one-shot reset.
      min_lines: 120
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_host_sync_assist_tests.cpp
      via: Injected JUCE playhead tests drive processBlock transitions and prove the armed start only fires on a host play transition with valid timing data.
      pattern: getPlayHead
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_server_timing_sync_tests.cpp
      via: Server-timing regressions guard that host assist logic still depends on authoritative room timing rather than replacing it.
      pattern: getTransportUiState
---

<objective>
Add the processor-side host playhead observation and armed one-shot sync-assist state machine.

Purpose: Prove the narrow Phase 6 behavior is feasible in JUCE/Ableton without weakening the existing server-authoritative timing model.
Output: Explicit processor contracts for host-sync assist state, injected playhead integration coverage, and implementation of the one-shot aligned-start behavior.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-CONTEXT.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-RESEARCH.md
@.planning/phases/06-ableton-sync-assist-research-prototype/06-VALIDATION.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_server_timing_sync_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
struct TransportUiState
{
    bool connected { false };
    bool hasServerTiming { false };
    SyncHealth syncHealth { SyncHealth::Disconnected };
    bool metronomeAvailable { false };
    int beatsPerMinute { 0 };
    int beatsPerInterval { 0 };
    int currentBeat { 0 };
    float intervalProgress { 0.0f };
    std::uint64_t intervalIndex { 0 };
};

[[nodiscard]] TransportUiState getTransportUiState() const noexcept;
```

Create these exact contracts in `src/plugin/FamaLamaJamAudioProcessor.h` before implementation:
```cpp
struct HostTransportSnapshot;
struct HostSyncAssistUiState;
[[nodiscard]] HostSyncAssistUiState getHostSyncAssistUiState() const noexcept;
bool toggleHostSyncAssistArm();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the processor contracts for host-sync assist state and commands</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h</files>
  <action>Extend the processor header with explicit Phase 6 contracts for host playhead observation and sync-assist UI state. Create exact public contracts named `HostTransportSnapshot`, `HostSyncAssistUiState`, `getHostSyncAssistUiState()`, and `toggleHostSyncAssistArm()`. Include enough fields for the editor and tests to know whether the assist is armable, armed, blocked, failed, and what room BPM/BPI it is targeting. Do not add any API that suggests generic host tempo writeback or loop-region control.</action>
  <verify><automated>powershell -Command "Select-String -Path src\\plugin\\FamaLamaJamAudioProcessor.h -Pattern 'HostTransportSnapshot|HostSyncAssistUiState|getHostSyncAssistUiState|toggleHostSyncAssistArm'"</automated></verify>
  <done>The processor exposes stable public contracts for Phase 6 without forcing the executor to invent names or editor-facing state later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add injected playhead coverage and implement the armed one-shot start state machine</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_host_sync_assist_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Arming fails cleanly when server timing is absent or when host tempo does not match room BPM, and the UI snapshot exposes the blocking reason.
    - Test 2: When armed and the host transitions from stopped to playing with valid PPQ and bar-start data, the processor computes aligned start offset from host musical position and clears the armed state after success.
    - Test 3: If timing is lost while armed, or the host starts without required musical-position fields, the assist cancels or fails visibly and never falls back to fake timing.
    - Test 4: After a successful aligned start, later host stops do not silently re-arm or force the processor back into waiting-for-host mode.
    - Test 5: After a failed aligned start, the processor clears the armed trigger so later host Play transitions are ignored until `toggleHostSyncAssistArm()` is called again explicitly.
  </behavior>
  <action>Write `tests/integration/plugin_host_sync_assist_tests.cpp` against JUCE playhead injection first, using `AudioProcessor::setPlayHead()` rather than a parallel fake transport system. Then implement the processor logic in `processBlock()` and related helpers so host play-state snapshots are captured on the audio thread, arming is only permitted with real room timing plus room-BPM tempo match, armed waiting suppresses NINJAM playback/metronome output, aligned start uses host PPQ plus bar-start position for mid-bar offset handling, timing loss cancels the armed state, and missing host musical-position data yields an explicit failed-start state instead of a best-effort fallback. Treat failed start as a consumed one-shot trigger: clear the armed flag immediately, expose the failure in the UI snapshot, and require a fresh manual re-arm before any later host Play event can attempt alignment again. If host float representation requires it, use only a conservative tolerance for tempo match and document it in code comments or summary.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_sync_assist]"'</automated></verify>
  <done>The processor can perform the Phase 6 prototype behavior end-to-end under deterministic playhead tests while preserving server-authoritative timing rules.</done>
</task>

<task type="auto">
  <name>Task 3: Lock server-authoritative timing regressions around the new assist behavior</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.cpp, tests/integration/plugin_server_timing_sync_tests.cpp, tests/integration/plugin_host_sync_assist_tests.cpp</files>
  <action>Extend or tighten existing server-timing assertions so the Phase 6 processor changes cannot accidentally make host timing authoritative. Cover the critical edges where the armed state should hold transport progress and metronome output, where reconnect or timing loss must clear assist state, and where successful aligned start should return to the existing free-running authoritative timing path rather than a persistent host-following mode.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_sync_assist],[plugin_server_timing_sync]"'</automated></verify>
  <done>Phase 6 adds host-start assistance without regressing the established Phase 3 timing guarantees.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_host_sync_assist],[plugin_server_timing_sync]"'`

Phase-level verification criteria (applies after all Phase 6 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the one-shot assist starts from host musical position or fails visibly when required host fields are unavailable.
</verification>

<success_criteria>
- All tasks completed
- HSYNC-01 processor behavior is implemented with deterministic playhead-driven coverage
- Arming, waiting, aligned start, cancellation, failure, and explicit manual re-arm-after-failure edges are explicit and server-authoritative
- No generic host tempo or loop control behavior is implied by the new processor contract
</success_criteria>

<output>
After completion, create `.planning/phases/06-ableton-sync-assist-research-prototype/06-01-SUMMARY.md`
</output>
