---
phase: 08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation
plan: "02"
type: execute
wave: 2
depends_on:
  - 08.3.4.1-01
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - src/audio/CodecStreamBridge.h
  - src/audio/CodecStreamBridge.cpp
  - src/net/FramedSocketTransport.h
  - src/net/FramedSocketTransport.cpp
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_host_multi_io_routing_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/integration/plugin_voice_mode_toggle_tests.cpp
  - tests/integration/plugin_voice_mode_transport_tests.cpp
autonomous: true
requirements:
  - ROUTE-02
  - ROUTE-03
user_setup: []
must_haves:
  truths:
    - Ableton can feed `Main` and `Local Send 2` into one plugin instance and FLJ uploads them as distinct local NINJAM channels from one transport path.
    - Global local `Transmit` and `Voice` apply across active local channels while each local strip retains its own name, gain, pan, mute, and solo state.
    - A remote assigned to `Remote Out 1` or `Remote Out 2` leaves `FLJ Main Output` completely, while unassigned remotes remain on main.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Fixed local-slot runtime, fixed-cap remote output map, state serialization, and editor projection.
      min_lines: 3500
    - path: src/audio/CodecStreamBridge.cpp
      provides: Channel-indexed encoded local frame handling for multiple local uploads.
      contains: channelIndex
    - path: src/net/FramedSocketTransport.cpp
      provides: Per-channel local metadata and upload-begin channel indices on the existing outbound path.
      contains: writeUploadInterval
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Local header, add-channel or rename UI, and remote output dropdown integration in the current mixer layout.
      contains: Add channel
  key_links:
    - from: src/audio/CodecStreamBridge.cpp
      to: src/net/FramedSocketTransport.cpp
      via: Encoded outbound local frames must preserve channel index and metadata end to end.
      pattern: channelIndex|enqueueOutbound|setLocalChannelInfo|writeUploadInterval
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: The editor must consume processor-owned local-slot and remote-route snapshots rather than inventing local UI state.
      pattern: Add channel|FLJ Main Output|Remote Out 1|Remote Out 2
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_state_roundtrip_tests.cpp
      via: Hidden extra local slots and remote assignments must survive save or restore by stable slot ID, not editable display name.
      pattern: local-send-2|remoteOutput|visible|displayName
---

<objective>
Implement the full fixed-bus runtime and current-layout UI for the first practical multi-input or routed-output workflow: two local transmit slots, fixed remote output pairs, global local send controls, and restore-safe per-strip state.

Purpose: This is the actual ROUTE-02 and ROUTE-03 implementation phase. It must convert the proof buses into real multi-channel transport and editor behavior without widening scope into dynamic buses or a mixer redesign.
Output: Green automated routing, UI, voice-mode, and restore coverage for the fixed two-local or fixed-remote-output implementation.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-CONTEXT.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-RESEARCH.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-VALIDATION.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-01-PLAN.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-02-SUMMARY.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-03-SUMMARY.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@src/audio/CodecStreamBridge.h
@src/audio/CodecStreamBridge.cpp
@src/net/FramedSocketTransport.h
@src/net/FramedSocketTransport.cpp
@tests/integration/plugin_host_multi_io_routing_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
bool toggleTransmitEnabled();
bool toggleLocalVoiceMode();
[[nodiscard]] std::vector<MixerStripSnapshot> getMixerStripSnapshots() const;
[[nodiscard]] bool getMixerStripSnapshot(const std::string& sourceId, MixerStripSnapshot& snapshot) const;
void getStateInformation(juce::MemoryBlock& destData) override;
void setStateInformation(const void* data, int sizeInBytes) override;
```

From src/plugin/FamaLamaJamAudioProcessorEditor.h:
```cpp
using MixerStripsGetter = std::function<std::vector<MixerStripState>()>;
using MixerStripSetter = std::function<bool(const std::string&, float, float, bool)>;
using MixerStripSoloSetter = std::function<bool(const std::string&, bool)>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixed local slots and channel-indexed outbound uploads</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/audio/CodecStreamBridge.h, src/audio/CodecStreamBridge.cpp, src/net/FramedSocketTransport.h, src/net/FramedSocketTransport.cpp, tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_host_multi_io_routing_tests.cpp, tests/integration/plugin_voice_mode_transport_tests.cpp</files>
  <behavior>
    - Test 1: `Main` uploads as local channel `0` and `Local Send 2` uploads as local channel `1` on the existing codec or transport path.
    - Test 2: Local channel metadata uses stable slot identity plus editable label, while voice mode applies to all active local slots.
    - Test 3: Hidden or inactive extra local slots do not emit audio or duplicate channel-info messages.
  </behavior>
  <action>Replace the current singleton-local sender path with a fixed local-slot registry and runtime state in the processor. Extend `CodecStreamBridge` and `FramedSocketTransport` so outbound local uploads carry channel index and channel metadata through the one existing upload pipeline rather than adding a second sender subsystem. Preserve the host-owned bus model: FLJ exposes `Main` and `Local Send 2`, Ableton routes tracks externally, and FLJ never enumerates Ableton tracks. Keep `Transmit` and `Voice` global across active local slots, but do not yet move per-strip UI; that wiring lands in Task 3.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>The focused routing and voice transport suites are green for two local upload channels on one fixed-bus plugin instance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the fixed remote-output assignment map and exclusive bus rendering</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/net/FramedSocketTransport.h, src/net/FramedSocketTransport.cpp, tests/integration/plugin_host_multi_io_routing_tests.cpp, tests/integration/plugin_voice_mode_transport_tests.cpp</files>
  <behavior>
    - Test 1: Remote sources can be assigned only to `FLJ Main Output`, `Remote Out 1`, or `Remote Out 2`.
    - Test 2: A routed remote is removed from the main mix completely rather than mirrored.
    - Test 3: Cleared or invalid assignments fall back to main, and disabled aux outputs remain silent.
  </behavior>
  <action>Retire the one-source `selectedRoutedSourceId` proof seam and replace it with a fixed-cap `sourceId -> outputBusIndex` assignment map where `0` means main and `1..2` mean the extra routed output pairs. Expand the processor bus declaration from the proof output pair to the fixed two-pair cap locked in plan 01. Apply the assignment map uniformly to interval and voice remote rendering, and keep unassigned remotes on main. Do not mirror routed remotes, do not add arbitrary bus counts, and do not add a generic patchbay abstraction.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>The fixed-cap route map is live, routed remotes leave main completely, and aux outputs clear correctly when nothing is assigned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the current editor and state model to the fixed routing runtime</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/integration/plugin_voice_mode_toggle_tests.cpp</files>
  <behavior>
    - Test 1: The local area shows one shared `Transmit` or `Voice` control set plus an inline `Add channel` action that reveals the extra full strip.
    - Test 2: Local names default from host or bus name when available, otherwise `Local Send 2`, and names are editable without breaking stored strip state.
    - Test 3: Removing the extra local channel hides it but remembers its settings, and remote strips expose a small output dropdown with explicit host-facing labels.
  </behavior>
  <action>Project the new processor-owned local-slot runtime into the current editor without redesigning the layout. Move global local `Transmit` and `Voice` into a local-group header, add an inline `Add channel` action that only reveals the one extra local slot, preserve per-strip gain or pan or mute or solo, and add a small remote-strip dropdown using `FLJ Main Output`, `Remote Out 1`, and `Remote Out 2`. Persist hidden-local visibility, editable local labels, and remote output assignments by stable slot or source IDs so save or restore honors the locked hide-and-remember behavior. Do not start horizontal strip work, meter redesign, or any broader Phase 9 layout changes.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip],[plugin_voice_mode_toggle]"</automated>
  </verify>
  <done>The editor and restore suites are green for add-channel, rename, global local controls, hide-and-remember behavior, and remote-strip output routing.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_voice_mode_transport]"`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip],[plugin_voice_mode_toggle]"`
- [ ] `ctest --test-dir build-vs-2026 --output-on-failure`
</verification>

<success_criteria>
- ROUTE-02 is implemented as exactly two fixed local transmit paths using the existing transport stack with channel-indexed metadata
- ROUTE-03 is implemented as fixed exclusive remote output routing with no main-output mirroring
- The current editor supports the locked UX without reopening dynamic buses, Ableton track enumeration, or Phase 9 layout scope
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-02-SUMMARY.md`
</output>
