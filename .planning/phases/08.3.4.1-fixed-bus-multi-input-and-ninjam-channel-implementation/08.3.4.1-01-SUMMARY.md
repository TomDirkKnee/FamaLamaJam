---
phase: 08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation
plan: "01"
subsystem: testing
tags: [routing, fixed-bus, ninjam, ableton, catch2]
requires:
  - phase: 08.3.4-host-multi-io-routing-research
    provides: fixed-bus host proof, Ableton routing verdict, and host-owned routing scope
provides:
  - fixed-slot processor and transport contracts for two local send slots and fixed host-facing output labels
  - RED integration, mixer-ui, and state-roundtrip coverage for missing multi-local upload, UI, and restore behavior
  - manual Ableton matrix for ROUTE-02 and ROUTE-03 validation
affects: [08.3.4.1-02, ROUTE-02, ROUTE-03, fixed-bus-routing]
tech-stack:
  added: []
  patterns: [compile-time fixed-slot routing contracts, captured outbound client metadata in MiniNinjamServer]
key-files:
  created: [docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md]
  modified:
    [
      src/plugin/FamaLamaJamAudioProcessor.h,
      src/audio/CodecStreamBridge.h,
      src/net/FramedSocketTransport.h,
      src/plugin/FamaLamaJamAudioProcessorEditor.h,
      tests/integration/support/MiniNinjamServer.h,
      tests/integration/plugin_host_multi_io_routing_tests.cpp,
      tests/integration/plugin_mixer_ui_tests.cpp,
      tests/integration/plugin_state_roundtrip_tests.cpp,
    ]
key-decisions:
  - "Lock Wave 0 to two local send slots and three fixed host-facing output labels before runtime work begins."
  - "Use header-level compatibility shims and captured transport metadata so RED coverage can compile against the new contracts without implementing the runtime early."
patterns-established:
  - "Pattern 1: Fixed routing shape is declared in public headers first, then runtime work must satisfy those contracts."
  - "Pattern 2: MiniNinjamServer captures outbound client metadata so multi-local upload behavior is proven at the protocol boundary."
requirements-completed: [ROUTE-02, ROUTE-03]
duration: 12 min
completed: 2026-04-05
---

# Phase 08.3.4.1 Plan 01: Lock fixed-slot routing contracts, RED Wave 0 coverage, and the Ableton fixed-bus matrix Summary

**Fixed-slot routing contracts, outbound metadata capture, RED UI or restore coverage, and an Ableton matrix now define the exact gaps the runtime must close next.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-04-05T08:42:18Z
- **Completed:** 2026-04-05T08:53:54Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Added fixed-slot processor and transport declarations for `Main`, `Local Send 2`, `FLJ Main Output`, `Remote Out 1`, and `Remote Out 2`.
- Extended `MiniNinjamServer` and the routing suite so RED coverage now fails on missing `Main` label propagation, missing second-local metadata, hardcoded upload channel `0`, and the missing third host-facing output.
- Added RED mixer or restore coverage plus a manual Ableton matrix for the local-header UX, remote output dropdown, hidden-slot restore, and routed-output persistence expectations.

## Task Commits

Each task was committed atomically:

1. **Task 1: Lock fixed-slot routing contracts and RED transport coverage** - `603cdf0` (test)
2. **Task 2: Add RED editor or restore coverage and write the manual Ableton matrix** - `29388ff` (test)

## Files Created/Modified
- `src/plugin/FamaLamaJamAudioProcessor.h` - Declares the fixed local-slot and fixed remote-output contracts plus narrow test helpers.
- `src/audio/CodecStreamBridge.h` - Adds compatibility shims for channel-indexed local metadata without changing runtime behavior yet.
- `src/net/FramedSocketTransport.h` - Adds compatibility shims for channel-indexed outbound metadata contracts.
- `tests/integration/support/MiniNinjamServer.h` - Captures outbound client channel-info and upload-begin metadata for RED assertions.
- `tests/integration/plugin_host_multi_io_routing_tests.cpp` - Failing routing coverage for fixed slot counts, labels, and channel-indexed uploads.
- `src/plugin/FamaLamaJamAudioProcessorEditor.h` - Declares the local-header and remote-output label seams for the next implementation step.
- `tests/integration/plugin_mixer_ui_tests.cpp` - Failing UI coverage for the local header, add-channel affordance, inline rename, and remote output selector.
- `tests/integration/plugin_state_roundtrip_tests.cpp` - Failing restore coverage for hidden extra locals and routed-output assignments.
- `docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md` - Manual Ableton validation rows for ROUTE-02 and ROUTE-03.

## Decisions Made

- Locked Wave 0 to exactly two local send slots and three host-facing output labels so Phase 08.3.4.1 cannot drift back into dynamic-bus scope.
- Used header-first compatibility seams and protocol-boundary capture so the RED tests describe the target behavior while the proof runtime remains unchanged.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 can implement against explicit failing coverage for fixed local slot identity, channel-indexed upload metadata, local-header UI, remote output routing, and restore behavior.
- Current RED failures are the expected blockers: missing `Main` local metadata, no second local upload identity, no third fixed output pair, no local-header controls, and no routed-output persistence.

## Self-Check: PASSED

- FOUND: `.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-01-SUMMARY.md`
- FOUND: `603cdf0`
- FOUND: `29388ff`
