---
phase: 08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/audio/CodecStreamBridge.h
  - src/net/FramedSocketTransport.h
  - tests/integration/support/MiniNinjamServer.h
  - tests/integration/plugin_host_multi_io_routing_tests.cpp
  - tests/integration/plugin_mixer_ui_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md
autonomous: true
requirements:
  - ROUTE-02
  - ROUTE-03
user_setup: []
must_haves:
  truths:
    - The fixed-bus implementation is locked to exactly two local transmit slots and a small fixed routed-output cap before runtime work begins.
    - Automation fails on missing second-local upload identity, missing hide-and-remember restore behavior, and missing remote main-output exclusion before implementation starts.
    - Ableton validation has a concrete matrix for `Main`, `Local Send 2`, `FLJ Main Output`, `Remote Out 1`, and `Remote Out 2`.
  artifacts:
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Fixed-slot routing contracts and processor-facing local or remote assignment seams.
      contains: Local Send 2
    - path: tests/integration/plugin_host_multi_io_routing_tests.cpp
      provides: RED routing coverage for outbound local channel indices, fixed output assignments, and non-mirrored remote routing.
      min_lines: 250
    - path: tests/integration/plugin_mixer_ui_tests.cpp
      provides: RED editor coverage for add-channel, global local controls, rename, and remote output dropdown behavior.
      min_lines: 250
    - path: tests/integration/plugin_state_roundtrip_tests.cpp
      provides: RED restore coverage for hidden extra local slots and remote output assignments.
      min_lines: 450
    - path: docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md
      provides: Manual Ableton matrix for the fixed-bus multi-input and remote-output workflow.
      contains: P8.3.4.1-LOCAL-01
  key_links:
    - from: tests/integration/plugin_host_multi_io_routing_tests.cpp
      to: src/audio/CodecStreamBridge.h
      via: The routing tests must assert channel-indexed outbound local uploads instead of the current single-payload queue.
      pattern: channelIndex|submitInput|popEncoded|Local Send 2
    - from: tests/integration/plugin_mixer_ui_tests.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.h
      via: The editor tests must compile against explicit local-header and remote-dropdown seams rather than child-index guesses.
      pattern: Add channel|FLJ Main Output|Remote Out 1|Remote Out 2
    - from: docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md
      to: .planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-VALIDATION.md
      via: The manual matrix and phase validation contract must describe the same fixed-bus cap and host-facing labels.
      pattern: ROUTE-02|ROUTE-03|Local Send 2|Remote Out
---

<objective>
Define the executable Wave 0 contract for the first fixed-bus implementation: fixed local-slot identities, fixed routed-output labels, RED coverage, and a manual Ableton matrix before runtime code changes begin.

Purpose: The implementation phase needs explicit contracts for transport metadata, processor slot state, editor affordances, and host validation so execution does not drift into dynamic bus work or a premature Phase 9 redesign.
Output: Header-level contracts, failing runtime and UI or state tests, and a manual matrix for Ableton validation.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-CONTEXT.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-RESEARCH.md
@.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-VALIDATION.md
@.planning/phases/08.3.4-host-multi-io-routing-research/08.3.4-03-SUMMARY.md
@docs/validation/phase8.3.4-host-multi-io-routing-matrix.md
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/audio/CodecStreamBridge.h
@src/net/FramedSocketTransport.h
@tests/integration/plugin_host_multi_io_routing_tests.cpp
@tests/integration/plugin_mixer_ui_tests.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp
@tests/integration/support/MiniNinjamServer.h

<interfaces>
From src/plugin/FamaLamaJamAudioProcessor.h:
```cpp
static constexpr const char* kLocalMonitorSourceId = "local-monitor";
static constexpr const char* kHostRoutingProofAuxInputBusName = "Local Send 2";
static constexpr const char* kHostRoutingProofAuxOutputBusName = "Remote Out 1";
[[nodiscard]] bool getMixerStripSnapshot(const std::string& sourceId, MixerStripSnapshot& snapshot) const;
[[nodiscard]] std::vector<MixerStripSnapshot> getMixerStripSnapshots() const;
```

From src/audio/CodecStreamBridge.h:
```cpp
void submitInput(const juce::AudioBuffer<float>& input, double sampleRate);
bool popEncoded(juce::MemoryBlock& payload);
```

From src/net/FramedSocketTransport.h:
```cpp
void enqueueOutbound(const juce::MemoryBlock& payload);
void setLocalChannelInfo(std::string channelName, std::uint8_t channelFlags);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock fixed-slot routing contracts and RED transport coverage</name>
  <files>src/plugin/FamaLamaJamAudioProcessor.h, src/audio/CodecStreamBridge.h, src/net/FramedSocketTransport.h, tests/integration/support/MiniNinjamServer.h, tests/integration/plugin_host_multi_io_routing_tests.cpp</files>
  <behavior>
    - Test 1: The processor contract exposes exactly two local transmit slots with stable IDs and fixed fallback labels: `Main` and `Local Send 2`.
    - Test 2: The transport or codec contract can carry per-local-channel metadata and channel index so `Main` is asserted as channel `0` and `Local Send 2` as channel `1`.
    - Test 3: Remote output assignment contracts are fixed-cap and use explicit host-facing labels `FLJ Main Output`, `Remote Out 1`, and `Remote Out 2`, with routed remotes leaving main completely.
  </behavior>
  <action>Replace the ad hoc `HostRoutingProof` implementation shape with explicit fixed-slot or fixed-route contract declarations in headers only. Lock this phase to exactly two local transmit slots and exactly two extra routed output pairs; this is the planner decision recommended by research to satisfy the host-facing dropdown labels without reopening dynamic bus growth. Extend `MiniNinjamServer` so tests can capture client channel-info messages and upload-begin channel indices, then extend `plugin_host_multi_io_routing_tests.cpp` so the suite compiles against the new contracts and fails on the current single-channel runtime. Do not implement processor or transport behavior yet, do not add dynamic bus APIs, and do not introduce plugin-owned Ableton track enumeration.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_voice_mode_transport]"</automated>
  </verify>
  <done>The focused routing suite compiles against fixed-slot contracts and fails because runtime behavior still only supports the proof implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED editor or restore coverage and write the manual Ableton matrix</name>
  <files>src/plugin/FamaLamaJamAudioProcessorEditor.h, tests/integration/plugin_mixer_ui_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md</files>
  <behavior>
    - Test 1: The editor has one local-group header for global `Transmit` and `Voice`, an inline `Add channel` action, editable local names, and no per-local-strip duplicate global controls.
    - Test 2: Removing the extra local channel hides it but preserves its name and per-strip mix state for restore.
    - Test 3: Remote strips expose a small output dropdown using `FLJ Main Output`, `Remote Out 1`, and `Remote Out 2`, and invalid restored assignments fall back to main.
  </behavior>
  <action>Add only the minimal editor-facing test hooks required for RED coverage, then extend the mixer and state-roundtrip suites so they fail on the current singleton-local and proof-routing behavior. In the same task, create `docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md` with the manual rows from research for `Main` plus `Local Send 2`, separate local-channel visibility from another NINJAM client, routed-remote isolation, aux silence when unassigned, and restore behavior after reopen. Keep the matrix and tests tightly aligned to the locked UX: full extra local strips, editable names, global local `Transmit` and `Voice`, small remote-strip dropdown, no dynamic bus growth, and no Phase 9 layout work.</action>
  <verify>
    <automated>build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip]"</automated>
  </verify>
  <done>The editor and restore suites fail for the right fixed-bus reasons, and the manual matrix exists with explicit ROUTE-02 and ROUTE-03 rows.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_host_multi_io_routing],[plugin_voice_mode_transport]"`
- [ ] `build-vs-2026/tests/Debug/famalamajam_tests.exe "[plugin_mixer_ui],[plugin_state_roundtrip]"`
- [ ] `rg -n "P8\\.3\\.4\\.1-LOCAL-01|P8\\.3\\.4\\.1-ROUTE-03|FLJ Main Output|Remote Out 1|Remote Out 2" docs/validation/phase8.3.4.1-fixed-bus-routing-matrix.md`
</verification>

<success_criteria>
- Wave 0 covers all missing automated and manual validation seams called out by research
- The fixed implementation shape is locked before runtime work: two local slots, two extra routed output pairs, host-owned routing, and no dynamic bus growth
- Execution can proceed against explicit contracts instead of extending `HostRoutingProof` ad hoc
</success_criteria>

<output>
After completion, create `.planning/phases/08.3.4.1-fixed-bus-multi-input-and-ninjam-channel-implementation/08.3.4.1-01-SUMMARY.md`
</output>
