---
phase: 08-server-discovery-history
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/infra/net/PublicServerDiscoveryClient.h
  - src/infra/net/PublicServerDiscoveryClient.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - src/plugin/FamaLamaJamAudioProcessorEditor.h
  - src/plugin/FamaLamaJamAudioProcessorEditor.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
  - tests/integration/plugin_server_discovery_ui_tests.cpp
  - tests/integration/support/FakeServerDiscoveryClient.h
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Users can choose from one combined discovery workflow containing remembered private servers first and public servers after, without losing visible manual host/port entry.
    - Selecting a discovery entry only populates editable host/port draft fields; it does not silently connect or lock the user into picker-only mode.
    - Public discovery failure and stale-cache states are explicit and recoverable while manual connect and remembered history remain usable.
    - Picker rows surface richer server-identifying information, including connected-user count when available, while staying compact enough for the current single-page editor.
  artifacts:
    - path: src/infra/net/PublicServerDiscoveryClient.cpp
      provides: Dedicated background fetch orchestration for the public server list rather than blocking editor timers or the audio path.
      contains: requestRefresh
    - path: src/plugin/FamaLamaJamAudioProcessor.cpp
      provides: Public-list refresh/cache/state logic and the combined discovery snapshot consumed by the editor.
      contains: refreshPublicServerList
    - path: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      provides: Compact combined picker UI, manual-field population behavior, and compact status-line/error presentation.
      contains: refreshServerDiscoveryUi
    - path: tests/integration/plugin_server_discovery_ui_tests.cpp
      provides: Editor-level coverage for combined ordering, manual-field population, and direct-workflow non-regression.
      min_lines: 160
    - path: tests/integration/support/FakeServerDiscoveryClient.h
      provides: Deterministic success/failure/stale scenarios for discovery tests without relying on the live internet.
      contains: FakeServerDiscoveryClient
  key_links:
    - from: src/infra/net/PublicServerDiscoveryClient.cpp
      to: src/plugin/FamaLamaJamAudioProcessor.cpp
      via: Dedicated worker-based refresh results publish compact snapshot updates back into processor-owned discovery state.
      pattern: requestRefresh|handlePublicServerListResponse
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      via: Processor-owned discovery snapshot drives the picker rows and lightweight status line without editor-owned fetch logic.
      pattern: getServerDiscoveryUiState
    - from: src/plugin/FamaLamaJamAudioProcessorEditor.cpp
      to: tests/integration/plugin_server_discovery_ui_tests.cpp
      via: UI tests assert combined ordering, host/port population, and continued manual editability after selection.
      pattern: clickConnectForTesting|setSettingsDraftForTesting
---

<objective>
Add public-list discovery behavior and wire the compact combined picker into the existing connection area.

Purpose: Fulfill the public-server selection requirement without degrading the validated manual host/port workflow.
Output: Public discovery refresh/cache/failure behavior, combined picker UI, and editor integration coverage for selection and manual-edit flows.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-server-discovery-history/08-CONTEXT.md
@.planning/phases/08-server-discovery-history/08-RESEARCH.md
@.planning/phases/08-server-discovery-history/08-VALIDATION.md
@src/infra/state/SessionSettingsSerializer.cpp
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@src/plugin/FamaLamaJamAudioProcessorEditor.h
@src/plugin/FamaLamaJamAudioProcessorEditor.cpp
@tests/integration/plugin_rehearsal_ui_flow_tests.cpp
@tests/integration/plugin_room_controls_ui_tests.cpp

<interfaces>
From `src/plugin/FamaLamaJamAudioProcessor.h` after 08-01:
```cpp
struct ServerDiscoveryEntry;
struct ServerDiscoveryUiState;
[[nodiscard]] ServerDiscoveryUiState getServerDiscoveryUiState() const;
```

Add editor/testing contracts needed for Phase 8:
```cpp
[[nodiscard]] std::vector<juce::String> getVisibleServerDiscoveryLabelsForTesting() const;
[[nodiscard]] juce::String getServerDiscoveryStatusTextForTesting() const;
bool selectServerDiscoveryEntryForTesting(int index);
```

Do not fetch the public list from the existing editor timer or any audio-thread path. Use a dedicated worker/client seam so UI refresh remains non-blocking and testable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED UI coverage for the combined picker and manual draft integration</name>
  <files>tests/integration/plugin_server_discovery_ui_tests.cpp, tests/integration/support/FakeServerDiscoveryClient.h, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Remembered/private entries appear before public entries in one discovery surface.
    - Test 2: Selecting an entry populates editable host and port fields without triggering a connection.
    - Test 3: Users can still type over populated host/port values and connect manually.
    - Test 4: Stale/failure status remains visible while the picker and manual fields stay usable.
  </behavior>
  <action>Create `tests/integration/plugin_server_discovery_ui_tests.cpp` and a small fake discovery client seam first, then register the UI tests with a `[plugin_server_discovery_ui]` tag. Reuse the existing editor-harness style from the rehearsal and room UI tests instead of inventing a second UI test pattern. Keep the assertions focused on the connection section behavior and compact status copy rather than broad geometry changes that belong to the later layout phase.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery_ui]"'</automated></verify>
  <done>Phase 8 has editor-level RED coverage for the combined picker workflow before any UI changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement public discovery refresh, stale/failure state, and compact picker UI</name>
  <files>src/infra/net/PublicServerDiscoveryClient.h, src/infra/net/PublicServerDiscoveryClient.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, src/plugin/FamaLamaJamAudioProcessorEditor.h, src/plugin/FamaLamaJamAudioProcessorEditor.cpp, tests/integration/plugin_server_discovery_tests.cpp, tests/integration/plugin_server_discovery_ui_tests.cpp, tests/integration/support/FakeServerDiscoveryClient.h</files>
  <behavior>
    - Test 1: Public server entries can be loaded into discovery state, merged after remembered entries, and rendered with richer row labels including connected-user count when available.
    - Test 2: If refresh fails after successful public data exists, the snapshot marks the public list stale and presents recoverable status text.
    - Test 3: Selecting any discovery entry updates the current host and port draft fields while leaving them editable.
    - Test 4: Manual connect remains usable even when public discovery is unavailable or stale.
  </behavior>
  <action>Implement the public-list refresh/cache path through a dedicated worker/client seam rather than editor-owned network logic or the existing editor timer. Parsing should be defensive and should tolerate missing fields, while still surfacing richer row labels and connected-user count when available from the upstream-style public list source. Keep the public cache runtime-only for Phase 8, while remembered history remains persisted from 08-01. In the editor, add a compact combined picker and lightweight status line in the existing connection area, keeping manual host/port visible and editable at all times. Entry selection should only prefill the draft fields. Use the fake discovery client in tests so stale/failure states and merge behavior are deterministic.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"'</automated></verify>
  <done>The plugin now exposes a compact public/private combined discovery workflow that preserves manual entry and honest stale/failure behavior.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]"'`

Phase-level verification criteria (applies after all Phase 8 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the combined picker remains readable in the plugin window, manual host/port stays convenient, and stale/failure status is understandable.
</verification>

<success_criteria>
- All tasks completed
- DISC-01 public discovery behavior is implemented with recoverable stale/failure handling
- DISC-02 remembered/private entries are integrated into the same workflow without harming manual entry
- The current single-page connection area remains compact enough to defer broader layout work to Phase 9
</success_criteria>

<output>
After completion, create `.planning/phases/08-server-discovery-history/08-02-SUMMARY.md`
</output>
