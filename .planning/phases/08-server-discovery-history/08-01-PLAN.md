---
phase: 08-server-discovery-history
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/session/ServerDiscoveryModel.h
  - src/app/session/ServerDiscoveryModel.cpp
  - src/infra/state/SessionSettingsSerializer.h
  - src/infra/state/SessionSettingsSerializer.cpp
  - src/plugin/FamaLamaJamAudioProcessor.h
  - src/plugin/FamaLamaJamAudioProcessor.cpp
  - tests/unit/server_discovery_parser_tests.cpp
  - tests/integration/plugin_server_discovery_tests.cpp
  - tests/integration/plugin_state_roundtrip_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - The plugin persists a short remembered-server history based only on successful connections, not failed attempts or transient draft edits.
    - Remembered/private entries are available to the UI as processor-owned discovery state, with remembered servers ordered ahead of public servers in the eventual combined workflow.
    - Discovery state distinguishes persisted remembered history from transient public-list fetch status so stale or in-progress network state is not treated like durable project settings.
    - The active manual host and port workflow remains the actual connection source of truth; Phase 8 foundation work must not force an immediate-connect picker model.
  artifacts:
    - path: src/app/session/ServerDiscoveryModel.h
      provides: Shared discovery-entry, remembered-history, and parser-facing contracts that stay separate from the active connection draft model.
      contains: RememberedServerEntry
    - path: src/infra/state/SessionSettingsSerializer.cpp
      provides: Wrapped plugin-state persistence support for remembered-server history without polluting active session-settings validation.
      contains: toValueTree
    - path: src/plugin/FamaLamaJamAudioProcessor.h
      provides: Processor-owned discovery snapshot contracts exposed to the editor and tests.
      contains: getServerDiscoveryUiState
    - path: tests/integration/plugin_server_discovery_tests.cpp
      provides: Integration coverage for successful-history updates, bounded ordering, and separation of remembered state from transient fetch state.
      min_lines: 140
    - path: tests/unit/server_discovery_parser_tests.cpp
      provides: Parser and endpoint-normalization coverage for the public-list text format before transport/editor wiring begins.
      min_lines: 100
  key_links:
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: src/infra/state/SessionSettingsSerializer.cpp
      via: Successful lifecycle transitions update remembered-server state through the wrapped plugin-state path while active draft settings remain separate.
      pattern: ConnectionEventType::Connected|rememberSuccessfulServer
    - from: src/plugin/FamaLamaJamAudioProcessor.cpp
      to: tests/integration/plugin_server_discovery_tests.cpp
      via: Tests prove remembered history changes only after real connection success and remains bounded in recency order.
      pattern: getServerDiscoveryUiState
---

<objective>
Build the persisted history and processor-owned discovery-state foundation for Phase 8.

Purpose: Establish the remembered-server model and discovery snapshot contracts before adding public-list fetching or picker UI.
Output: Bounded successful-connection history, processor discovery getters, and integration coverage proving the persistence/update rules.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-server-discovery-history/08-CONTEXT.md
@.planning/phases/08-server-discovery-history/08-RESEARCH.md
@.planning/phases/08-server-discovery-history/08-VALIDATION.md
@src/app/session/SessionSettings.h
@src/infra/state/SessionSettingsSerializer.h
@src/infra/state/SessionSettingsSerializer.cpp
@src/plugin/FamaLamaJamAudioProcessor.h
@src/plugin/FamaLamaJamAudioProcessor.cpp
@tests/integration/plugin_state_roundtrip_tests.cpp
@tests/CMakeLists.txt

<interfaces>
From `src/app/session/SessionSettings.h`:
```cpp
struct SessionSettings
{
    std::string serverHost;
    std::uint16_t serverPort;
    std::string username;
    float defaultChannelGainDb;
    float defaultChannelPan;
    bool defaultChannelMuted;
};
```

Create these exact Phase 8 contracts before implementation:
```cpp
struct RememberedServerEntry
{
    std::string host;
    std::uint16_t port { 0 };
};

struct ServerDiscoveryEntry
{
    enum class Source { Remembered, Public };
    Source source { Source::Remembered };
    std::string label;
    std::string host;
    std::uint16_t port { 0 };
    int connectedUsers { -1 };
    bool stale { false };
};

struct ServerDiscoveryUiState
{
    bool fetchInProgress { false };
    bool hasStalePublicData { false };
    std::string statusText;
    std::vector<ServerDiscoveryEntry> combinedEntries;
};

[[nodiscard]] ServerDiscoveryUiState getServerDiscoveryUiState() const;
```

Keep remembered-history persistence separate from the active `SessionSettings` validation path. The active draft remains host/port/username/channel defaults only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED discovery-history coverage and register the Phase 8 tests</name>
  <files>tests/unit/server_discovery_parser_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: Successful connection updates remembered history, while failed connection attempts and unsaved manual edits do not.
    - Test 2: Remembered history is bounded to a short recent list and promotes a re-used server instead of duplicating it.
    - Test 3: Plugin state roundtrip preserves remembered private servers while excluding transient discovery status like in-progress fetch state.
    - Test 4: Public-list parser helpers tolerate the upstream `SERVER ... END` format, malformed rows, and optional user-count extraction without requiring editor code.
    - Test 5: The processor exposes a discovery snapshot even before any editor work exists, so later plans can consume stable contracts.
  </behavior>
  <action>Create `tests/unit/server_discovery_parser_tests.cpp` and `tests/integration/plugin_server_discovery_tests.cpp` first, then register them in `tests/CMakeLists.txt` with `[server_discovery_parser]` and `[plugin_server_discovery]` tags. Extend `plugin_state_roundtrip_tests.cpp` only where needed to prove remembered-history persistence and transient-state exclusion. Keep the initial tests focused on parser correctness, remembered private-server behavior, and processor-visible discovery state; do not pull in UI layout assertions yet.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery]"'</automated></verify>
  <done>Wave 0 discovery-history tests exist and fail for the right reasons before foundation code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement remembered-server persistence, parser helpers, and processor discovery snapshot contracts</name>
  <files>src/app/session/ServerDiscoveryModel.h, src/app/session/ServerDiscoveryModel.cpp, src/infra/state/SessionSettingsSerializer.h, src/infra/state/SessionSettingsSerializer.cpp, src/plugin/FamaLamaJamAudioProcessor.h, src/plugin/FamaLamaJamAudioProcessor.cpp, tests/unit/server_discovery_parser_tests.cpp, tests/integration/plugin_server_discovery_tests.cpp, tests/integration/plugin_state_roundtrip_tests.cpp</files>
  <behavior>
    - Test 1: A real successful `Connected` lifecycle transition records the active host/port in remembered history.
    - Test 2: Reconnecting to an existing remembered server moves it to the most-recent position without duplication.
    - Test 3: Remembered history is bounded to the agreed short list and survives plugin state save/restore.
    - Test 4: Public-list parser helpers normalize endpoints and tolerate missing user counts or non-classic room-info text.
    - Test 5: `getServerDiscoveryUiState()` returns remembered entries and transient fetch status separately, without implying any direct-connect or picker-owned source of truth.
  </behavior>
  <action>Create a small discovery/history helper model that owns endpoint normalization, parser logic for upstream `SERVER ... END` payloads, and bounded remembered-history behavior. Persist remembered history through the wrapped plugin-state serializer instead of broadening `SessionSettings` validation. In the processor, add the exact `ServerDiscoveryEntry` and `ServerDiscoveryUiState` contracts from the interface block and implement `getServerDiscoveryUiState()` so the editor can later consume remembered entries plus transient status text from one snapshot. Update the lifecycle success path so remembered history changes only after a real successful connection using the active settings draft already applied by the normal workflow. Do not add real public-list fetching yet; keep public-cache state runtime-only and empty/default so the contracts are stable before the fetch layer arrives.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery],[plugin_state_roundtrip]"'</automated></verify>
  <done>The plugin now has persisted remembered history and stable processor-owned discovery state that later UI/fetch work can build on safely.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cmake --build build-vs --target famalamajam_tests --config Debug'`
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[server_discovery_parser],[plugin_server_discovery],[plugin_state_roundtrip]"'`

Phase-level verification criteria (applies after all Phase 8 plans finish):
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && ctest --test-dir build-vs --output-on-failure'`
- [ ] Manual Ableton validation confirms the combined picker remains readable, manual host/port still works naturally, and stale/failure status is understandable.
</verification>

<success_criteria>
- All tasks completed
- DISC-02 persistence and recency rules are implemented with deterministic integration coverage
- Processor discovery contracts exist before UI work starts
- The validated manual connection workflow remains the source of truth for actual connections
</success_criteria>

<output>
After completion, create `.planning/phases/08-server-discovery-history/08-01-SUMMARY.md`
</output>
