---
phase: 08-server-discovery-history
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .planning/phases/08-server-discovery-history/08-VERIFICATION.md
  - docs/validation/phase8-server-discovery-matrix.md
  - .planning/phases/08-server-discovery-history/08-03-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Phase 8 closes with both automated evidence and a focused manual check that discovery remains compact and usable inside the real plugin window.
    - Full-suite validation proves the new discovery/history features do not regress the already validated connection, room, mixer, and sync workflows.
    - The final verification record clearly separates implemented behavior from any deferred or still-uncertain public-list source details.
  artifacts:
    - path: .planning/phases/08-server-discovery-history/08-VERIFICATION.md
      provides: Final automated and manual validation record for DISC-01 and DISC-02.
      contains: DISC-01
    - path: docs/validation/phase8-server-discovery-matrix.md
      provides: Manual Ableton checklist for combined picker readability, stale/failure messaging, and manual-entry non-regression.
      min_lines: 40
  key_links:
    - from: .planning/phases/08-server-discovery-history/08-VALIDATION.md
      to: .planning/phases/08-server-discovery-history/08-VERIFICATION.md
      via: Final verification should reference the planned automated gates and manual-only checks rather than inventing new acceptance criteria late.
      pattern: plugin_server_discovery
---

<objective>
Run the final validation gate for Phase 8 and record the result cleanly.

Purpose: Close the phase with durable evidence that server discovery/history works without regressing the validated rehearsal workflow.
Output: Automated gate results, manual validation matrix, verification record, and updated state.
</objective>

<execution_context>
@C:/Users/Dirk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dirk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-server-discovery-history/08-CONTEXT.md
@.planning/phases/08-server-discovery-history/08-RESEARCH.md
@.planning/phases/08-server-discovery-history/08-VALIDATION.md
@.planning/phases/08-server-discovery-history/08-01-PLAN.md
@.planning/phases/08-server-discovery-history/08-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated Phase 8 gate and rebuild the manual-test plugin artifact</name>
  <files>.planning/phases/08-server-discovery-history/08-VERIFICATION.md</files>
  <action>Run the targeted discovery tags, then the full suite, then rebuild the Debug VST3 artifact on the verified `build-vs` path. Capture the exact commands, pass/fail totals, and any residual warnings in `08-VERIFICATION.md`. If targeted and full-suite evidence disagree, stop and document the gap instead of inferring success.</action>
  <verify><automated>cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]" && ctest --test-dir build-vs --output-on-failure && cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug"'</automated></verify>
  <done>The phase has a fresh plugin artifact and explicit automated evidence, not just inferred readiness.</done>
</task>

<task type="manual">
  <name>Task 2: Capture the focused Ableton/manual validation matrix</name>
  <files>docs/validation/phase8-server-discovery-matrix.md, .planning/phases/08-server-discovery-history/08-VERIFICATION.md</files>
  <action>Create a short manual matrix covering: remembered private server appears after successful connection, remembered entries appear before public entries, selecting an entry populates editable host/port, manual typing still works normally, stale/failure status is understandable, and the compact picker remains readable without crowding the page excessively. Pause for the user to test the built VST3 in Ableton, then record pass/fail outcomes in both the matrix and the verification report.</action>
  <verify><manual>User tests the Phase 8 matrix in Ableton and reports pass/fail notes for each case.</manual></verify>
  <done>Phase 8 finishes with direct host-window evidence, especially around UI compactness and manual-flow non-regression.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase cleanly in planning state</name>
  <files>.planning/phases/08-server-discovery-history/08-VERIFICATION.md, .planning/phases/08-server-discovery-history/08-03-SUMMARY.md, .planning/STATE.md</files>
  <action>After automation and manual validation complete, write the final Phase 8 summary and update `STATE.md` so the roadmap points to the next phase discussion. Carry forward any residual risk, such as limitations of the public list source or remaining layout pressure, instead of silently dropping it.</action>
  <verify><automated>powershell -Command "Select-String -Path .planning\\phases\\08-server-discovery-history\\08-VERIFICATION.md,.planning\\STATE.md -Pattern 'Phase 8|Server Discovery|DISC-01|DISC-02'"</automated></verify>
  <done>The project can hand off to Phase 9 without ambiguity about what Phase 8 actually achieved and what remains deferred.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cmd /c 'call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && .\build-vs\tests\famalamajam_tests.exe "[plugin_server_discovery],[plugin_server_discovery_ui]" && ctest --test-dir build-vs --output-on-failure && cmake --build build-vs --target famalamajam_plugin_VST3 --config Debug'`
- [ ] Manual Phase 8 validation matrix completed in Ableton

Phase-level verification criteria:
- [ ] Discovery/history behavior is documented as verified or explicitly flagged with residual risk
- [ ] The built VST3 path is recorded for the user
- [ ] `STATE.md` is ready to route to the next roadmap phase
</verification>

<success_criteria>
- All tasks completed
- DISC-01 and DISC-02 have both automated and manual evidence
- The plugin artifact is rebuilt for user testing
- Phase 8 closes with explicit residual-risk notes instead of hidden assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/08-server-discovery-history/08-03-SUMMARY.md`
</output>
